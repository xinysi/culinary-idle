// 一次性：v1.8.4 文档同步（信箱放宽）+ 设计文档 §11.17 + AGENTS.md 条目改写
import fs from 'fs'

// ── ① README ──
const R = 'README.md'
let r = fs.readFileSync(R, 'utf8')
r = r.replace('> **当前版本：v1.8.3**（2026-09-11）', '> **当前版本：v1.8.4**（2026-09-11）')
r = r.replace('  git tag v1.8.3 && git push origin v1.8.3', '  git tag v1.8.4 && git push origin v1.8.4')
const ROW = [
  '| **v1.8.4** | 2026-09-11 | **信箱放宽：溢出零丢失 + 系统奖励到账**（玩法数值零改动）。**先实测出两处旧隐患**：① 把信箱塞到 60 封未领后，后续溢出**被拒收 → 物品真的蒸发**（正是信箱本要修的毛病）；',
  '② 合并只看「最后一封」，交替溢出 A、B、A、C、B 会产生 **5 封**。改法：**溢出邮件按物品合并**（同物品永远一封，上述例子降到 3 封）；**软上限 60→200 且全未领也照收**，只有硬上限 500 才止收（实测 260 种物品溢出：**丢失 0 种**）；',
  '**新增「🎁 奖励到账」通道**，并把**赛季档位**接上——**领档即记账**（`claimed` 立刻标记，赛季进度与 seasonAll/season5 成就阈值不受影响），奖励改为邮件到账、到信箱统一领；金币类不占背包格必然能领，**物品类背包满时也先安全存着**；',
  '硬上限拒收（几乎不可达）时回退直接发放，保证奖励永不丢。**为什么不担心信箱变成无限仓库**：信箱里的东西不能用，只是「暂时取不走」的缓冲，不消解背包扩容的意义；而拒收的代价是物品蒸发。另：`system_test` C11 扩容到 **42 项**（含「溢出零丢失」这条锁死旧 bug 的断言） |',
].join('')
const anchor = '| **v1.8.3** | 2026-09-11 |'
if (!r.includes(anchor)) throw new Error('未找到 v1.8.3 行')
r = r.replace(anchor, ROW + '\n' + anchor)
// 系统表里的信箱行同步
r = r.replace(
  '| 📬 信箱 | **背包满不再丢东西**：溢出（新种类放不下 / 触到堆叠上限）自动转存待领，上限 60 封；离线结算回执留档（产出仍在结算时直接发放）；一键领取 / 分类筛选 / 侧栏待领角标 |',
  '| 📬 信箱 | **背包满不再丢东西**：溢出（新种类放不下 / 触到堆叠上限）自动转存待领，**按物品合并、软上限 200 封且全未领也照收**（实测 260 种溢出零丢失）；**系统奖励到账**（赛季档位等，领档即记账、奖励寄到信箱统一领）；离线结算回执留档；一键领取 / 分类筛选 / 侧栏待领角标 |'
)
fs.writeFileSync(R, r)
console.log('① README:', /当前版本：v1\.8\.4/.test(r), '| 历史行:', /v1\.8\.4.*信箱放宽/.test(r), '| 系统表:', r.includes('按物品合并、软上限 200 封'))

// ── ② AGENTS.md 信箱条目改写 ──
const A = 'AGENTS.md'
let a = fs.readFileSync(A, 'utf8')
const oldPos = '- **定位：只做「兜底 + 留档」，绝不接管既有奖励的发放路径**。三条来源：① `overflow` 溢出转存 ② `offline` 离线回执（产出仍由 `settleOffline` 直接发放，邮件只是明细）③ `welcome` 新档欢迎信（**不带附件**，不动物值曲线）。**新增任何「送东西」的功能仍走原来的 `gainGold`/`gainItem`**，不要图省事塞进信箱。'
const newPos = '- **定位：兜底 + 留档 + 系统奖励到账**（2026-09-11 起放宽，见设计文档 §11.17）。四类来源：① `overflow` 溢出转存 ② `reward` **系统奖励到账**（目前用于赛季档位：**领档即记账**，`claimed` 立刻标记所以赛季进度/成就阈值不受影响，奖励改为邮件到账）③ `offline` 离线回执（产出仍由 `settleOffline` 直接发放，邮件只是明细）④ `welcome` 新档欢迎信（不带附件）。\n  **判断标准**：奖励**可以**走邮件（但要「先记账、后到账」，并在投递被拒时回退直接发放）；而**结算类的既有产出**（离线、采集、制作等）仍走原来的 `gainGold`/`gainItem` 直接进包，不要改成邮件。'
if (a.includes(oldPos)) { a = a.replace(oldPos, newPos); console.log('② AGENTS 定位条目已改写') } else console.log('  ⚠️ AGENTS 定位条目未匹配')

const oldCap = '- **上限 60 封**：满了先淘汰最旧的「已领或无附件」邮件；若 60 封全是未领附件则**拒收**（`sendMail` 返回 `null`，`_fileOverflowMail` 返回 `false` → 此时才回落到 `inventory:full` 事件）——刻意保留「背包不够就得扩容」的压力，**别把信箱改成无限仓库**。'
const newCap = '- **容量（2026-09-11 放宽，勿改回拒收）**：软上限 `MAIL_CAP=200` 只淘汰「已领或无附件」的旧邮件；**全是未领附件也照收**，到硬上限 `MAIL_HARD_CAP=500` 才拒收（`sendMail` 返回 `null`，`_fileOverflowMail` 返回 `false`，此时才回落 `inventory:full` 事件）。配合「溢出邮件**按物品**合并」，封数≈有溢出的物品种类数。\n  ⚠️ 实测教训：改前「60 封全未领就拒收」会让物品**真的蒸发**（实测 21 次拒收），而那正是信箱本要修的毛病。**不要**用「封数没增长」判断是否被拒收——**淘汰**（清理已领/无附件旧邮件）同样不会让长度增长；要判就判「物品在不在背包或某封邮件里」（C11 的「溢出零丢失」断言就是这么写的）。'
if (a.includes(oldCap)) { a = a.replace(oldCap, newCap); console.log('② AGENTS 容量条目已改写') } else console.log('  ⚠️ AGENTS 容量条目未匹配')
fs.writeFileSync(A, a)

// ── ③ 设计文档 §11.17 ──
const D = '《美食放置：食灵山海》设计文档（当前版本）.md'
let d = fs.readFileSync(D, 'utf8')
const i = d.indexOf('## 11.16 左栏功能页体检')
if (i < 0) throw new Error('未找到 §11.16')
const SEC = `## 11.17 信箱放宽：溢出零丢失 + 系统奖励到账（2026-09-11）

§11.13 给信箱定的边界是「只做兜底与留档，绝不接管既有奖励的发放路径」。用户追问「放宽怎么放宽」后，**先实测**再决定，结果发现旧设计里藏着两处隐患。

### 实测发现（决定了先改什么）

1. **拒收 = 物品真的蒸发**：把信箱塞到 60 封未领后继续制造溢出，**21 次被拒收**——也就是说旧设计在最坏情况下会退回到「静默丢东西」，正是信箱本要修的那个毛病。
2. **合并规则太弱**：只与「最后一封」合并，实测交替溢出 A、B、A、C、B 会产生 **5 封**邮件。
3. 体积参考：单封邮件约 236 字节，新档存档 6.6KB → 60 封约 14KB。

### 改法

- **溢出邮件按物品合并**：查全表而非只看最后一封（上述例子 5 → **3 封**），封数≈有溢出的物品种类数。
- **软上限 60 → 200，且不再因满而拒收**：`MAIL_CAP=200` 只淘汰「已领或无附件」的旧邮件；全未领则继续累积，到 `MAIL_HARD_CAP=500` 才拒收做病态保护。实测 260 种物品溢出：**丢失 0 种**。
- **新增 `reward` 奖励到账通道，首个来源是赛季档位**：`seasonClaimTier` **领档即记账**（`claimed` 立刻 push、扣点、+3 见闻都照旧，所以赛季进度与 `seasonAll/season5` 成就阈值完全不受影响），奖励改为寄一封邮件；金币类不占背包格必然能领，**物品类在背包满时也先安全存着**；**硬上限拒收时回退直接发放**，奖励永不丢。
- 配套：`SeasonView` 的成功提示交回 `season:claim` 全局监听（一个动作一条日志，说法统一为「已寄到信箱」）；`MailView` 加「🎁 奖励到账」分类、改写容量说明并补上「信箱里的东西不能用」。

**为什么不担心信箱变成无限仓库**：信箱里的物品**不能使用**，不构成额外存储，只是「暂时取不走」的缓冲，不消解背包扩容的意义；而拒收的代价是物品蒸发——两害相权，取不丢。

### 放宽后仍然保留的边界

奖励**可以**走邮件，但必须「**先记账、后到账**」，且投递被拒时回退直接发放；**结算类的既有产出**（离线、采集、制作）仍走 `gainGold`/`gainItem` 直接进包，不改成邮件。方向四（把每日/周常/成就等既有奖励全部改成邮件发放）经评估**不做**：会动到大量「即时到账」断言，并改变「点领取」的节奏，收益不抵代价。

### 守卫

`system_test.mjs` C11 信箱扩到 **42 项**，本轮新增：**溢出零丢失**（锁死旧 bug 的核心断言）、软上限时优先淘汰无附件旧邮件、按物品合并、硬上限才拒收、赛季领档发邮件且金币不直接进包、领取奖励邮件即到账。

⚠️ 一条测试教训：**不要用「邮件数没增长」判断是否被拒收**——淘汰（清理已领/无附件旧邮件）同样不会让长度增长。改前我就这么判过一次，把一个「淘汰了欢迎信」的正常事件误报成「拒收」，改成断言「物品在不在背包或某封邮件里」才是对的。

`
d = d.slice(0, i) + SEC + d.slice(i)
fs.writeFileSync(D, d)
console.log('③ 设计文档 §11.17 已插入:', fs.readFileSync(D, 'utf8').includes('## 11.17 信箱放宽'))

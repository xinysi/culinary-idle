<script setup>
// 状态分类卡（2026-09-15 立，v2.5.4）——把「页头下方的一串状态」按**类别**分块显示。
//
// 起因（用户）：「农耕页主体内容上面的内容太多了」→ 我把 5 张卡并成 1 张；随后用户要求
// 「根据农耕合并卡所承载的内容，把合并卡适配到所有技能页与功能页；无需合并卡的就不要实装，
//   合并卡在不同页面可以根据页面实际情况进行排版和分类上的变化」。
//
// 结构：容器（一张 card）+ **由页面自己给出的若干「行」**，每行里放若干 `<StatusChip>`。
//   · 一行就够的页面：直接放几块，组件会把它们包进一行（自动折行）。
//   · 需要语义分组的页面（如农耕：第一行规模/状态、第二行产出/规则）：自己写
//     `<div class="status-chips-row">…</div>` 多行即可。
// 样式在 main.css（`.status-chips*` / `.status-chip*`），全部走主题变量 ⇒ 浅色/深色/15 套皮肤自动适配。
</script>

<template>
  <div class="card status-line status-chips">
    <slot />
  </div>
</template>

node_modules/
dist/
.toolchain/
wuguan/
.reasonix/
playwork/
lmewexe/release/

# 原始图片工作目录（2026-09-10 已移入 wuguan/，不再入仓库）
lmewimg/
*.local
.DS_Store
npm-debug.log*

# 桌面打包产物（2026-09-06）
lmewexe/release2/

# 测试产物
/test-results/
/playwright-report/

# ZCode 规划模式生成的计划文件（非项目产物）
.zcode/

# Python 脚本缓存（scripts/gen/process_item_images.py 会生成）
__pycache__/

# 模拟器的暂存输出（原始 CSV 已归档到本地实验记录目录，不入库）
/test-data/

# 样式指纹工具的工作目录 / 临时探针截图（可重建）
.fingerprint/
.probe-*.png

# 🔴 本地内部资料：**只留在本地，绝不入库**（2026-09-21 起）
# 仓库是公开的（在线游玩 + Releases 都靠它），但下面这些是「只给自己/导师看」的一层：
#   AGENTS.md         —— 本地工程笔记
#   docs/             —— 本地资料（设计与实验记录）
#   scripts/sim/      —— 本地仿真与研究工具
#   scripts/measure/  —— 界面成本实测工具
# 已在 2026-09-21 从仓库与**全部 git 历史**里摘除；守卫见 `scripts/ci/audit_sync.mjs` 的「发布面」一条。
/AGENTS.md
/docs/
/scripts/sim/
/scripts/measure/
/scripts/dev/agents_split.mjs
/scripts/dev/agents_assemble.mjs
/scripts/dev/agents_verify.mjs

# 根目录的写作类资料（规格文档 / 美术提示词 / 名册）——同样是「只留本地」
/《美食放置：食灵山海》设计文档（当前版本）.md
/小游戏UI标准.md
/*提示词*
/敌名清单.*
/敌绘描述.*

# filter-branch 中止后可能残留在仓库根的临时目录（勿入库）
/.git-rewrite/

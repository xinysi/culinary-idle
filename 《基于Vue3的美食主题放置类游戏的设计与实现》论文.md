# 基于 Vue 3 的美食主题放置类网页游戏的设计与实现

**摘要**：放置类游戏（Idle Game）以极低操作成本与持续成长的数值反馈著称，是休闲游戏市场的重要品类。本文设计并实现了一款以中华美食文化为主题、融合经营与角色养成要素的放置类网页游戏《食之契约》。游戏基于 Vue 3 组合式 API 与 Pinia 状态管理构建，采用数据驱动设计，将 1275 件物品、20 项技能、467 件装备、28 位首领、40 个赛季、20 家公会等海量内容以纯数据文件形式组织，并通过可插拔的技能类架构实现挂机引擎、数值成长、资源经济、对决战斗、经营模拟等多系统协同。本文阐述了游戏的总体架构设计、核心系统的详细实现（含离线结算算法、并行挂机调度、数值平衡审计、存档体系等），并给出基于 Node.js 的自动化测试方案与结果。测试表明系统 183 项功能断言、75 项系统断言、123 项冒烟断言全部通过，游戏已打包为 Electron 桌面应用分发，运行稳定。

**关键词**：放置游戏；Vue 3；Pinia；游戏数值设计；数据驱动；离线结算

---

## 第一章 绪论

### 1.1 研究背景与意义

移动互联网时代，休闲游戏凭借碎片化、低门槛的特点占据游戏市场半壁江山。其中放置类游戏（Idle/Incremental Game）自 2013 年《Cookie Clicker》兴起后持续演化，以"挂机即可获得资源、离线亦有收益"的核心机制，契合现代人快节奏生活下的娱乐需求。知名作品如《Melvor Idle》《Idle Champions》等验证了该品类的商业价值。

然而，多数放置游戏存在内容深度不足、成长反馈同质化等问题。本课题尝试以**中华美食文化**为题材切入点，将放置挂机、角色养成、装备构筑、餐厅经营、公会社交、赛季活动、故事叙事等多种玩法要素有机融合，构建一款内容丰富、数值自洽的放置游戏《食之契约》，探索放置游戏在题材深度与系统复杂度上的可能性。

### 1.2 国内外研究现状

国外放置游戏以 Melvor Idle（2021）为代表，其将《RuneScape》式的 20+ 技能体系完整搬入放置框架，确立了"多技能并行挂机 + 目标驱动 + 数据可视化"的设计范式；国内放置游戏则多与仙侠、三国题材结合，如《放置奇兵》《剑与远征》的卡牌放置路线。本游戏借鉴 Melvor Idle 的技能体系结构，但在题材、经营元素与叙事完整性上做出差异化设计。

### 1.3 研究内容与论文结构

本文实现一款功能完整的放置游戏，主要研究内容：
1. 放置游戏核心机制（挂机调度、离线结算、并行技能）的设计与实现；
2. 大规模游戏数据（物品/技能/配方/赛季等）的数据驱动组织方式；
3. 多系统（经济、战斗、经营、社交）的数值平衡与审计方法；
4. 基于 Node.js 的自动化测试体系与 Electron 打包分发。

论文共六章：第一章绪论；第二章需求分析；第三章总体设计；第四章核心系统详细设计与实现；第五章系统测试；第六章总结与展望。

---

## 第二章 需求分析

### 2.1 功能性需求

经对放置游戏品类特征与目标用户（碎片时间玩家、数值收集爱好者）的分析，确定系统功能需求如下：

| 编号 | 功能模块 | 需求描述 |
| --- | --- | --- |
| F1 | 挂机采集 | 20 项技能（采摘/垂钓/狩猎/挖掘/农耕/烹饪/烘焙/腌制/调酒/调料/锻造/保鲜/探索/对决×3/食灵召唤/奥义等）可选择目标并行挂机，切换页面不中断 |
| F2 | 制作加工 | 约 700 条制作配方（烹饪 110、烘焙、腌制、调酒 99、调料、锻造、保鲜），含成功/失败、半额经验机制 |
| F3 | 对决战斗 | 三风格相生相克（刀>摆>调>刀，+15%），220 名对手、28 位守星首领（含灼烧/回血/秒杀/三阶段等机制）、持久战 |
| F4 | 装备系统 | 八槽位、六品质、467 件装备、强化（+10%/级，上限 +5）、40 季限定套装 |
| F5 | 经营模拟 | 餐厅（菜单/升级/100 种装饰，离线 80% 效率）、珍馐阁、炼金（208 条配方） |
| F6 | 社交活动 | 20 家公会（被动 buff/每日任务/商店）、40 个赛季（560 天循环）、竞技场（镜像对战） |
| F7 | 收集养成 | 图鉴（1275 件）、89 项成就、32 食灵、28 奥义、称号、卡牌对战 |
| F8 | 叙事引导 | 完整世界观故事（线性七章，随真实进度解锁）、游玩攻略 |
| F9 | 存档管理 | 3 存档位、导入/导出、自动保存、离线结算报告 |
| F10 | 系统设置 | 自动进食、并行上限、音效、界面缩放（80%-120%）等 |

### 2.2 非功能性需求

1. **性能**：主包 gzip 后约 29KB（视图懒加载，代码分割），物品数据层约 547KB（纯数据）；
2. **可维护性**：全部游戏内容以 JSON 风格数据文件组织（items/expansion1/expansion2 等），支持内容扩充而不改逻辑；
3. **可测试性**：核心逻辑与视图解耦，可在 Node.js 环境无头运行全量断言；
4. **跨平台**：浏览器（Vite 构建）与桌面端（Electron 打包）双端兼容，资源路径相对化。

### 2.3 用例分析

以"玩家 A 游玩一天"为例的典型用例：早晨选择采摘/垂钓目标挂机 → 上班期间离线（结算离线收益报告）→ 晚上查看产出，制作料理提升烹饪等级 → 以料理补充对决品鉴值挑战首领 → 收集图鉴 → 加入公会接取任务 → 领取赛季奖励 → 存入仓库/炼金升级 → 保存存档退出。

---

## 第三章 总体设计

### 3.1 技术选型

| 层次 | 选型 | 理由 |
| --- | --- | --- |
| 前端框架 | Vue 3.5（组合式 API） | 响应式状态驱动 UI，setup 语法组织逻辑 |
| 状态管理 | Pinia | 单一数据源，store 即存档本体 |
| 构建工具 | Vite 8 | 秒级热更新，视图级代码分割 |
| 桌面封装 | Electron 31 + electron-packager | 免服务器桌面分发 |
| 音效 | Web Audio API | 零依赖程序化合成 |
| 测试 | Node.js 原生断言脚本 | 核心逻辑无头运行，快速回归 |

### 3.2 系统架构

系统采用**数据驱动 + 分层架构**，自上而下分为四层：

```
┌─────────────────────────────────────────┐
│ 视图层：15 个懒加载视图 + 全局弹窗组件      │
│（技能/餐厅/公会/赛季/竞技场/商店/炼金/图鉴…）│
├─────────────────────────────────────────┤
│ 状态层：Pinia Store（player 存档本体）     │
│（serialize/applySave 全量序列化）          │
├─────────────────────────────────────────┤
│ 逻辑层：技能类/战斗引擎/经济系统/事件总线    │
│（GatheringSkill/ProductionSkill/Combat/  │
│  EventBus 模块级单例）                    │
├─────────────────────────────────────────┤
│ 数据层：纯数据文件（1275 物品/配方/赛季/    │
│  公会/首领/成就/故事/攻略…）              │
└─────────────────────────────────────────┘
```

**设计要点**：
1. **存档即状态**：`player.serialize()` 输出完整 JSON（含 3 存档位），`applySave` 反序列化并做缺省字段迁移，保证版本兼容；
2. **技能类多态**：20 项技能继承自 `GatheringSkill`/`ProductionSkill`/`CombatSkill` 等基类，通过 `registry` 单例统一实例化与调度；
3. **事件总线**：模块级单例 EventBus 解耦"玩法事件 → UI 日志/音效/进度联动"，通过 `fn.toString()` 去重防 HMR 重复监听；
4. **时间驱动**：挂机使用绝对时间戳（`lastOnlineAt`），离线结算以 `Math.min(elapsed, 上限)` 计算，天然支持跨会话。

### 3.3 数据组织

游戏内容全部以数据文件组织，通过辅助函数紧凑定义：

```js
// 物品定义：it(id, 名称, 类型, 类别, 档位, 价值, 附加)
potato: it('potato', '土豆', 'ingredient', 'root', 1, 6),
saltOre: it('saltOre', '盐矿', 'ingredient', 'mineral', 1, 10),

// 配方定义
{ id: 'roastPotato', name: '烤土豆', reqLevel: 1, xp: 30,
  ingredients: { potato: 2 }, output: { itemId: 'roastPotato', qty: 1 } }
```

数据规模：1275 件物品、467 件装备、70 挖掘目标、110 烹饪食谱、208 炼金配方、40 赛季 × 8 任务、20 公会、28 首领、89 成就、100 装饰、1277 张物品图片。全部内容**零重复**（任务目标物品 236 项全局唯一，经自动化校验）。

---

## 第四章 核心系统详细设计与实现

### 4.1 挂机引擎与并行调度

挂机是游戏的心脏。系统以 `player.tick(ms)` 为统一心跳，由 rAF 驱动的 `GameEngine` 以 ~100ms 粒度推进。核心设计：

1. **每技能独立目标**：`skillTargets[skillId]` 记录每项技能当前挂机目标（默认待机，玩家选择后开始），支持**多技能并行**；
2. **并行上限**：`settings.maxParallelIdle`（0=无限制/1/2/3）限制同时运行技能数，活动技能优先；
3. **节奏控制**：每个目标定义 `intervalSec`，引擎按"已累计时间 ≥ 间隔"判定是否执行动作（支持成功率/双倍/稀有掉落等随机结算）；
4. **暂停/恢复**：`pausedSkills` 记录暂停集，切换页面不中断任务。

### 4.2 离线收益结算

离线结算（`settleOffline`）是放置游戏的体验核心，实现如下：

```js
// 伪代码：离线结算
const elapsed = now - player.lastOnlineAt           // 绝对时间差
const capped = Math.min(elapsed, 12h + offlineBonusH) // 能量饼干可扩展上限
for (每个运行中的技能) {
  actions = floor(capped / intervalSec)              // 期望动作次数
  for (i < actions) { 判定成功/双倍/稀有 → 产出入账 }
}
生成离线报告（各技能产出汇总）→ 应用 → 更新 lastOnlineAt
```

要点：结算基于**期望值 + 逐次判定**，与在线行为同构；离线产出同样触发成就/赛季/公会进度，保证行为一致性。

### 4.3 技能成长与数值体系

**经验曲线**：采用经典递增曲线，1→99 级总经验 ≈ 1303 万，转生（任一级 99 级可轮回）获得永久 +10% 经验加成，突破至 120 级上限，形成长线成长目标。

**数值平衡**：装备/料理/配方价值经**价值审计脚本**统一校准：
- 炼金 208 条配方全部满足"价值微亏"（产出 ≤ 投入），杜绝刷价值漏洞；
- 452 个食材/调料经审计**100% 有制作用途**（配方反向索引验证）；
- 制作失败消耗材料但获半额经验，形成风险-收益权衡。

### 4.4 对决战斗系统

对决采用回合制简化模型：
- **三风格相生相克**：刀工克摆盘、摆盘克调味、调味克刀工（克制 +15% 伤害）；
- **资源体系**：品鉴值（HP）、火候（防御）、调味能量（技能消耗，每回合回复 +5，茶饮可恢复）；
- **对手体系**：220 名对手按区域/等级解锁 + 28 位首领（专属机制：灼烧/回血/秒杀/随机风格/三阶段），首领掉落独有装备；
- **持久战**：胜利后自动再战当前敌人，沉淀经验与品鉴点。

### 4.5 经济与经营系统

- **产出→加工→消费**三级经济链：采集食材 → 制作料理/调料 → 料理用于对决回血、餐厅经营、卡牌、任务交付；
- **餐厅**：菜单放料理按小时产出金币（离线 80%），升级扩菜位，100 种装饰每件 +1% 收入（全购 +100%）；
- **珍馐阁**：按分类出售任何非装备物品（售价 = 价值 ×2.2），作为金币消耗出口；
- **炼金**：低阶食材 2:1 链式升级为高阶，解决扩展食材无出口问题（173 条升级链）。

### 4.6 社交与活动系统

- **公会**：20 家分战斗/采集/制作/辅助四类，加入需对应类型等级 + 金币；被动 buff 逐级递增；每日任务 → 公会点 → 20 种商店兑换（含神话饰品）；
- **赛季**：40 个主题赛季 × 14 天（560 天循环），每季 8 任务 → 10 档奖励（一套 8 件限定装备逐档发放），点数经济闭环（任务点数和 = 奖励需求和 +15）；
- **竞技场**：10 名随机镜像对手（数值随对决等级生成），30 分钟刷新，每 5 连胜开宝箱、破纪录有奖。

### 4.7 收集与图鉴系统

- 图鉴 1275 件物品**两级分类**（一级 type 大类 + 二级 category 细分），未获得置灰；
- 详情弹窗含"详细作用 + **可用于制作**（反向配方索引，产物可点击跳转）+ 获取来源（含炼金路径）"；
- 89 项成就（进度条实时显示）、首领图鉴、赛季图鉴、称号、卡牌对战（战力 = 回血 + 价值×0.5 + 档位×3，三局两胜）。

### 4.8 存档系统

- 3 存档位（`localStorage`，预留 IndexedDB 迁移接口）；
- `saveNow` 自动保存 + 手动保存 + 切档保存；
- **删除当前存档位自动重置为新游戏**（防止自动保存把旧档写回，已加回归测试）；
- 导入/导出 JSON 文件，`schemaVersion` 版本迁移。

### 4.9 叙事与引导

- **故事系统**：完整世界观（太古厨神封印饕餮之影、二十八星坠），线性七章覆盖全部功能系统，每章需求全达标解锁下一章（与玩家真实进度动态联动）；
- **攻略系统**：六阶段攻略按对决等级自动定位，内容对照全部当前数据（自动化校验一致性）。

### 4.10 用户体验设计

- 三栏布局（技能导航/主区域/状态面板），顶部功能导航 15 按钮自动换行；
- **卡片式分段布局**：采集/制作/炼金页按类别/等级分段卡片（图片+属性+贴底操作按钮，可折叠）+ 分类快速导航 + 一键回顶；
- 1277 张物品图片全链路展示（相对路径，浏览器与 Electron 双端兼容）；
- 全局滚动条美化、tooltip 防裁剪（fixed 跟随鼠标）、红点提醒（签到/赛季）、界面缩放滑条（80%-120%）；
- Web Audio 程序化音效（采集/制作/升级/战斗/奖励/警告）。

---

## 第五章 系统测试

### 5.1 测试策略

核心逻辑与视图解耦，使**无头自动化测试**成为可能：所有测试脚本在 Node.js 环境直接加载游戏核心模块（Pinia store + 技能类），以断言方式验证。

| 测试套件 | 断言数 | 覆盖内容 |
| --- | --- | --- |
| system_test | 183 | 技能成长/并行挂机/对决/装备/离线结算/存档往返（含删除槽位回归）/经济闭环/图鉴/数值安全/赛季完整性/攻略一致性 |
| system_test2 | 75 | 扩展内容完整性/配方引用有效性/分类体系一致性 |
| smoke_test | 123 | 核心冒烟：经验曲线/配方数量/物品规模/商店/任务 |
| audit_sync | 19 | 跨系统数据同步（配方/来源/进度钩子） |
| verify_modules | 82 模块 | 全部视图模块编译 + 渲染风险静态扫描 |

### 5.2 关键测试用例

1. **离线结算**：构造 1 小时离线，验证产出数量与期望值一致、金币/经验入账、离线报告生成；
2. **并行挂机**：采摘 + 垂钓同时 tick 均产出；切到制作页后采摘不中断；上限 1 时仅活动技能运行；
3. **存档往返**：serialize → applySave → serialize 深比较无丢失；旧档缺字段默认填充；损坏 JSON 不崩溃；
4. **经济平衡**：炼金 208 条配方价值比审计（0 条溢出 >15%）；任务物品全局零重复；
5. **删除存档回归**：删除当前槽位后保存不写回旧档（修复"刷新后旧档复活"bug）。

### 5.3 测试结果

全部测试通过（183/183、75/75、123/123、19/19、82 模块编译无渲染风险），构建成功。游戏打包为 Electron 桌面应用（Windows x64），启动与运行稳定。

---

## 第六章 总结与展望

### 6.1 工作总结

本文设计并实现了一款内容完整、数值自洽、可测试、可分发的美食题材放置游戏《食之契约》。主要成果：
1. 以数据驱动 + 分层架构支撑 1275 件物品、20 项技能、40 赛季等大规模内容，新增内容无需改动核心逻辑；
2. 实现放置游戏核心机制（并行挂机、离线结算、存档体系），并通过自动化测试保证行为一致性；
3. 建立数值审计方法（价值微亏、全局唯一、反向索引），从工程层面保障平衡性；
4. 形成"无头测试 + 静态扫描 + 构建 + 打包"的完整质量闭环。

### 6.2 不足与展望

1. **真人在线 PVP**：当前竞技场为本地镜像对战，未来可接入 WebSocket 后端实现实时对决；
2. **移动端适配**：已做响应式布局，尚未进行真机专项走查与手势优化；
3. **内容深度**：120 级终局内容、更多首领机制（弹幕/环境效果）可继续扩充；
4. **性能优化**：物品数据层 547KB 可进一步按需分片加载；
5. **数据持久化**：可迁移至 IndexedDB 支撑更大规模存档与云存档。

---

## 参考文献

[1] Vue.js 3 官方文档. https://vuejs.org
[2] Pinia 官方文档. https://pinia.vuejs.org
[3] Vite 官方文档. https://vitejs.dev
[4] Electron 官方文档. https://www.electronjs.org
[5] Melvor Idle. https://melvoridle.com
[6] Cookie Clicker. https://orteil.dashnet.org/cookieclicker
[7] Mozilla Developer Network. Web Audio API. https://developer.mozilla.org/zh-CN/docs/Web/API/Web_Audio_API

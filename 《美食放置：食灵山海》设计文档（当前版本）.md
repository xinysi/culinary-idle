# 《美食放置：食灵山海》设计文档（当前版本）

> 版本：v2.1（覆盖当前全部已实现功能） · 技术栈：Vue 3 + Pinia + Vite
> 本文件为当前游戏实现的完整设计规范，取代早期需求文档。

---

## 1. 游戏概述

放置挂机 + 料理题材的 Melvor Idle 式游戏。玩家通过**选择技能目标挂机获取食材**，**制作料理/装备**，在**料理对决**中挑战对手与 BOSS，同时经营**餐厅/公会/赛季/竞技场**，收集**图鉴/成就/称号**。

- **核心循环**：选择目标挂机 → 获取资源 → 制作/锻造 → 对决（获取经验与品鉴点）→ 离线收益 → 转生循环
- **核心数值**：经验曲线 = RuneScape 式（`720×2^(L/7)`，99 级 ≈ 13,034,431 XP）整体 ×221.12 缩放（99→100 = 3 亿）；等级上限 100，转生后 120
- **成长节奏**（2026-09-09 标定，参照 Rocky Idle）：精通经验上限 ×4、固定间隔 2.0~3.6s、**保底产量 50 级 +1 / 100 级 +2**；实测单技能满级 采集 24~33h / 制作 7~14h / 农耕 16 天 / 探索 7.4 天 / 战斗 9.3 天（`scripts/sim/growth_sim.mjs` 真实引擎测量）
  - 间隔口径（2026-09-12 修）：精通比例因子 **≥10 级恒为 ÷2**，≥20 级起再与固定档值 **取更快者**（`min(固定档值, 基础÷2)`）。
    旧写法「≥20 直接换成固定档值」会让 367 个采集目标里 **51% 在精通 19→20 时反而变慢**（最差 2.4 倍）；
    改后逐目标逐档**不再出现变慢**（守卫 `system_test.mjs` C18），而节奏维持原标定（复测 采摘 32.6h / 垂钓 30.4h / 狩猎 23.9h / 挖掘 24.6h）。

## 2. 技能系统（27 技能：采集 7 / 制作 6 / 对决 6 / 辅助 3 / 副业 5）

| 分类 | 技能 | 机制要点 |
| --- | --- | --- |
| 采集 | 采摘 | 142 目标（核心+扩展+嫩替代），1% 双倍（随专精提升，上限 25%），50% 附带木材、10% 掉种子 |
| 采集 | 垂钓 | 成功率（55% 基础 + 等级差），失败获 20% 经验，0.5% 稀有金龙鱼（经验 ×2） |
| 采集 | 狩猎 | 每次消耗 1 陷阱（弹药），15% 野鸡蛋附产物 |
| 采集 | 挖掘 | 42 目标（**根茎/菌类**；矿物已独立为采矿），2% 化石食材、50% 铜矿 + 30% 铁矿附产物、10% 掉种子 |
| 采集 | 伐木 | 20 目标 = **20 档木材**（每 5 级一档，对齐 20 个装备品质套），供锻造与强化按档取用 |
| 采集 | 采矿 | 43 目标（41 个既有矿物 + **铜矿@Lv1 / 铁矿@Lv5** 两个新增定向入口），掉落宝石原料 |
| 副业 | 木工 | **10 配方**（每 10 级一件，吃同档木材）→ 10 件木器 → 在「餐厅装潢」做成 **10 件手工装潢**（收入 +0.6%~4.2%，合计 +24%）|
| 副业 | 陶艺 | **10 配方**（每 10 级一件，同档木材 + 同档矿物作釉料）→ 10 件陶器 → 抬**地窖单槽价值上限**（+1,500/件，16,000 → 31,000）|
| 副业 | 编织 | 10 配方（同档木材 + 茎叶类蔬菜作纤维）→ 10 件织物 → 抬**餐厅小费**（+2%/件，满 10 件 +20%）|
| 副业 | 刺绣 | 10 配方（同档木材 + 水果作果染）→ 10 件绣品 → 进**米其林第七维「招牌绣屏」**（+12 分/件，满 10 件 +120 分）|
| 副业 | 蜡烛制作 | **8 配方**（每 12 级一件，同档木材 + 肉类熬脂）→ 8 支蜡烛 → 延长**夜市狂潮窗口**（+1h/件，16:00–22:00 → 16:00–次日 06:00）。**五支都不吃食灵经验、不入山海食经**（见 §11.47 / §11.48）|
| 采集 | 农耕 | 199 作物（15 手写 + 184 种子扩充），时间戳生长、3% 枯萎（**堆肥降 1% / 肥沃堆肥降 0% + 收获 +1**）、种子/肥料商店购买 + 采摘/挖掘 10% 掉落 + 保鲜制作；**自动收种**（2026-09-09 放置化：成熟即收获并补种同种种子，设置可关） |
| 制作 | 烹饪 | 234 食谱（L1-99，7 档回血，对决回血） |
| 制作 | 烘焙 | 持续回血 buff；能量饼干：离线 +4h（上限 +12h） |
| 制作 | 腌制 | 酱料（对决 buff）、腌制品（高级原料） |
| 制作 | 调酒 | 果汁（回血）、茶饮（调味能量）、酒类（buff + 醉酒 debuff） |
| 制作 | 调料调配 | 复合调料；神秘调料（随机对决效果） |
| 制作 | 厨具锻造 | 365 配方 / 244 装备：20 品质套（铜/铁/青铜 + 钢→鎏金各用**同名矿** + 钨/锰/钒/萤高端矿）+ **21 独立矿完整 8 槽套**（紫石英/锡/石膏…蓝晶） |
| 辅助 | 食材保鲜 | 15 配方（保鲜剂/经验增益剂/产量增益剂各 5 阶级 Ⅰ~Ⅴ，lv 1~99 连续，契约盐矿/稻米）+ 肥料 2 + **入门 1（厨余堆肥 Lv1，土豆，2026-09-09 解除 Lv1-4 无配方阻塞）**：保鲜剂（刷新腐坏计时）+ 增益剂（经验/产量临时倍率）；**肥料（堆肥/肥沃堆肥）**另置于 FERTILIZER |
| 辅助 | 美食探索 | 200 目标：成功获金币/稀有食材/料理/调料，失败扣钱（不足扣 HP）；**经验走卡片系数（×60，与采集/制作同口径，2026-09-09 修复）** |
| 辅助 | 食灵召唤 | 160 食灵（32 主题 × 5 阶级，1 级起可召唤），2 出战位，契约材料召唤；每阶级覆盖一个技能域（采耕/烹制/饮藏/御对/超凡），5 阶级合起来覆盖所有技能 |
| 辅助 | 美食知识 | 32 奥义：品鉴点持续消耗换取战斗/采集加成（激活后前 10 秒宽限免费试用）；**技能经验来自对决胜利**（按敌人等级，2026-09-09 修复：此前无经验来源 → 永久 Lv1） |
| 对决 | 刀工/摆盘/调味 | 三攻击风格（三角克制 +15%），对决中同步成长 |
| 对决 | 火候掌控 | 防御向，对决中同步成长 |
| 对决 | 品鉴力 | HP 来源（10 + (L-1)×10），决定对决等级 |

**多技能并行挂机**（§3.1 扩展）：每技能独立目标（`skillTargets`），全部采集/探索并行产出；设置项「并行上限」0-3（0=无限制，活动技能优先占位）；手动暂停/继续（随存档）；**挂机框「✕ 关闭」**：停止并隐藏任务（`closedIdleTasks` 随存档），技能页重新选择目标或点「继续」恢复显示。

## 3. 对决系统

- **克制三角**：刀工 → 摆盘 → 调味 → 刀工（+15% 伤害）
- **伤害公式**（§11.2）：`基础伤害 = 攻击 × (1 + 克制15%) × (1 - 防御/(防御+100))`，命中 = `acc/(acc+eva)`，暴击 ×2
- **回合制**：自动回合（基础 2.4s，装备/奥义加速，下限 1.2s）；对战框含**回合间隔进度条**（`turnTimer/speedMs` 实时显示距下一回合）
- **对手规模**：10 区域 × 22 对手 = **220**；**28 BOSS**（面条之王 L25 … 寰宇食尊 L101），机制：回血/灼烧/中毒/降攻速/秒杀/随机风格/三阶段/高暴击/高闪避/高攻速
- **前期节奏**：扩充区域对手等级上浮 5 级（clamp 99）——前期需先刷基础对手升级，放缓解锁节奏（模拟验证：对决 10 级约 12-14 场）
- **端局曲线**（`endgame_sim.mjs` 验证）：92-96 级 BOSS 满配 99 级可挑战（胜率 50-65%）→ 120 级（满转生 + 神话装）稳定收割（63-88%）；终局 100/101 级 BOSS 需毕业装备（禁忌食神 70%、寰宇食尊 70%）
- **对手数值**：`HP=12+L×6，攻=1+L×0.7，防=round(L×1.5)，命中=10+L×2，闪避=4+L×1.5，暴击=min(0.04+L×0.002,0.3)，攻速=max(1400, 2400-L×8)`
- **掉落**：全部 248 敌人均有掉落（基础掉落 + 等级段独有装备 5% / BOSS 独有 15-25%），悬停浮框查看
- **道具**：料理（回血，3 回合冷却）、酱料（增益 10 回合）、饮品（调味能量/醉酒）、神秘调料；自动进食阈值可调
- **胜负**：胜获金币/经验/品鉴点；败恢复 HP、随机丢失一件装备（非硬核）
- **对战框**：实时 HP 条/回合/日志（引擎循环 100ms 刷新）

## 4. 装备系统

- **8 槽位**：weapon/offhand/body/helmet/amulet/ring/legs/boots
- **6 品质**：普通(铜/铁) → 精良(钢/银/青铜) → 稀有(金/秘银) → 史诗(水晶/精金) → 传说(玄铁/寒铁/陨铁/BOSS/赛季) → 神话(星辰/龙鳞/琉璃/鎏金/钨/锰/钒/萤/初代食神)
- **档位 T 与品质随制作等级**（2026-09-09 归一）：锻造装备 `tier = ceil(制作等级/10)`（1~10）、品质按上面 6 档曲线；由 `equipTierCurve.js` 唯一口径 + `itemBalance` 启动时归一（此前补齐件硬编码神话、既有件停在旧索引，最多脱钩 57 级）
- **套装效果**（2026-09-09 新增）：同套穿戴 **2/4/6 件**叠加（攻防 0.12×套件等级 / 生命 0.5× + 命中 0.1× / 暴击 0.5% + 攻速 3%）；覆盖 81 套（20 品质套 + 21 独立矿套 + 40 赛季套，`equipSets.js` 纯读取现有数据）；穿戴件中最高 `tier×10` 作为套件等级，低阶套不会压过高阶套
- **规模**：**736 件装备**（含 40 季 × 8 件套 + BOSS 独有 + 扩充金属系 + 13 同名矿套 + 21 独立矿完整 8 槽套）
- 穿戴/换装/卸下/属性合计（攻击/命中/防御/闪避/暴击/品鉴值/攻速）

## 5. 背包 / 仓库 / 出售

- **背包**：20 格起（按物品种类计），商店扩展至 100；满格拒绝新种类 + 提示
- **仓库**：100 格起，扩展至 500；背包↔仓库转移（弹窗，格子显示，数量条/输入框操作）
- **物品详细作用**：选中物品右侧显示详细作用表（类型/品质/槽位/回血/buff/装备属性/保鲜/增益/腐坏/食灵效果）+ 获取来源（背包、仓库、图鉴共用 `itemDetail.js`）
- **堆叠**：食材/料理 9999 上限；不可堆叠物品（装备等）上限 1
- **出售**：杂货铺页签，价格 = 价值 × 0.5（向下取整，保底 1）
- **腐坏**：lv≥25 荤食（海鲜/肉类/蛋）按 `12 + 等级×0.6` 小时腐坏（越高级越耐放），保鲜剂刷新（按各食材自身等级时长）；**保鲜被动**：保鲜技能等级越高腐坏越慢（每级 +2%，封顶 +100%）；**冷库**可冻存腐坏食材（存入冻结倒计时，取出后从剩余时长继续），支持一键冻存全部、保鲜剂给冷库续时，默认 5 格，每次扩充 +1 格花 1000 金币，上限 100 格

## 6. 离线进度

- 基础 **12h** 上限，**80% 效率**；能量饼干 +4h（上限 +12h，共 24h）
- 采集/探索按目标分别结算（受并行上限约束）；餐厅离线收入 80%；手动暂停的技能不结算
- 绝对时间戳计算，无 DST/跨天精度问题
- **离线只打 8 折，不再另立一套算式**（2026-09-12 修）：每次动作的**期望产出**（平均双倍率 + 精通保底产量 + 奥义/公会/图谱/节庆/天气/信仰/运势/荣誉的产量加成）与**卡片精通经验倍数**都与在线同源
  （出口是 `GatheringSkill.expectedYield()` / `xpMultOf()`；`bootstrap.settleOffline` 以 `addCardXp(r.exp, r.xpMult)` 应用倍数），
  探索的**掉落也按期望发放**（修前离线一件物品都不给，只报经验与金币估算）。修前实测离线产量只有在线的 1/2~1/3。
- **读档结算完必须推进并落盘 `lastOnlineAt`**（2026-09-12 修）：否则反复读同一份存档会反复发放同一段离线收益
  （实测第二次能拿到 3434→7070 的翻倍奖励）。守卫：`system_test.mjs` C15。

## 7. 存档系统

- 3 存档位（保存/读取/删除/覆盖），LocalStorage + JSON 导入导出
- **版本迁移**：缺字段自动默认填充；未知版本不崩溃；损坏 JSON 容错
- **硬核模式**：新建存档可选（开启前二次确认警示「死亡即删档不可恢复」），死亡即删档（独立标记 + 成就）

## 8. 经济系统

- 金币来源：对决胜利、探索、餐厅（小时收入，离线 80%）、出售、成就/任务/赛季奖励
- 金币消耗：商店（弹药/种子/容量扩展）、食谱材料间接
- 商店：购买弹药/种子/背包扩展/仓库扩展 + 出售页签

## 9. 成就 / 图鉴 / 任务 / 称号 / 故事

- **成就**：**178 项**（技能 60 / 对决 7 / 收集 13 / 探索 7 / 特殊 91），含 诸神黄昏（28 首领）、时空穿梭者（40 赛季）、硬核勇士、转生里程碑（3/5/8/12 次）、图鉴里程碑（25/50/75/100%）、硬核生存（1/7/30/100 天）；**2026-09-09 新增 12 项**：秘境 5/15/30 层、采集队 10/100 轮、评论家 10 次、镶嵌 10 颗、套装 2 套、图谱 6/12 节点、周挑战 4 次、挂机计划 3 次；**2026-09-10 新增 37 项**：配方精通 3、地窖 2、常客 3、食灵物语 2、自动化 1、牧场 2、分店 2、交易所 2、试炼 2、米其林 2、风味搭配 2、厨具大赛 2、菜系研究 3、雇工 2、产地 2、师徒传承 3、食神信仰 2；**未解锁成就显示实时进度条**（当前/需求）
- **图鉴**：**2210 件物品**（**两级分类**：一级 type 大类（全部/食材/料理/饮品/调料/种子/道具/装备/食灵）+ 二级 category 细分（食材→根茎/蔬菜/水果/肉类/海鲜/菌类/矿物/腌制品/种植产物(199 种子产物)…；料理→主菜/主食/汤品/甜点/烘焙品；饮品→果汁/茶饮/酒类；装备→槽位 含 饰品1/饰品2）；未获得置灰；**详情弹窗**：详细作用 + **可用于制作**（该物品参与的全部配方/炼金，产物可点击跳转）+ 获取来源（含**炼金合成**路径）；分类标签 `CATEGORY_LABEL`）+ **首领图鉴**（28）+ **赛季图鉴**（40，每季 8 件套）
- **物品图片**：1921 张按类型分目录（food/tool/seed/spirit/equipment）；背包/仓库/图鉴/商店/珍馐阁/公会商店/详情大图/卡牌/采集目标/农耕作物/制作成品/食灵 全链路展示（无图自动隐藏）
- **配方手册**：全部配方一览（含未解锁与需求等级）；**材料/产物点击弹窗查看获取来源**
- **故事**：完整世界观《食之契约》（太古厨神封印饕餮之影、二十八星坠），**线性八章**（序章→六章→终章），**每章覆盖全部功能系统**（采集/制作/对决/首领/装备/强化/食灵/奥义/餐厅/公会/赛季/竞技场/卡牌/探索/签到/炼金/转生/图鉴/金币；2026-09-09 起第三/四/五章各增一段，覆盖远行采集队/挂机计划/菜系图谱/宝石镶嵌/套装/食神秘境/每周挑战赛/美食评论家），**每章需求全达标 → 通过本章 → 解锁下一章**（与玩家真实进度联动）
- **卡牌对战**：从已收集的料理/装备中选 ≤3 张，对手从**同一卡池**（你已收集的料理）抽 3 张（不足时全池补足），战力 = 回血 + 价值×0.5 + 档位×3，三局两胜；胜利 +50 金币，首胜额外能量饼干
- **主线任务**：500 个（前 13 个为线性引导：采集/制作/对决/首领目标链）
- **称号**：随成就解锁（当前 82 个 = 成就称号 73 + 商店 7 + 图鉴兑换 2），侧边栏展示

## 10. 赛季（40 季轮换）

- 40 个主题赛季（盛夏/暖冬/春鲜/…/龙腾四海），每季 14 天，**560 天完整循环**
- 每季 **10 个任务**（采集×4/制作×2/探索/首领/对决/餐厅）+ 赛季点 → **10 档奖励**（逐档稀有：金币/能量饼干/神秘调料/增益剂 + **一套 8 件限定装备**分档发放，末档含原限定彩蛋件）
- **点数经济闭环**：每季任务点数总和 **1120** = 10 档需求总和 **1100** + 20（做完任务可领满全部奖励且略有富余）
- 采集/制作/探索/对决/餐厅任务需求 **100~8000**（首领 2 次）；任务目标覆盖 46 个不同 param（同一 param 跨季复用）；进度按赛季 id 持久化
- **限定套装命名**：每季 8 件前缀各不相同（季核心字 + 意境词：焚天/燎原/裂空/涅槃/熔岩/星火/映日/流霞 + 修饰词 + 槽位词）

## 11. 公会 / 餐厅 / 竞技场

- **餐厅**：菜单放料理 → 小时金币（离线 80%）→ 升级扩菜位；**装饰 300 种**（金币购置，收入 +0.5%~3%/件，随价格递增）
- **公会**：**20 家**（初始 3 家无要求 + 战斗×5/采集×4/制作×4/辅助×4），后 17 家需**类型等级 + 金币**加入（战斗=对决等级、采集/制作/辅助=分类总等级），**被动 buff 逐级递增**（伤害/产量/制作成功率/经验 +5%→+10%）；公会列表**按钮弹窗**查看详情加入；每日任务 → 公会点 → **公会商店 24 种**（含神话饰品）
- **竞技场**：挑战榜 **10 名随机镜像**（名字/数值随机，强度随对决等级提升，**每 5 分钟刷新**，带倒计时），连胜/最佳纪录；对战框实时显示 + **回合间隔进度条**；**每 5 连胜宝箱、破纪录奖励**
- **每日签到**：连续 7 天循环奖励（金币/神秘调料/能量饼干/增益剂），漏签重置
- **炼金**：**1499 条配方**（核心合成 + 食材升级链：海鲜/肉类/水果/蔬菜/根茎/矿物/菌类/药材 全部扩展食材 2:1 链式升级到已有高级食材；补全矿物链（低阶矿→高阶矿递进）与加工产物（用对应原料/基质）；**所有种子可炼金**（种植产物 ← 对应成熟食材×1，价值≈0.6 倍比率≈1.67）；新增**高端矿物多催化精炼配方**（主料=低一档矿，附加=盐矿+铜矿/铁矿 2 种，比率≈1.12~1.28）；全部按**价值微亏**审计平衡；**452 个食材/调料 100% 有制作用途**）；炼金页**卡片式**展示（材料图→产物图，按产物类别分段可折叠，附加材料用制作页式文字展示）
- **装备强化**：每级 +10% 属性（铁矿/盐矿 + 金币，上限 +5），右侧装备栏操作
- **统计面板**：对决胜率/首领/制作/采集/图鉴/转生/经济等里程碑（2026-09 新增：试炼塔最高层/硬核生存天数/每日任务连续/大赛月分/食灵羁绊/餐厅好感）
- **配方手册**：日志子页，全部配方一览（含未解锁与需求等级）；**材料/产物点击弹窗查看获取来源**
- **全局搜索**：物品/对手/首领名称查找，物品可看详情
- **快捷状态**（右侧面板）：当前任务/赛季（可领档数）/公会 一键跳转 + 2026-09 新增「每日任务」行与「毕业挑战」（试炼塔/大赛）行

## 11.5 日活与长线内容（2026-09-06 新增）

- **每日/周常任务**：每日 3 个小目标（12 模板池按日期确定性轮换，金币按对决等级放大），全清再发礼包（能量饼干+金币）并累计「连续天数🔥」；每周 1 个大目标（8 模板，1000+ 产量级，奖励倍档）；进度与主线任务/赛季/公会共用同一条事件链（skill:action / combat:end）。
- **随机奇遇**：在线采集/制作动作 0.2% 触发（10 秒防抖、一次只弹一个），8 个事件 × 3 选 1 分支（金币/材料/小代价），纯新增层。
- **无尽挑战塔**：对决等级 99 解锁；对手由 `opp()` **动态生成**（每 4 层 +1 级、hp/atk ×(1+0.012×层) 逐层上浮、塔深无限——不触碰 COMBAT_REGIONS/BOSS 铁律数据）；**def 只按 √g 上浮、eva 层数项 ×0.05**（2026-09-12 修：def 跟同一个 g 会在饱和减伤 `def/(def+100)` 下变成死墙——实测满配玩家 F90 只剩 6/20、F100 起 0/20，改后 F90 20/20、F100 17/20、F110 起 0/20），量测见 `scripts/sim/endgame_sim.mjs` 末尾（每层须满血开局，否则量到的是新档遗留的 10 点 HP 假墙）；每 10 层里程碑一次性发奖（金币/能量饼干/神秘调料），第 100 层「厨神之路」、130+ 层「轮回饕餮殿」；页面与对决页同款（风格/属性组合框 + 完整对决框 + 道具），胜利自动爬楼。
- **月度厨艺大赛**：15 个主题按月份（YYYYMM%15）轮换、与 14 天赛季错峰；每日 3 次提交（消耗 1 件符合主题料理，得分 = value + tier×5 + heal×0.1）；月分 1000/3000/6000 档里程碑一次性发奖。
- **转生里程碑**：转生 3/5/8/12 次 → 成就「轮回之门/轮回行者/轮回大师/永恒轮回」（含称号）。
- **食灵羁绊**：出战食灵按**真实在线时长**累计羁绊经验（2/6/12/20/30 天 → 1-5 级），每级该食灵全部效果 ×(1+4%×级)（玩家侧乘区，SPIRITS 契约/效果数据不动）；离线不计、切换出战按 id 续计。
- **餐厅顾客好感**：餐厅收入 0.02 点/金 累积好感，500×L² 需求（20 级封顶），每级小费 +3%。
- **图鉴完成度里程碑**：25/50/75/100% → 成就「图鉴入门/图鉴行家/图鉴宗师/美食全知者」。
- **硬核生存挑战**：硬核档按日历日累计生存天数（lastDayKey 防重启重计），1/7/30/100 天 → 成就「硬核开局/硬核一周/硬核一月/硬核传说」（死亡即删档，成就与档存亡一致）。
- **远行采集队**（2026-09-09 新增，参照 Rocky Idle 的 Runs）：**5 条线路**（🚢 远洋渔队 / ⛰️ 深山采药队 / ⛏️ 矿脉勘探队 / 🍞 面点采买队 / 🫙 腌酿商队）× **4 个槽位**，周期 **1/2/4 小时**；到期**领取**产出（领取后自动开始下一轮；离线照常计时、**不叠加**）。槽位按对应采集技能等级解锁（线路 Lv25/30/25，槽位 1/30/50/70）；每条线有独立**熟练度**档位（完成 1/2/4/8/16/32/64/128 次升档，每档产量 +10%、稀有率 +0.5%）；产出为**既有物品**（渔获/药材菌草/矿石，含 0.5~2% 稀有掉落如金龙鱼/灵果/黄金矿）；**不给技能经验**（不改变已标定的满级节奏）。平衡口径：每槽每轮 ≈ 周期×5 个，满配 ≈ 主动采集一条线的 15%。
  - 页面「📋 熟练度阶梯 + 三条线档案」折叠卡：**8 档熟练度**（累计完成/本级还需/产量倍率/稀有率加成）+ **12 个槽位档案**（解锁技能等级/周期/每轮产出件数/每轮金币/可产出物品）。
- **厨房笔记**（2026-09-10）：把制作类技能每张配方独立的精通（0~100 级）显式化——1248 张配方一览，展示等级/进度/档位效果（经验倍率/双倍率/保底产量），支持按精通/进度/等级排序与「只看未满级」。
- **统计页（含原名人堂）**（2026-09-10）：招牌纪录 6 张大字卡（对决等级/图鉴/塔层/秘境/连胜/月分）+ 分类统计 + 称号墙 + 一键生成战报名片；原「名人堂」独立页已并入统计（导航位置为顶栏右侧 📊）。
- **地窖陈酿**（2026-09-10）：调酒 Lv10 解锁的时间型放置线——酒类/腌制品入窖 12/24/48/96h，出窖按价值 ×1.5/×2/×3/×4 换金币（单槽 ≤99 件、价值 ≤16000），撤回无损；栏位 3→9（金币扩建）。
- **常客名录**（2026-09-10）：十二位常客（餐厅 Lv1~36 解锁）各有偏好料理，每日招待 1 次 → 金币 + 好感；好感 0~5 级（3/7/12/18/25 次），每级餐厅小费 +2%，满级领神秘调料。
- **食灵物语**（2026-09-10）：出战食灵羁绊 Lv2/3/4/5 解锁心声片段（按采耕/烹制/饮藏/御对/超凡五域模板生成），每段一次性奖励（金币/神秘调料/能量饼干）。
- **自动化中心**（2026-09-10）：金币解锁三项自动化（自动出售 5000 / 自动续队 8000 / 自动领取 12000），每 5 秒执行一次；自动出售按价值阈值清理采集食材（每种留 1，矿物/化石/材料/补给除外）。
- **牧场养殖**（2026-09-10）：农耕 Lv15 解锁——4 种动物按 4~12h 周期消耗作物饲料产出蛋/奶/肉（全部为既有食材），离线补算上限 12h；栏位 2→4。
- **餐厅分店**（2026-09-10）：餐厅 Lv5 解锁——六家分店每小时自动入账（时收 ×(1+10%/餐厅等级)），店长 +25%；离线补算上限 12h。
- **交易所**（2026-09-10）：调料调配 Lv10 解锁——每 4h 确定性轮换 6 种食材，收购价 0.6~1.6× 基准价值浮动、买卖价差 35%，每日每件限 60 件。
- **厨神试炼**（2026-09-10）：对决 Lv30 解锁的四条限制挑战（速攻 ≤12 回合 / 无伤 ≥90% 血量 / 越级 +5 级 / 连胜 3 场），对手由 `opp()` 动态生成；首通大奖、重复通关 30% 金币；判定数据由 `combat:end` 附带（回合数/剩余血量）。
- **米其林评级**（2026-09-10）：六维加权评分（菜单成色 12/装潢 0.6/出餐口碑 1.2/评论家 25/常客好感 8/连锁 60）→ 1~3 星（门槛 120/300/620），每日评审、可升可掉；星级收益 餐厅收入 +10/20/35%、全技能经验 +0/3/6%。
- **雇工班底**（2026-09-10）：掌勺/跑堂/采买/掌柜/账房五个岗位各 5 级（一次性 8000×级² 金币，时薪 220/130/90/200/70×级；后两个岗位 2026-09-12 并入既有「餐厅收入%」通道），在岗加成餐厅收入与订单奖励；每小时结算工资，欠薪停工、补足复岗。
- **风味搭配册**（2026-09-10）：28 条食材组合，制作成功时按配方材料表匹配并点亮（顺序无关），每条一次性奖励。
- **菜系研究**（2026-09-10）：六大流派（按料理大类划分）各 5 级，材料 10×级 + 金币 2000×级² + 2+级 小时；每级 制作经验 +5% / 进食回血 +6% / 餐厅贡献 +8%（该类料理）。
- **产地与风土**（2026-09-10）：七个产地（考察费 2~12 万），可把采集队线路派驻；派驻后产量 +12~22%、稀有率 +5~10%，产出混入产地特产池；当季（按月份）×1.5。
- **节庆日历**（2026-09-10）：8 个按日期触发的全服节庆，效果并入 `marketBoost`（餐厅/采集/制作/对决经验）与采集产量乘区。
- **厨具大赛**（2026-09-10）：每周一届（按 epoch 周），评分 = 价值×1 + 属性×6 + 暴击×400 + 攻速×200 + 词条×220 + 宝石×160 + 强化×320；D/C/B/A/S 五档发奖。
- **师徒传承**（2026-09-10）：转生时保留 `floor(等级×5%)`（上限 20）作为轮回起步等级；徒弟按自然日 +1 级（上限 50），每级离线效率 +0.4%（80% → 100%）。
- **食神信仰**（2026-09-10）：八位守护神各 3 级（供品 5×级 + 金币 6000×级²），同时只信一位；切换 12000 金币 + 24h 冷却；效果挂在料理回血/技能经验/采集产量/餐厅收入/地窖出窖五个聚合点。
- 以上全部为**新增层**：旧存档 applySave 自动补默认字段，无数据铁律文件被修改。

## 11.6 玩法扩充（2026-09-09 新增 8 项）

- **自动补给**：设置开关（默认开）+ 保留金币下限；狩猎陷阱/摆盘装饰食材低于 50 时自动从杂货铺补到 200（每 5 秒检查一次）。
- **套装效果**：同套穿戴 **2/4/6 件**叠加（攻防 0.12×套件等级 / 生命 0.5× + 命中 0.1× / 暴击 0.5% + 攻速 3%）；覆盖 81 套（20 品质套 + 21 独立矿套 + 40 赛季套），`equipSets.js` 纯读取现有数据。
- **宝石镶嵌**：装备按品质提供 0~3 个插槽（普通 0/精良 1/稀有 1/史诗 2/传说 3/神话 3），用**现有矿物**镶嵌（10 种宝石各配属性）；镶嵌消耗 1 个宝石、拆卸/换装**自动返还**；加成并入 `equippedStats`。
- **美食评论家**：随机 2~4 小时到访（6 种头衔），要求「tier ≥ N 的某类料理」（按对决等级缩放，60 分钟有效）；提交得金币 300+50×等级 + 神秘调料 + 好感 30；超时仅错过奖励。
- **挂机计划**：SkillView 可编辑的顺序计划（技能 + 目标 + 条件：精通满 N 级 / 技能到 LvN），条件满足自动切下一步（换目标 + 取消暂停 + 切页），全部完成自动暂停；随存档持久化。
- **每周挑战赛**：每周按周序号确定性轮换 1 个**难度型**目标（塔层/首领/对决胜场/探索/竞技场连胜/制作量），进度按周累计，达成一次性领奖（1200~1800 金 + 神秘调料/能量饼干）；历史最佳按挑战 id 记录。
- **食神秘境**：Roguelike 局内模式（对决页内）——逐层挑战 `opp()` 动态生成的对手（等级随层上浮、封顶 99，血量 +6%/层），每胜一层 **3 选 1 临时增益**（9 种，仅本局生效），阵亡/放弃按层结算（金币 150+90×层 + 5 层起调料 + 10 层起饼干）。
- **菜系图谱**：永久天赋树（美食知识页）——用「美食见闻」（图鉴首收集 +1 / 成就 +5 / 赛季领档 +3 / 首杀首领 +2）解锁 12 个节点（采集/制作/对决三线），永久加成经验/产量/制作成功率/对决攻防。
- 以上全部为**新增层**：旧存档 applySave 自动补默认字段，无数据铁律文件被修改。

## 11.7 玩法扩充（2026-09-10 第二批 10 项）

- **里程碑之路**（左侧栏 · 记录与回顾）：`milestones.js` 定义 **21 项长线目标**（分 成长/收集/对战/经营/长线 五组），每项给 目标值/单位/提示/取值函数；页面按组显示进度条与百分比，支持「只看未完成」筛选，点击跳转对应页面。
- **厨师年鉴**（左侧栏 · 记录与回顾）：`chronicle.js` 定义 **12 类事件**（首领/转生/餐厅/秘境/挑战塔/竞技场/试炼/厨具赛/研究/分店/食灵/赛季）；`player.recordChronicle(key, kind, text)` 按 key 去重（首次才记），上限 **300 条**；页面按天分组 + 类别筛选 + 一键生成战报名片。
- **天气与运势**（左侧栏 · 今日）：`weather.js` **纯日期哈希**（`hash32(dayKey)`，无存档状态、跨端一致）——6 种天气各给一条全局加成（`boost` 键与 `marketBoost()` 对齐 + `gatherYield` 产量），运势再指定今日**幸运食材**（采集它 +20% 产量）与 3 条「宜做」建议；页面含 7 日预报与全天候对照表。天气作为**第四层乘区**叠在节庆 / 整点活动之上。
- **厨神之路 · 轮回天赋树**（v2.0 新增，左侧栏 · 成长与信仰）：转生专属长线追求。货币「**轮回印记**」由**转生次数派生**
  （可用 = `stats.prestiges` − 已投入，**不新增货币存档字段**）；四路（采撷 🌾 / 火工 🍳 / 厨武 ⚔️ / 经营 🏮）× 三层 × 各 3 节点 = **36 节点**，
  层数门槛为「本路已解锁数 ≥ 2 / 5」（沿路推进，不能直接跳点满）；单路 18 印记、四路共 72（≈72 次转生，毕业级目标）。
  效果全部是**功能层乘区**，挂在既有聚合点上：采集产量（`yieldExtraChance`）、按类别的采集/制作经验与全技能经验（`Skill.addXp`）、
  对决伤害与暴击、最大品鉴值（`Combat.playerStats`/`playerAttack`）、餐厅收入与订单赏金、分店收入、金币获得、
  **离线时长上限**（`settleOffline`）与**种子掉落概率**（采摘/挖掘，在线与离线同口径）。
  存档：唯一新增字段 `daoUnlocked: string[]`（懒建、旧档零迁移）；解锁只增不减，`stats.daoUnlockedTotal` 计数供成就/统计用。
  **页面两个视图（v2.1 加）**：「🗺 图谱」（SVG 可拖动缩放的节点图，见 §11.26）与「📋 列表」（原三列卡片布局，保留）。
- **吉祥物**（左侧栏 · 今日）：`mascots.js` 7 位（招财猫 / 面团小人 / 锦鲤 / 灶王玩偶 / 铜锅精灵 / 灶下龟 / 柴火狐），各有金币基数与专属礼物池；买下后每天蹭一次（`lastPetDay` 防重），累计次数按 **1/5/15/30/60** 五档升好感，每级奖励 +25%。
- **宴会承办**（左侧栏 · 餐厅经营）：`banquets.js` **5 档**宴席（家宴 10 桌 12h / 寿宴 20 桌 18h / 官宴 40 桌 24h / 国宴 60 桌 36h / 皇宴 80 桌 48h，对决 Lv95 起），需按「**桌数 ×2**」交付**指定类别 + tier 门槛**的料理（消耗库存）；按时交付给金币 + 神秘调料 + 餐厅好感，超时作废、放弃无惩罚。页面「📋 席面规格 + 可承办类别」折叠卡给出各档的桌数/份数/限时/最低 tier/奖励/**折合每份**与 5 个可承办类别（每次委托随机指定一类）。
  - ⚠️ `BANQUET_TIERS[].reqCats`（1/2/2/3）**当前没有任何代码消费**，属未接线字段——页面刻意不展示它，避免写成「要求 N 类」却与实现不符。
- **外卖业务**（左侧栏 · 餐厅经营）：`takeout.js` 每小时按等级**并发外送**（Lv1~5 → 1~5 单），消耗菜单里的料理换金币；单价 `×1.2` 且随等级 +10%/级；每累计 50 单自然升级；与堂食/宴会共用同一份库存（形成「多备货多卖」的取舍）。
- **分店主题**（并入分店页）：`branchThemes.js` 6 个主题各绑定一个**菜系研究学派**；定/换主题花一次性金币，时收倍率 = `1 + 6% × 对应学派等级`；`branchHourlyOf` 与离线结算（`_tickBranches`）都走该倍率，读的是既有学派状态。
- **供应商合约**（左侧栏 · 餐厅经营）：`suppliers.js` 7 家商行（粮油行 / 菜商 / 肉铺 / 渔行 / 香料铺 / 酒坊 / 山珍行），定金签 **7 天**长约；每日自动到货、货款按 **78% 物价**（`value × 0.78 × 数量`）；**金币不足当日不到货、次日再试（不累积）**；同时最多 **3 份**，到期自动失效、解约定金不退。
- **名厨挑战**（左侧栏 · 挑战与休闲）：`chefChallenges.js` 10 位名厨各有**固定流派**与一句宣言，按 **epoch 周**确定性轮换；对手由 `opp()` 动态生成（等级 = 玩家对决等级 + 3~9，封顶 99），**不触碰 `COMBAT_REGIONS`/`COMBAT_BOSSES` 铁律数据**；战胜给金币 + 神秘调料 + 能量饼干，每周一次；失败可重复挑战。结算挂在 `combat:end` 并**校验对手名**——中途逃跑后再赢别的对手不会误领奖励。
- **赛季回顾**（左侧栏 · 记录与回顾）：**只读聚合页**——本赛季进程/任务明细/档位领取/点数收支（获得·兑换·余额）+ 往届 40 季战绩（任务进度、领档数、限定装备）+ 赛季类年鉴条目；不改动任何赛季数据（档位点数、任务结构、限定装备仍为一等固定数据）。赛季任务全清 / 档位领满时追加一条 `seasonal` 年鉴条目。
- 以上全部为**新增层**：旧存档 `applySave` 自动补默认字段（`branchThemes` / `contracts` / `chefChallenge` / `chronicle` / `mascots` / `banquet` / `takeout`），无数据铁律文件被修改；图鉴来源索引补入「供应商合约·各家（每日到货）」与「名厨挑战」。

## 11.8 玩法扩充（2026-09-10 第三批 4 项：激活存量 + 竞争层）

- **荣誉殿堂**（左侧栏 · 记录与回顾，`data/honor.js` + `HonorView.vue`）：把 **93 个称号**（成就 84 + 商店 7 + 图鉴 2）做成有被动——`perkOf(name)` 按**关键词确定性映射**到四通道（`xpPct` 全技能经验 / `gatherPct` 采集产量 / `craftPct` 制作成功率 / `goldPct` 经营收入）之一，值固定 **2%**；`honorBonuses({equipped, ownedTitles})` 汇总：佩戴称号 +2%（仅当已拥有），荣誉等级（`floor(owned/8)`，封顶 9）每级四通道各 +1%。四条通道分别接在既有聚合点：`Skill.addXp`、`GatheringSkill.yieldQuantity`、`ProductionSkill.successChance`、`restaurantHourlyIncome`——不碰对决数值曲线。
- **图鉴兑换所**（左侧栏 · 记录与回顾，`data/codexShop.js` + `CodexExchangeView.vue`）：货币「**图鉴点数**」按完成度档位一次性发放（`CODEX_TIERS` 5%~100% 共 15 档，满档 **310 点**），与菜系图谱的「美食见闻（每件 +1）」区分开；货架 7 件 = 限量道具（神秘调料/能量饼干/品鉴点）+ **限定称号**（进 `titles.js` 的第三来源 `CODEX_SHOP_TITLES`，同时计入荣誉殿堂）+ **专属头像框**（`avatar-frame-codex` / `codexOmni`）。`codexRedeem(id)` 扣点发放，`player.codexOwned` 持久化。
- **套餐与定食**（左侧栏 · 餐厅经营，`data/setMeals.js` + `SetMealView.vue`）：菜单凑齐不同**大类**即自动成套餐——`SET_MEALS` 9 套（家常套餐 主食+主菜 +8% … 豪华全席 四类齐 **+22%**）；`activeSetMeal(menu)` 返回达标套餐中**加成最高**的一套，`menuCategoryCount` 按不同料理数计（同类重复**不叠加**）。加成接 `restaurantHourlyIncome`，外卖单价按 `SET_MEAL_TAKEOUT_RATIO = 0.5` 折算接进 `_tickTakeout` 与 `takeoutPriceOf`。**无新增存档状态**（纯由菜单推导）。
- **同业竞争榜**（左侧栏 · 餐厅经营，`data/rivals.js` + `RivalsView.vue`）：月度榜单（epoch 月，30 天/月），5 家 NPC（12 家池按 `(month*3 + i*2) % 12` 轮换）vs 玩家；对手分 = `power × (1 + 6% × 月份) × (0.88~1.12 稳定哈希抖动)`；玩家分 `playerScoreFrom` = 米其林分 + 菜单档次×60 + 装饰×20 + 分店×120 + 常客×12 + 订单×2 + 累计收入/5000。`rivalClaim()` 按名次每月领一次奖，`rivalPromote()` 花 2 万金币本月分数 +30%。**只给正向激励，不做收入惩罚**——挂机游戏不该有负反馈惩罚。
- 以上全部为**新增读取层**；旧档 `applySave` 补默认字段（`codexOwned` / `rivals` / `avatarFrame`）。**顺带修复既有缺陷**：`avatarFrame` 此前从未进存档（商店头像框重载即丢），现已补全默认值/读档/存档。

## 11.9 功能页内容补全（2026-09-10 补）

对 36 个功能页做内容体检，补齐「数据已存在但未渲染」与「页面近表单」两类问题；**纯展示层 + 既有数据可见化，未改动任何固定数据**。

- **新增展示字段**（additive，旧档自动补默认值）：`trials[id].bestTurns`/`bestHp`（精确最佳成绩：回合制记最少回合、其余记最高剩余品鉴值；原有 `best` 是 0~100 综合分，含义随条件类型而变，保留不动）、`stats.mascotGold`（吉祥物累计蹭到金币）、`gearScore().factors`（按 7 项评分因子汇总的明细）。
- **新模块 `flavorRecipes.js`**：风味搭配反查——从既有配方表（技能实例 + 炼金）反查「能一次做齐该食材组合」的配方，按 `reqLevel` 升序并缓存；供风味册页显示「建议配方」与展开全部。只读，不改任何配方。
- **共用组件 `RelatedPages.vue`**：把「相关页面」跳转条统一为组件，36 个左栏功能页全部接入（按玩法关联配置，如 产地 ↔ 采集队、套餐定食 ↔ 餐厅 ↔ 外卖）。顶栏 7 个高频入口不在范围。
- **逐页补全要点**：试炼最佳成绩与重复收益；厨具赛权重补全（暴击 ×400 / 攻速 ×200 此前在说明中缺失）+ 因子明细；传承总览 4 卡 + 徒弟称号阶梯 + 传承进度；年鉴总览 4 卡 + 12 类别可跳转 + key 内嵌进度值；风味册奖励预览（未点亮也显示）+ 在哪做；吉祥物礼物概率与 Lv1~5 收益阶梯；外卖 Lv1~5 阶梯表 + 收入预估并改用 `takeoutPriceOf` 与结算同口径；产地总览 + 当季日历 + 特产池明细；雇工班底收支核算（收入增量 / 时薪 / 净收益）。
- **守卫**：`content_sync_audit.mjs` 新增「左栏功能页均含 `<RelatedPages>` 跳转条」检查（35 页）；`system_test.mjs` 新增 13 项（试炼记录 / 厨具赛因子合计 / 吉祥物累计 / 风味反查正确性与稳定性）。

## 11.10 新增三个功能页（2026-09-11 补）

把三处「数据早已写好、却只能挤在别人的标签页或弹窗里」的内容**独立成页**（36→39 个功能页）；三者均为**纯读取层**，只读既有数据与既有 store 方法，**未改动任何固定数据**。

- **⚔️ 装备总览**（左侧栏 · 研究与收集，`GearView.vue`）：此前 `gems.js`（宝石）/`gearMods.js`（词条）/`equipSets.js`（套装）三套数据只出现在 `EquipmentModal` 弹窗里，且弹窗以「操作」为主、看不到收集进度。新页分三个子页签——**套装**（81 套按套件等级排序，显示「已拥有/总数」进度条、已穿戴件数、2/4/6 件效果并按该套等级换算数值、展开可见全部部件与拥有状态）、**宝石**（10 种矿物的加成/持有数 + 按品质的插槽数表 + 已镶嵌一览）、**词条**（按品质的条数区间、8 类词条池权重与占比、洗练费用表、当前穿戴生效中的词条）。穿戴/强化/镶嵌/洗练仍在装备弹窗，新页只负责「看与查」。另给「去采矿 / 去厨具锻造」直连按钮——`RelatedPages` 只能切 view、切不了具体技能，故这两颗按钮直接调 `setActiveSkill`。
- **📋 任务中心**（左侧栏 · 今日，`QuestsView.vue`）：原先**四套任务全挤在「图鉴 → 任务」同一个标签**里（主线 500 条 + 每日 3 条 + 周常 + 每周挑战）。新页拆成四个子页签：**主线**（当前任务的目标进度条 + 全部任务列表，可「只看未完成」、默认只渲染 30 条并按需「显示更多」以免一次渲染 500 张卡）、**每日**（人任务进度/领取 + 连续天数 + 全清礼包状态）、**周常**（产量型周目标）、**每周挑战**（难度型周目标 + 历史最佳）。领取直接调用既有 `claimDailyTask` / `claimWeekly` / `claimChallenge`，无新增状态。
- **🏅 成就与称号**（左侧栏 · 记录与回顾，`AchievementsView.vue`）：原先 178 成就与 82 称号只是「图鉴」页的两个标签。新页**成就**按 5 大类筛选（带各类计数）、可「只看未完成」、可搜索，每张卡显示进度条与奖励；**称号**按三个来源分组（成就解锁 / 游戏商店 / 图鉴兑换所），已拥有即可点击佩戴（写入既有的 `player.title`，与荣誉殿堂的称号被动联动）。
- **顺带抽出的共享模块 `data/achievementProgress.js`**：成就进度换算（`achievementProgress(player, a)` / `achievementNeed(a)`）原先内联在 `LogView.vue` 里，现已抽出，图鉴页与新页**共用一份**，避免两处各写一遍导致口径漂移。
- **`setView` 增加 view key 白名单**（`stores/ui.js` 的 `VIEW_KEYS`）：`App.vue` 的分派链末尾是 `v-else` 兜底到 `SkillView`，因此拼错的 key 不报错、只静默显示技能页。现在**开发期**对未知 key 打告警并回退到技能页，**线上行为不变**（保持原样透传），新增页面漏登记时会立刻暴露而不是「点了没反应」。同时修掉 `ui.js` 中早已过期的 `activeView` 取值注释。
- **守卫同步**：`e2e-dark.spec.mjs` 的 `VIEWS` 加入 `gear`/`quests`/`achievements`（深色体检 47→50 页，扫描量约 2.2 万→3.0 万元素）；`content_sync_audit.mjs` 的「左栏功能页均含 `<RelatedPages>`」检查自动覆盖到 38 页。**新增功能页时必须同时登记四处**：`ui.js` 的 `VIEW_KEYS`、`App.vue` 的 import 与 `v-else-if` 分派、`Sidebar.vue` 的 `FEATURE_GROUPS`、`e2e-dark.spec.mjs` 的 `VIEWS`。

## 11.11 再补两页并收敛三处重复入口（2026-09-11）

延续 §11.10 的思路，把最后两处「数据在、页面不在」抽成独立页，并把上一批留下的三处重复入口收敛掉（功能页 39→41）。

- **🏯 食神秘境**（左侧栏 · 挑战与休闲，`data/mysticRealm.js` + `MysticRealmView.vue`）：原先整个 Roguelike 模式塞在**对决页顶部一张卡**里。新页把界面搬出（本局状态条 / 3 选 1 祝福弹窗 / 对战框 / **祝福池图鉴** 9 项 / **结算奖励阶梯** 7 档 / 规则速览），并新增两项此前看不到的信息：**下一层对手的等级与血量上浮预览**、**按通过层数的金币与额外道具阶梯**（`realmReward`：150+90×层，第 5 层起神秘调料、第 10 层起能量饼干）。
  - **对决页保留 `combat:end` 的秘境分支**（`player.realmState().active` → 胜则 `realmAdvance`、败则 `realmEnd`），只把界面换成一张「状态 + 进入秘境 ↗」的卡。原因：战斗跑在单例 `Combat` 上，本局开打后玩家可能切到对决页甚至别的页；若两处监听都不在，本局会卡住不推进。两个视图由 `v-if/v-else-if` 互斥挂载，因此同一时刻只有一个监听在跑，不会重复推进。
- **🏮 餐厅装潢**（左侧栏 · 餐厅经营，`data/restaurantDecor.js`（300 件，生成器产出）+ `DecorView.vue`）：原先只是餐厅页底部一个长列表。新页补上**分类进度**（8 类各自的已购/总数/合计加成）、**性价比排序**（`%/万金`）、**只看未购置**、以及**「下一件推荐」**（未购置里性价比最高且当前买得起的一件）。餐厅页保留一行状态（已购 x/300、总加成）与「去装潢 ↗」。购买逻辑与原实现一致（`spendGold` + 追加 `restaurant.decor`），**未改动任何装饰数值**。
- **图鉴页三标签收敛为跳转按钮**（`LogView.vue`）：上一批新增任务中心/成就与称号后，图鉴页里仍有「任务 / 成就 / 称号」三个**功能重叠的旧标签**。本次把它们换成两个跳转按钮（`📋 任务中心 ↗`、`🏅 成就与称号 ↗`），并**删除对应的模板块与脚本死代码**（含每日/周常面板、任务卡/成就卡视图、称号佩戴函数、`PAGE.quest`/`PAGE.achieve`、`qPage`/`aPage` 等），文件 **64.3kB → 54.6kB**；默认页签由 `quest` 改为 `log`（图鉴），`logInitialTab` 对已迁移的三个旧值做重定向，防止老入口跳到空白页。`StatusPanel` 的两处「去领取 / 查看」由 `openLogTab('quest')` 改为 `ui.setView('quests')`。
- **新增常驻守卫**（`e2e-test.spec.mjs`）：逐 key 切换 `VIEW_KEYS` 里的**全部 view key**，捕获 `ui.setView` 的「未知的 view key」告警并断言无告警、且每页切换后 `main-scroll` 有内容。这条守卫直接针对本文档反复强调的坑：`App.vue` 末尾 `v-else` 兜底到 `SkillView`，漏注册的 key **不报错、只静默显示技能页**。深色体检 `VIEWS` 同步加到 52 页。

## 11.12 响应式断点失效修复（2026-09-11）

§9.3 的单栏/两栏布局写了媒体查询，但**从未真正生效**——三处 `display` 规则被「声明在媒体查询之后、同特指度」的基础规则反向覆盖，属 CSS 层叠顺序错误。

- **根因**：`.sidebar{display:flex}`、`.status-panel{display:flex}`、`.mobile-nav{display:none}` 三条基础规则都写在 `@media` 块**之后**；与媒体查询里的裸选择器同为 (0,1,0) 特指度时，**后声明者胜出**，于是 `@media` 里的 `display:none` / `display:flex` 全部被吃掉。
- **实测后果（改前，Playwright 逐宽度取 computed display）**：
  | 宽度 | 改前 | 改后 |
  | --- | --- | --- |
  | 1440（桌面） | 三栏正常 | 三栏正常（无回归） |
  | 900（平板 <940） | 右栏**照旧显示** 200×457，中区被压成半高 690×433 | 右栏 `none`，中区 690×900 占满高 |
  | 390（手机 <720） | 右栏**占满屏** 390×396；左栏 `display` 仍是 `flex`（只靠 grid 高度 0 才没露出来）；底部导航**任何宽度都不显示** | 左/右栏 `none`，底部导航 `flex` 390×56，中区 390×788 |
- **修法**：媒体查询里的三个选择器加 `.app-layout` 前缀提权为 (0,2,0)：`.app-layout .app-status` / `.app-layout .app-sidebar` / `.app-layout .mobile-nav`。选 `.app-layout` 而非任意 `:not()` 提权，是因为它是真实祖先（三者都在 `.app-layout` 内），且**抽屉里的 `<Sidebar />` 只带 `.sidebar` 类**，前缀不会误伤抽屉（实测抽屉内 Sidebar 仍为 `flex / 239px`）。
- **守卫**（`e2e-test.spec.mjs`）：在 900/390 两个宽度逐条断言 computed display，并断言平板下中区高度 >800（防再次被压半高），最后**实点底部导航**验证抽屉打开、选技能后收起。
- **已知遗留（非 bug，是设计选择）**：手机上右侧状态栏（挂机 / 事件日志 / 快捷状态）没有任何入口——媒体查询的意图就是单栏隐藏它，底部导航 5 个按钮（技能/餐厅/商店/背包/设置）也不含它。若要让手机可看状态栏，属**新增功能**（需给它一个抽屉入口），不应靠放开这条 `display:none` 实现。

## 11.13 信箱（2026-09-11）

**第一个带新存档字段的系统**（`player.mail`），因此格外注意了存档兼容与「不改变既有行为」。定位很窄：**只做兜底与留档，不接管任何既有奖励的发放路径**。

### 要解决的两个真问题（都是先确认存在才动手）

1. **东西会静默消失**：`gainItem` 在两种情况下丢东西且**绝大多数调用方不看返回值**——
   - 新物品种类 + 背包格已满：发个 `inventory:full` 事件就 `return false`；
   - 已达堆叠上限（食材 9999 / 装备等不可堆叠品上限 1）：`add = min(qty, cap-have)` 截断，**连事件都不发**。
   实测：连做两把同名「不可堆叠」厨具，第二把直接蒸发；挂机时背包一满，后面的产出全丢。
2. **一次性明细无处回查**：离线结算弹窗一关，之前的产出明细就再也看不到（只有一条滚动日志）。

### 实现

- **数据**：`player.mail = { list: [{ id, ts, kind, from, subject, body, reward, claimed, read }], nextId }`，已进 `defaultState` / `serialize` / `applySave`。旧档回退空信箱；旧档缺 `nextId` 时按现有最大 id 推算（防 id 撞车）。
- **三个动作**：`sendMail`（投递）、`claimMail`/`claimAllMail`（领取，**先过 `canGainItem` 预检**）、`deleteMail`/`clearSettledMail`（有未领附件的不可删）。`canGainItem(itemId, qty)` 是新增的严格容量判定（新种类要有空格、已有种类要够堆叠余量），`unsocketGem` 也改用它。
- **三条来源**：① **溢出转存**（`gainItem` 的溢出分支调 `_fileOverflowMail`；与上一封未领的同类溢出邮件**合并**，避免刷出几十封只装 1 个的邮件）② **离线回执**（`bootstrap.settleOffline` 里追加，**不带附件**）③ **新档欢迎信**（纯文案、不带附件——刻意不给启动礼包，避免动早期数值曲线）。
- **容量 60 封**：满则淘汰最旧的「已领或无附件」邮件；若全是未领附件则拒收（`sendMail` 返回 `null`）。刻意不做成无限仓库，保留背包扩容的压力。
- **`gainItem` 返回值语义保持不变**（满时仍 `false`、截断仍按 `add > 0`），所以既有调用方零改动；新增的是「不丢东西」这条通道。
  ⚠️ **一处必须先拆的雷**：`unsocketGem` 原先用 `if (!gainItem(id,1)) return { ok:false }` 判断背包满——加上转存后，背包满时宝石会**既留在插槽里、又被转存进邮箱**，玩家领回来就白嫖一颗（无限复制漏洞）。已改为 `canGainItem` 预检 + 必定成功两步。这条写进了 AGENTS.md，因为以后凡是「发不出去就回滚」的逻辑都有同样的坑。
- **入口**：左栏「今日」分组的 📬 信箱（**带待领数量角标**，`.feature-tile` 加了 `position: relative` + 新 `.feature-badge`），右栏「快捷状态」在有待领时出现一行「📬 N 封待领 [去查看]」。`inventory:full` 的旧提示语同步改写为「…已转存到信箱」，否则玩家会以为东西没了。
- **守卫**：`system_test.mjs` 新增 **C11 信箱 31 项**（欢迎信无附件、溢出不丢且合并、领取的容量预检与防重复、堆叠截断转存、**拆卸宝石不重复**、删除规则、容量封顶与淘汰、一键领取、存档往返、旧档迁移/缺 nextId 推算）。

## 11.14 系统日志 / 行情 / 厨友（2026-09-11）

Tier B 的后三项。前两项是**把已有数据从「弹窗/角落」搬到可查的页面**，第三项带一个新存档字段。

- **🗂 系统日志**（`LogsView.vue`，左侧栏 · 记录与回顾）：原先只有右栏 `StatusPanel` 显示**最近 8 条**。新页把 `ui.log` 做成可**分类筛选**（常规/获得/升级达成/离线/警告，带计数）、**关键字搜索**、**一键复制**（贴给他人排查用）、**清空**的独立页。
  - **保留量 100 → 500**（`stores/ui.js` 的 `LOG_MAX`）。**仍然不入存档**：挂机游戏每 tick 都可能 `pushLog`，写档会显著放大存档体积与写入频率；页面里明确写了「刷新即清空，长期记录看年鉴」。
  - 这是刻意的取舍：**日志页不追求跨会话可回溯**，跨会话的历史由既有的「年鉴/统计」承担。

- **💹 行情与限时窗口**（`MarketView.vue`，左侧栏 · 今日）：`marketEvents.js` 的 7 个限时窗口原先只在 `MarketEventsModal` 弹窗里可见，且只显示「现在开了哪些」。新页给出：
  - **此刻行情**：命中窗口 + **剩余时间**（每秒刷新，跨整点自动换档）+ 叠加后的各乘区倍率；
  - **今日排班**：24 个小时格，格子里是该小时命中的窗口图标（深色格 = 有加成），**点格子看该小时的叠加明细**；
  - **每周固定档**（主厨日 · 周日 / 疯狂星期四 · 周四）与**每日轮换**（5 个），当前命中的标绿并显示剩余时间；
  - 说明了两件容易踩的事：多个窗口**相乘叠加**、时段按**本机时间**判定（「午夜食堂」跨夜，排班表里拆成两段）。
  - 全部调用既有 `activeMarketEvents(hour, weekday)` / `aggregateMarketBoost(hour, weekday)`（它们本就支持传参，正好用来算排班表）——**没有改动 marketEvents.js 一行**。

- **🤝 厨友**（`data/friends.js` + `FriendsView.vue`，左侧栏 · 餐厅经营）：第四个候选，也是本轮唯一带新存档字段的。
  - **为什么是「镜像 NPC」**：这是纯单机、无后端，所以社交只能本地生成（与竞技场镜像对手、同业榜 NPC 同一思路）。页面里也**明说**「单机没有真人好友，这 8 位是本地生成的镜像同行」，不假装有服务端。
  - **8 位厨友**各有菜系、口头禅与一个**委托物品池**（池子里全是**现存**低阶食材，保证早期交得起）。
  - **两条每日动作**：**拜访**（每人每日一次，给金币 = `base × (1+0.25×羁绊等级)`，沿用吉祥物公式）；**帮厨委托**（每人每日一份，交付指定食材换赏金，赏金 = `物品 value × 数量 × (1.4~2.0)`，对齐「食客订单」的 ×(1.5~2.2) 并略低一档）。委托**优先点名你背包里已有的物品**，避免交不起。
  - **羁绊 1~5 级**（累计互动 1/5/12/25/45 次）**只放大本系统自身**的礼物与赏金（每级 +15~25%），**绝不叠加**到餐厅/采集/对决等既有乘区——那些节奏已经标定过。另加「一键串门」按钮与左侧栏待拜访角标。
  - 存档：`friends: { data: { [id]: { bond, lastVisitDay } }, orders, orderDay }`，已进 `defaultState`/`serialize`/`applySave`；旧档回退空对象且 `friendState()` **懒初始化**（旧档不迁移也能立刻拜访）。
  - **守卫**：`system_test.mjs` 新增 **C12 厨友**（羁绊门槛换算、委托池物品必须真实存在、同一日重复拜访被拒、跨天恢复、材料不足拒交、交付扣料给钱并清委托、跨天重掷、存档往返、旧档懒初始化）。

## 11.22 抽出「卡牌对战」+ 新增「奇遇图鉴」（2026-09-11）

「还能加什么页面」那轮待办的最后两项。

### ① 抽出「🎴 卡牌对战」（纯搬家，功能页 48）

它是个完整小游戏（3v3、三档难度、每日首胜奖励），却和图鉴、故事一起挤在「图鉴」页的标签里——性质与从对决页抽出的「食神秘境」完全一致。
做法与故事那轮相同（先做依赖分析 → 脚本按行搬运 → 四处登记 + 攻略词条）：搬走 80 行模板 + 46 行脚本、19 个符号，`PAGE.cards` 换成新页局部常量。**抽出后 LogView 累计 56.1 → 35.0 kB**，页签栏只剩「图鉴 / 配方手册」+ 四个跳转按钮。

### ② 新增「❓ 奇遇图鉴」（新系统，功能页 49）

奇遇（挂机动作 0.2% 触发的小事件）此前**完全不留痕**：弹出、选完即消失，玩家既看不到全貌也回看不到遇过哪些、选过哪支——与「信箱」修「离线明细无处回查」是同一类问题。

- **新存档字段** `player.encounters = { seen: { [id]: 次数 }, picks: { [id]: [各分支次数] }, total }`，已进 `defaultState`/`serialize`/`applySave`，旧档回退为空。
- **记录点两处**：`bootstrap` 触发时 `markEncounterSeen`（并在首次遇到时写一条**年鉴**），奇遇弹窗选择时 `recordEncounterPick(id, index)`。
- **页面**：未遇到的事件显示为「❓ 未知奇遇」（保留探索感），遇到过的显示正文 + 各分支奖励预览（金币按**当前对决等级**换算）+「选过 N 次」。
- **一律不改触发率与奖励数值**：奖励仍由 `EncounterModal` 原路径发放，本页只记录与展示。
- 存档体积影响极小（仅计数，无列表）。

### ③ 顺手修掉两个真实缺陷（都是新写的断言抓出来的）

1. **奇遇分支里有幽灵物品 id**：`egg` 与 `cheese` 在物品库中都不存在。后果比「拿不到」更糟——`gainItem` 会把它们当作新品种写进 `inventory`，于是玩家背包里多出一个**不在物品库里的假条目、白占一格**。已替换为真物品：野鸡蛋（Lv1，同为蛋类）/ 芝士面包（库中无奶酪，取「芝士」类真物品）。
2. **图鉴「获取来源」完全没收录奇遇**——正因为没人扫过 encounters 的物品引用，幽灵 id 才一直没被发现。已补录来源串，并在 `sourceJump.js` 补上跳转规则（**漏了这一步 `item_triple_audit` 会直接 FAIL**，本轮就是被它拦住的）。

新增两条 CI 断言把这条链闭合：**分支奖励物品必须真实存在** + **这些物品必须在图鉴来源中标注「随机奇遇」**。C13 共 19 项。

### 数字更正

- 功能页 47 → **49**；LogView 累计 **56.1 → 35.0 kB**（两轮抽出共省 21.1 kB）。
- README 系统表里「随机奇遇」原写「8 个事件 × 3 选 1」——实测分支数是 **3,2,3,3,3,3,3,3**（有 1 个只有 2 支），已更正为「多为 3 选 1、个别 2 选 1」。

## 11.21 抽出「故事与传闻」独立页（2026-09-11）

承接「还能加什么页面」那轮的待办第 2 项。**这不是新增功能，是把图鉴页里一块内容搬出来**——故事（主线 8 章 + 传闻 506 条 + 轶事 3588 条）原本是图鉴页的最后一个标签，共 **122 行模板 + 123 行脚本**，与图鉴的物品浏览是两种完全不同的任务。

### 做法（沿用抽秘境/装潢的同一条路）

- 先做**依赖分析**再动手：把故事模板块引用的脚本符号分成「仅故事使用（23 个，可搬）」与「其它模板也在用（5 个）」；实测确认故事模板的 **39 个类全部来自全局样式**（LogView 的 scoped 样式里一个都没有）→ **不需要搬任何样式**。
- **用脚本按行范围搬运**（模板 122 行 + 脚本 123 行），不手抄，避免转录错误。
- 图鉴页保留三个标签（图鉴/卡牌对战/配方手册）+ 三个跳转按钮；`tales_ext`（1.4MB 文案）的**懒加载随 refs 一起搬到新页**——只有进故事页才拉，首屏不背。

### 结果

- **LogView 56.1 kB → 42.8 kB**（省 13.3 kB）；新页 14.4 kB / 300 行。
- 新页自带 主线/传闻/轶事 三档切换、分页（轶事实测 24 张/页）、深色适配；左栏落在「记录与回顾」组（与年鉴相邻，同为记录/叙事类）。

### 三个踩到并修掉的问题（都值得记）

1. **脚本常量与模板引用的成对替换**：把 `PAGE.quirk` 改为新页局部常量 `QUIRK_PAGE` 时，只改了脚本、**漏了模板里那一处**（`<Pagination v-if="currentQuirks.length > PAGE.quirk">`）→ 新页里 `PAGE` 未定义、运行时报错。**页面照样渲染出别的内容**，截图与内容断言都没抓到，是**控制台错误捕获**发现的。
2. **守卫写死文件名 → 一搬家就误报**：`content_sync_audit` 的「故事需求 kind 有分支」原先固定读 `src/views/LogView.vue` 找 `storyCur`，抽走后立刻 FAIL。已改成**按内容自定位**（找出包含 `function storyCur` 的视图文件；找不到才 FAIL），以后再搬家不会失效。
3. **口径更正**：我此前报「故事 256 行 / 11.9 kB」是把「从故事块到文件末尾」算进去了，**把 `<style>` 也算成了故事**；实际是 **122 行 / 6.6 kB**。数字下结论前要确认统计区间。

## 11.20 新手引导横幅：按目标页淡化（2026-09-11）

用户报「点今日待办却跳到采摘界面」。核查后确认**导航本身完全正常**（四处登记齐全、构建产物里 `activeView === "today"` 与 `TodayView` chunk 都在、线上与本地 dev 实测点击都正确停在今日待办），
而线上抓取的横幅文案给出了真相：

```
hasBanner: true
bannerText: "① 选择挂机目标开始采集 / 技能页 · 采摘 / ✕"
```

**新手引导横幅在每个页面都渲染、并且一直用主色高亮**，而它第一步的定位文案就是「技能页 · 采摘」——它压在「📌 今日待办」标题上方，被读成「页面内容/跳到了采摘页」是很自然的事。

### 改法（用户选的方案 c：当前页不是目标页时淡化）

- **每个引导步骤声明目标页面 `view`**（①②③⑤ → `skill`，④ → `quests`）。
- 当前页 ≠ 目标页时加 `.newbie-dim`：去掉主色强调（文字转 `--muted`、字重 600→500、背景由橙色渐变换成 `--bg-soft`、边框转 `--border`），**✕ 仍保持正常可见**（用逐元素降级而不是整体 `opacity`，否则连关闭按钮也一起变淡）。
- 只有真正该去的页面才醒目——实测：技能页 `rgb(184,68,42)`/600，今日待办与信箱 `rgb(122,111,104)`/500。

### 顺带修掉一条过期定位

第 ④ 步原文案是「图鉴 → 任务页」，但**每日任务早在 v1.8.0 就搬到了独立的「任务中心」页**（当时把图鉴里那个标签改成跳转按钮，漏改了这一句）——现更正为「左侧栏 · 任务中心」。这类「内容搬家后没同步文案」的漏项，本轮的派生守卫（§11.15）只覆盖攻略关键词，**覆盖不到引导文案**，所以只能靠人工排查时顺手发现。

## 11.19 今日待办页（2026-09-11）

用户问「还有什么功能页面可以加」后，先扫了一遍「数据模块 vs 页面」（见 §11.18 那轮的方法），结论是：**纯新增的缺口只剩「随机奇遇」一个**（且它要做成页面得新增存档字段记录「遇过哪些」），而**收益更高的不是再加系统，而是把已存在的散落内容聚起来**。

### 依据：每日动作的分散程度（实测）

- 全项目有 **15 处「每日闸门」**（用 `todayKey` 判定、每天重置）：每日任务、签到、天气、吉祥物、厨友、宴会、外卖、供应商、常客、交易所、地窖、采集队、评论家、大赛、赛季；另有周度（名厨、厨具赛、公会）。
- 它们住在 **12 个页面**里，而右栏「快捷状态」只覆盖 4 项（每日任务/任务/赛季/信箱）。
- 加上上一轮实测出的「入口在顶栏、提示在左栏」错位 → 每次上线都要挨个翻页面找「今天还能做什么」。

### 页面设计

五类分区，按可操作性排序：**① 可领取**（做完没领的，排最前）**② 今日还没做**（签到/吉祥物/厨友/大赛/交易所额度）**③ 到货与到期**（地窖、采集队、评论家到访——给剩余时间）**④ 本周**（名厨、厨具赛、公会任务）**⑤ 今日信息**（限时窗口命中、天气与幸运食材）。

每条一个「去处理 ↗」：页面类直接 `ui.setView`，弹窗类（签到）走 `ui.toggleSignIn`。**本页只汇总与跳转，不代玩家执行领取/交付**——所有奖励仍由原页面原路径发放，因此**零新增存档字段**。
放在左栏「今日」组**第一格**（它是「上线先看什么」的落地页），磁贴角标 = 待领 + 信箱 + 可拜访之和。

### 为此新增的 4 个只读判定（都在 store，风格与 `spiritStoryPending`/`banquetReady` 一致）

`schoolReadyList()`（研究完成待收的学派）、`regularGiftReadyCount()`（满级且礼物未领的常客数）、`seasonClaimableCount()`（点数够且未领的档位数）、`guildTasks()`（当前公会任务列表）。

### 两个值得记住的点

1. **派生守卫当场发挥了作用**（§11.15 那轮把白名单从手抄改成派生的收益）：新页一加进来，`content_sync_audit.mjs` 立刻 FAIL 并点名 `today(今日待办→今日待办) 缺攻略关键词`——补上攻略词条与白名单后转绿。**如果是手抄白名单，这次又会静默漏掉。**
2. **CSS 私有类名混用的事故**：`.td-claim` 同时被用作「英雄区按钮」与「行的语气类」，按钮的 `margin-left: auto` 于是套到了每一行「可领取」的行上——在 flex 列容器里它把该行**推向右并缩成内容宽**（实测行宽 385px vs 卡片 1368px）。
   截图目视在深浅两色下都不明显，是**量几何**（断言「每行 left/width == 容器 left/width」）才当场露出来的。教训已写进 AGENTS.md：同前缀下别把元素类与状态类混用；布局改动要用几何断言验收。

## 11.18 顶栏窄屏修复与导航命名（2026-09-11）

用户问「功能页与顶栏该不该互相搬家」。核查后的结论是：**两层完全不重叠**（7 个顶栏 view 一个都不在左栏），所以没有冗余要清；但顺手**量出一个真缺陷**，比搬家值得修。

### 实测：顶栏在窄屏被裁

顶栏 17 个按钮共用一个 `overflow-x: auto` 的单行容器：

| 宽度 | 顶栏可视 / 内容 | 被裁按钮 |
| --- | --- | --- |
| 1440 | 920 / 920 | 0 |
| 768 | 558 / 894 | **6 个**：签到、搜索、厨藏、装备、设置、存档 |
| 390 | 390 / 768 | **7 个**：攻略、签到、搜索、厨藏、装备、设置、存档 |

滚动条被 `scrollbar-width: none` 隐藏，所以玩家看不到「右边还有」；更要紧的是**顶栏「右侧功能组固定贴右不动」的设计意图也一并失效**。**平板最严重**：768px 没有底部导航兜底，签到/背包/装备/设置/存档**全部**藏在那条看不见的横滚里。

### 修法

- **主按钮区与右侧组拆成两个容器**：`.top-nav-main`（7 个高频入口，自己横滚兜底）+ `.top-nav-right`（右侧功能区，永不跟着滚走）。
- **≤940px 右侧组收进「⋯」浮层**：`.top-nav-more` 呼出竖排浮层；点项/点外部/切页/滚动 四种时机都会收起；纯图标按钮在浮层里补文字标签（活动/统计/图鉴/攻略/设置/存档）——手机上本来就没有 hover 提示。
- 修后三档宽度**全部 0 被裁**、顶栏不再横向溢出（390px 下 7 个主按钮正好排进 346px）。

### 一个更隐蔽的 bug（值得记住）

浮层做完后截图发现**它根本没显示出来**，但 `toBeVisible()` 却是 true。原因：`.top-nav` 上残留 `overflow-x: auto`——按 CSS 规范，一个轴非 `visible` 会把另一轴也算成 `auto`，于是**绝对定位到顶栏下方的浮层被顶栏自己的盒子裁掉**。

**教训：`toBeVisible()` 只反映 CSS 可见性，不反映裁切与遮挡。** 判断「玩家真的看得见吗」要用 `document.elementFromPoint(元素中心)` 检查命中的是不是它自己。已把这条写进 AGENTS.md。

### 顺带两项

- **命名区分**：左栏「试炼」改名「**厨神试炼**」（与页面标题一致），与顶栏「🗼试炼塔」区分开——两者是**不同系统**（`trials` 厨神试炼 vs `tower` 无尽挑战塔），旧名字只差一个字、同在挑战语义里，最容易混。
- **任务中心补可领取角标**（每日 + 周常 + 每周挑战的待领数，新增 `player.pendingClaimCount()`）。
  ⚠️ **公会不加红点**——我原先的建议基于错误假设：实测公会任务是「完成即自动结算」（`guild.points += reward` 且进度归零），**没有待领态**，红点只会常年亮着。竞技场同理不加（每 5 分钟刷新）。红点的价值在「稀缺的一次性待办」。

### 关于「搬家」的结论

- **左栏 → 顶栏：不做。** 顶栏 7 位是 2026-09-10 从分页改回来的明确设计（「不再分页」），**容量是硬约束**；左栏最高频的三项（任务中心/信箱/厨友）都在「今日」组、首屏可见，其中两项已有角标——用「角标 + 靠前排序」提升可见性比占顶栏位划算。
- **顶栏 → 左栏：不做。** 餐厅/公会/赛季/竞技场/觅珍高频或有时效性；试炼塔与大赛虽低频，但承担**目标感**（让人看到还有东西可追），搬走反而弱化长线激励。
- 两层**没有重叠**（7 个顶栏 view 全不在左栏），分工清晰：顶栏 = 高频长线系统主页面，左栏 = 子系统与专项页。

## 11.17 信箱放宽：溢出零丢失 + 系统奖励到账（2026-09-11）

§11.13 给信箱定的边界是「只做兜底与留档，绝不接管既有奖励的发放路径」。用户追问「放宽怎么放宽」后，**先实测再决定**，结果发现旧设计藏着两处隐患。

### 实测发现（决定了先改什么）

1. **拒收 = 物品真的蒸发**：把信箱塞到 60 封未领后继续制造溢出，**21 次被拒收**——旧设计在最坏情况下会退回「静默丢东西」，正是信箱本要修的那个毛病。
2. **合并规则太弱**：只与「最后一封」合并，实测交替溢出 A、B、A、C、B 会产生 **5 封**。
3. 体积参考：单封邮件约 236 字节，新档存档 6.6KB → 60 封约 14KB。

### 改法

- **溢出邮件按物品合并**：查全表而非只看最后一封（上述例子 5 → **3 封**），封数≈有溢出的物品种类数。
- **软上限 60 → 200，且不再因满而拒收**：`MAIL_CAP=200` 只淘汰「已领或无附件」的旧邮件；全未领则继续累积，到 `MAIL_HARD_CAP=500` 才拒收做病态保护。实测 260 种物品溢出：**丢失 0 种**。
- **新增 `reward` 奖励到账通道，首个来源是赛季档位**：`seasonClaimTier` **领档即记账**（`claimed` 立刻 push、扣点、+3 见闻都照旧，所以赛季进度与 `seasonAll/season5` 成就阈值完全不受影响），奖励改为寄一封邮件；金币类不占背包格必然能领，**物品类在背包满时也先安全存着**；**硬上限拒收时回退直接发放**，奖励永不丢。
- 配套：`SeasonView` 的成功提示交回 `season:claim` 全局监听（一个动作一条日志，说法统一为「已寄到信箱」）；`MailView` 加「🎁 奖励到账」分类、改写容量说明，并补上「信箱里的东西不能用」。

**为什么不担心信箱变成无限仓库**：信箱里的物品**不能使用**，不构成额外存储，只是「暂时取不走」的缓冲，不消解背包扩容的意义；而拒收的代价是物品蒸发——两害相权，取不丢。

### 放宽后仍然保留的边界

奖励**可以**走邮件，但必须「**先记账、后到账**」，且投递被拒时回退直接发放；**结算类的既有产出**（离线、采集、制作）仍走 `gainGold`/`gainItem` 直接进包，不改成邮件。方向四（把每日/周常/成就等既有奖励全部改成邮件发放）经评估**不做**：会动到大量「即时到账」断言，并改变「点领取」的节奏，收益不抵代价。

### 守卫

`system_test.mjs` C11 信箱扩到 **42 项**，本轮新增：**溢出零丢失**（锁死旧 bug 的核心断言）、软上限时优先淘汰无附件旧邮件、按物品合并、硬上限才拒收、赛季领档发邮件且金币不直接进包、领取奖励邮件即到账。

⚠️ 一条测试教训：**不要用「邮件数没增长」判断是否被拒收**——淘汰（清理已领/无附件旧邮件）同样不会让长度增长。改前我就这么判过一次，把一个「淘汰了欢迎信」的正常事件误报成「拒收」；改成断言「物品在不在背包或某封邮件里」才是对的。

## 11.16 左栏功能页体检：图标与排序（2026-09-11）

对左栏 45 个功能页做了一次分组/排序/图标体检。**没有页面放错到影响使用**，但量出 6 类问题，其中 4 类（图标）与 2 类（排序）本轮已修。

### 量出来的事实

- 左栏内容高 **1712px**、可视 **966px**（1920×1080）→ 需滚动 746px，**后 3 组共 16 个瓦片默认在折叠线下**。（**已由下方「4 列」方案解决**：改 4 列后内容 1112px、需滚仅 146px。）
- 组容量不均：餐厅经营 12 页（4 行）最大，研究与收集/成长与信仰 各 4 页（2 行、末行空 2 格）；8 组里 6 组末行残缺。
- 图标**同形重复 13 处**（去掉变体选择符 FE0F 后比对）：`🌤``💹``🛒``🍽``🗺×4``📜``⚔×3``🤝×3``📖×3``🏮×3``🏅``🃏``✨`。

### 本轮修了什么

**图标（19 处替换）**
- **`🍽️` 与 `🍽` 是同一个码位 U+1F37D**，只差一个不可见的选择符——珍馐阁（商店）与宴会（限时大订单）因此**长着同一张脸**，跨平台还可能一个渲染彩色、一个渲染单色。宴会改 `🥂`。
- **同组内两个瓦片同图标**：`🤝` 供应商/厨友、`🃏` 厨具赛/名厨 → 供应商改 `📦`、厨具赛改 `⚒️`。
- **组标签与组内瓦片同图标**（5 处）→ 组标签统一改为更通用的符号：今日 `🗓️`、采买与转化 `🧺`、餐厅经营 `🥢`、记录与回顾 `🗃️`、挑战与休闲 `🎯`、成长与信仰 `🌱`（挂机产线 `🌾`、研究与收集 `📚` 本来就无冲突）。
- **跨栏同符号**（顶栏 vs 左栏）：餐厅装潢 `🏮`→`🛋️`、装备总览 `⚔️`→`🛡️`（这处是上一轮加页时我自己引入的）、常客 `📖`→`👋`、图鉴兑换 `📖`→`🎟️`、里程碑 `🗺`→`🏁`、产地 `🗺️`→`🌍`、年鉴 `📜`→`📰`、厨友 `🤝`→`🍻`。
- 结果自检：**45 个瓦片图标互不相同、8 个组图标互不相同、且与顶栏 7 个入口 + 6 个图标按钮均无同形**。

**排序（2 处）**
- **餐厅经营**：把带角标的「厨友」从第 11 位提到第 9 位（折叠线以上），并让「人与关系」聚在一起 →
  `评级 · 班底 · 分店 · 交易所 · 宴会 · 外卖 · 供应商 · 常客 · 厨友 · 同业榜 · 套餐定食 · 餐厅装潢`。
  实测角标「今日还可拜访 8 位」现在落在可见区（改前在折叠线下）。
- **记录与回顾**：系统日志从第 3 位移到末位，恢复「里程碑 → 年鉴 → 赛季回顾 → 荣誉殿堂 → 成就与称号 → 图鉴兑换」的时间线叙事（它是低频排查工具，插在中间打断叙事）。

**已知遗留（属刻意取舍，未动）**
**① 折叠线与列数（2026-09-11 已解决）**：3 列时需滚 746px、后 3 组全在折叠线下。实测四种做法后改 **4 列**：
瓦片 74→54px，内容 1712→**1112px**、需滚降到 **146px**（可视 87%），且**零名字换行、零裁切**。
顺带把「成就与称号」（5 字，4 列下唯一会换行的名字）在左栏简写为「**成就称号**」（页面标题与跳转文案仍用全称，与「评级/米其林评级」同例）。
实测 5 列虽能做到**零滚动**，但有 14 个瓦片名字换行并被裁切，故不采用；手机抽屉（239px 宽）下 4 列瓦片约 51px，仍高于 44px 可点下限。

**② 称号佩戴双入口（2026-09-11 已解决）**：「成就与称号」与「荣誉殿堂」原先**都能改 `player.title`**（状态同一份，但用户不知道该去哪个）。
现由**荣誉殿堂独占佩戴/取消佩戴**（称号的被动与荣誉等级本来就在那里），成就页的称号页签改为**只读展示**，并在页头给出「去荣誉殿堂佩戴 ↗」。
顺带修掉荣誉殿堂表头的一处标注缺口：它写「称号来源：成就 N + 商店 N，共 85 个」，实际含**三类**（成就 76 / 商店 7 / 图鉴 2），表头漏说了「图鉴兑换」。

**③ 短名 vs 全名（保持现状，未动）**：左栏一律短名（评级/班底/风味册/同业榜/厨具赛/套餐定食/餐厅装潢…），攻略与图鉴里是全称。
`content_sync_audit.mjs` 为此维护了 18 条映射——实测**其中 9 条冗余**（磁贴名本身就能在攻略正文里查到），**真正需要兜底的只有 9 条**。
保持短名的理由：4 列后瓦片只剩 54px，「风味搭配册」这类 5 字全称根本放不下；而映射的维护成本已被派生守卫自动化（漏配会 FAIL 并点名）。

## 11.15 内容同步补账（2026-09-11）— 一次「守卫有盲区」的复盘

v1.8.0 加了 9 个功能页与 2 个新系统（信箱、厨友），但只做到「页面能玩」：**没把新系统同步进既有的长线系统**（成就 / 统计 / 故事 / 称号 / 攻略）。用户指出后核查确认，确实整批漏掉。

### 为什么脚本没抓到（根因）

「六项同步」这条流程本身是有的（AGENTS.md 的强制项），每批也都跑了 `content_sync_audit.mjs`，且**每批都 PASS**。问题在于：

- 那条「**攻略：总览覆盖全部功能关键词（N 个）**」用的是一份**手抄白名单**——`content_sync_audit.mjs` 里的 `required` 数组，从 2026-09-10 起每批新系统都靠**人手**往里加（数组里至今留着「第X批：米其林 / 风味搭配册 / …」的注释）。
- 于是它**天然抓不到「新系统没被加进白名单」**——因为白名单本身就是那个该改的东西。它只能证明「白名单里的老关键词还在」。
- 实测后果：信箱 / 厨友 / 行情 / 系统日志 在 攻略 / 统计 / 故事 / 成就 里出现次数**全是 0**（「行情」那两处是误命中：成就名「行情老手」、故事里的「交易所行情」）。

**结论：PASS ≠ 已同步。** 一条自证的守卫会给人「已核对」的假安全感。

### 本轮补了什么

- **📖 攻略**（`guide.js`）：新增 信箱 / 厨友 / 行情与限时窗口 / 系统日志 四条总览词条；顺带修正攻略里过期的数字「成就（176）与称号（82）」→ 183 / 85。
- **🏅 成就 + 称号**（`achievements.js`）：给两个新系统各配成就——`信箱初启`（首次领附件，title「收信人」）、`物归原主`（累计领 30 次）、`初次登门`（首次拜访，title「街坊」）、`整条街的熟人`（8 位全部有往来，title「邻里之主」）、`老交情`（任一羁绊满级）。成就 178→**183**，称号 82→**85**（称号自动进称号页与荣誉殿堂被动）。为此在 `claimMail` 里加了 `stats.mailClaimed` 计数器（邮件会被清理，不能靠数 list）。
- **📊 统计**（`StatsView.vue`）：补「信箱待领」（含累计领取次数）与「厨友往来」（x/8 位 · 累计互动次数）两行。
- **📜 故事**（`story.js`）：第五章新增一段「街坊与信箱」，把厨友、限时窗口排班、信箱溢出转存、系统日志写进主线散文。
- **任务：不补**。`quests_extra.js` 是生成器产物（冻结数据，不许手改），且历史先例中新系统在 `quests.js`/`quests_extra.js` 里出现次数**全是 0**——任务这条只做「kind 有处理 + 引用有效」的一致性校验。

### 根因修复：把守卫从「手抄」改成「派生」

`content_sync_audit.mjs` 新增一条检查：**左栏「功能」页签里的每个页面，都必须在攻略总览里找到对应关键词**。关键词默认取侧栏磁贴名，叫法与攻略用语不同的（如 `decor`→装饰、`rivals`→同业竞争、`gearContest`→厨具大赛、`michelin`→米其林等 17 处）在 `VIEW_GUIDE_KEYWORD` 里显式映射。

这样「加了新页却忘了写进攻略」会直接 FAIL，而不是靠人记得手动加一行。

**并用负向测试验证过它不是摆设**：往 `Sidebar.vue` 注入一个假页面后复跑，检查如期 FAIL 并点名：

```
FAIL  攻略：左栏全部功能页均有攻略关键词（派生检查 46 页） 缺: brandnewpage(一个还没写进攻略的新页→…)
FAIL  攻略：导航入口均已注册视图（53 个） 未注册: brandnewpage
```

### 教训（已写进 AGENTS.md）

1. **一处手抄的清单就等于一处盲区**：凡是「靠人记得同步」的地方，都要问一句「忘了会怎样」——如果答案是「检查恒真」，那这条检查就是自证，必须改成从单一来源派生。
2. **新的守卫要用负向测试证明它不是空的**（故意造一个应该 FAIL 的输入，看它是否真的 FAIL）。
3. 文档里的**数字也是会过期的**：本轮顺带修掉 AGENTS.md 里「攻略覆盖 49 个关键词」（实测 75，改动后 79）与攻略页里「成就（176）与称号（82）」两处陈旧数字。


### 风味搭配册的组合可行性约束（2026-09-10 修正）

- **点亮判定**：`ProductionSkill.craft` 成功后调 `player.discoverFlavors(Object.keys(recipe.ingredients))` → `matchFlavorPairs` 要求**同一条配方的材料表里出现该组合的全部食材**（顺序无关，多余食材不影响）。
- **硬约束**：因此每条 `FLAVOR_PAIRS` 必须能在现有配方（1236 条：技能实例 + 炼金）中找到同时含全部食材的配方，否则玩家永远点不亮。
- **首版缺陷**：28 条里只有 **7 条**有解，「番茄与罗勒」这类组合因 `basil`（罗勒）只出现在 2 条配方里且都不含 `tomato` 而永远无法点亮。
- **修正方式**：按「保留原主题 + 换成真实共现食材」重写 21 条（如「番茄+罗勒」→「番茄+大蒜」= 披萨/意式酱底；「葱姜去腥」保留名与意，食材改为真实共现的 `onion + ginger_young` = 红烧鲈鱼）；**不改任何配方**（配方为数据铁律层）。
- **id 保持稳定**（如 `fp_tomato_basil` 仍用原 id 但食材已换），避免旧档已点亮的记录丢失；文件头已注明 id 是稳定键而非食材派生。
- **自查工具**：`src/game/data/flavorRecipes.js` 的 `recipesForPair(items)` / `easiestRecipeForPair(items)`（**必须先 `createSkillInstances(player)`**，否则 `getAllSkillInstances()` 为空会误判为无解）。
- **守卫**：`system_test.mjs` 断言「全部 28 条均可在现有配方中点亮」+ id 唯一 / 食材组合互不重复 / 2~3 味 / 名称说明奖励齐备 / 引用食材存在；已用反例验证（把任意一条改回无解组合 → FAIL 并点名该条）。

## 11.10 能量饼干的溢出问题与两个新出口（2026-09-10）

**问题**：能量饼干是**唯一**「一次性永久升级」型的消耗品（效果 = 离线时长上限 +4h），而离线上限本身封顶 +12h → **一生只用得上 3 块**；但它的发放是消耗品式的（30 项成就共 38 块 + 烘焙 Lv20 配方可无限制作 + 探索/竞技场/挑战塔/卡牌/吉祥物/名厨/节庆 7 处可重复产出），固定来源就有约 **12.7 倍**超额。原有两个「兜底」都等于没有：商店半价回收 60 金币（后期可忽略）、炼金 能量饼干→草莓蛋糕（120 价值换 60 价值，与直接卖掉等价）。对比神秘调料——同样珍贵但战斗内可**无限次使用**，所以没有这个问题。

**约束**：物品的效果数值（`offlineBonusH: 4`）属**固定数据层**，不得修改；所以只能新增「读取/消耗它」的功能（向上兼容），不能调数值。

**解决**：给它开两个不封顶的出口（参数集中在 `src/game/data/biscuitUse.js`，便于调平衡）

- **A · 战斗内「能量补给」**（`Combat.useEnergyBiscuit()`）：消耗 1 块 → 立刻恢复 **25%** 最大品鉴值 + **5 回合**「精力充沛」（命中 +8、攻速 +10%），自身冷却 **4 回合**（与料理 3 回合同规格，互不影响）。定位介于料理（回血）与酱料（增益）之间的应急续航；靠冷却控制节奏而非次数上限。
  - 「精力充沛」的攻速加成走新增的 `Combat.biscuitSpeedPct`，在 `buffTurns` 归零时**一并失效**（不会变成永久加成，有守卫锁住）。
- **B/C · 常驻回收**（`player.exchangeBiscuitForTaste()`）：在**美食知识页**不限量把饼干换成**品鉴点**（`BISCUIT_TASTE_RATE = 10` 点/块）。选品鉴点是因为它本身就是**不封顶的持续消耗系统**（奥义 `costPerSec` 持续扣），天然能吸收任意多的产出；汇率刻意取得略低于「打一场同级战斗」的效率，避免取代战斗成为主要来源。

**入口**：战斗内按钮在 `CombatPanel.vue`（对决/试炼/名厨共用）+ 竞技场 + 挑战塔，与神秘调料并列（**必须是独立分组**——首版误嵌进「有神秘调料才显示」的 `v-if` 里，没有调料就看不见按钮，已修）；回收区在 `GastronomyView.vue`（支持指定数量与一键全部）；制作页在离线加时已满时提示这两个出口。

**守卫**：`system_test.mjs` 新增 19 项——离线额度确认（3 块封顶、封顶后不消耗）、战斗补给（回血/增益/冷却/可重复使用/到期清零/无饼干失败）、回收（预览/扣发/一键全部/统计/超量夹取/可反复）、以及「物品效果未被改动（offlineBonusH === 4）」的固定数据保护。

## 11.11 图鉴「详细作用」补全与审计门禁修复（2026-09-10）

**问题**：图鉴里能量饼干只显示「类型/档位/价值/获取等级」，看不出效果。

**两层根因**

1. **详情缺分支**：`itemDetailLines`（`src/game/data/itemDetail.js`）覆盖了保鲜剂/经验增益剂/产量增益剂/对决回血/持续回血/对决增益/醉酒/调味能量/随机增益/腐坏时间，**唯独没有 `offlineBonusH` 分支** → 该物品的效果从未被展示。已补三行：离线时长（含 +12h 封顶说明）、战斗用途（能量补给）、可回收（→品鉴点）。
2. **门禁失效（更严重）**：三个审计脚本 + 两个测试脚本共 **107 处** `check` 调用把标签当条件——签名为 `(name, cond, detail)` 却按 `(section, name, cond)` 传参，导致第 2 参字符串恒为真、真正的条件被当作 `detail` 丢弃。统计：`item_triple_audit` 6/8 恒真、`content_sync_audit` 6/16、`audit_sync` 18/19、`system_test2` 70/77、`continuity_test` 7/7。

**修复内容**

- 合并 107 处调用为正确签名（`check('分类：说明', 条件, 详情)`）。
- `item_triple_audit` 新增**效果覆盖检查**：物品上带 `offlineBonusH/heal/regen/buff/drunk/flavorEnergy/randomBuff/spoilMs/use` 任一时，详情全文必须出现对应关键词，否则 FAIL（已反例验证）。
- 来源跳转抽出单一来源 `src/game/data/sourceJump.js`：此前 `ItemDetailModal.jumpForSource` 与 `item_triple_audit` 的关键词白名单是**两份手工镜像且已漂移**，导致 **17 种来源（约 408 条，含「交易所（行情买入）」346 件）在图鉴里没有跳转链接**。现两边共用同一份规则，审计直接断言「所有来源都能跳转」；并补齐采集队/供应商/交易所/牧场/名厨挑战/图鉴兑换所/地窖的跳转目标。
- 修 `audit_sync` 三处自身 bug：流派名断言写成 `'调味'`（实际 `'调味流'`）、探针把分类名（茶饮/保鲜剂）与错名物品（应为「佛跳墙（简化版）」）当物品、成就断言用普通对象取 getter `collectionPct`（恒假）。
- 修 `item_triple_audit` 的错误前提：探索目标（`explore_001`…）**本身不是物品**，没有同 id 物品，改为只校验其战利品引用。
- **真 bug 修复**：竞技场「破纪录但未开宝箱」时（3/4/6~9 连胜），`reward` 从 `null` 展开导致 `kind: 'record'` 丢失——改为以 `record` 对象为展开基底（宝箱同场时仍保持 `kind:'streak' + record:true`，符合竞技场铁律「破纪录不得覆盖宝箱提示」）。
- **永久防线**：`content_sync_audit.mjs` 新增静态扫描，凡 `(name, cond, detail)` 签名的脚本，`check` 第 2 个顶层实参不得为字符串字面量（已反例验证会 FAIL 并点名到行）。

## 11.23 v2.1 音频（音效 / BGM）与皮肤系统（2026-09-13）

**共同点：零外部资源**——音频全部用 Web Audio **合成**，皮肤全部是 CSS 变量，既不加体积也不引版权问题。

- **音效（`game/core/sound.js`）**：结构改为「**音效总线 `master` + 音乐总线 `musicGain`**」两条 GainNode 接到 `destination`，因此 `sfxVolume` 与 `bgmVolume` 互不干扰（`setSfxVolume` / `setBgmVolume` 即时改 `gain.value`）。**24 种音效**全部是 `tone(freq, dur, type, gain, when, music)` 的短音组合，由 `App.vue` 的**事件总线接线**驱动——除点击音外，制作成败、升级、对决胜负、竞技场结算、赛季领档、公会任务、食灵契约、试炼通关、食神/宝箱等 **13 处**都是 EventBus 事件触发；**BGM 模式下 `tone(..., music=true)` 走音乐总线**，与音效音量解耦。
- **BGM（3 首曲目：`day` / `night` / `battle`）**：曲目按主题与**是否在战斗中**自动切换（`syncBgm()`：`getCombat()?.inFight ? 'battle' : 深色 ? 'night' : 'day'`），开关由 `settings.bgmEnabled` 控制——**关掉即 `stop()`、打开即按当前场景起播**（`watch` 依赖里同时含 `bgmEnabled` 与 `ui.phase`，读档 / 进游戏 / 战斗始末都会重算）。调度用 **200ms lookahead**：`scheduleBgm()` 每次把未来 0.6 秒内到点的音符用 `tone(..., when)` **提前排期**，杜绝 `setInterval` 抖动造成的断音；`play()` 对同曲目重复调用直接返回（不重启、不叠音）。
- **AudioContext 懒建**：`ensureCtx()` 首次调用才 `new AudioContext()`，失败一律 `return null` 静默降级（非浏览器 / 测试环境 / 浏览器拒绝时**游戏照常运行**）；`primeAudio()` 供设置面板在用户手势里解锁。
- **皮肤（`data/skins.js` + `applySkinToDom`）**：**15 套**（`classic` 原味 / `jade` 青瓷 / `sakura` 樱花 / `matcha` 抹茶 / `salt` 海盐 / `indigo` 靛蓝 / `plum` 梅子 / `tomato` 番茄 / `cinnamon` 桂皮 / `amber` 琥珀 / `maroon` 豆沙 / `truffle` 松露 / `perilla` 紫苏 / `turmeric` 姜黄 / `ink` 水墨），按**成就数**逐套解锁（`need: 0/4/8/14/20/28/37/47/58/70/83/97/112/128/145`，`skinUnlocked(id, achievements)`）。每套定义 **`vars`（浅色）+ `varsDark`（深色）两版主色**，以 **`<html>` 行内样式**落地——行内样式优先级高于「基础 `:root`」与 `html[data-theme='dark']` 两个块，因此**换肤在深色下同样生效**，且改主题时 `applySkin()` 会重算成对应版本。（第三批起每套的**整份色板**改为按主色推导，见 §11.24 / §11.25。）
  - ⚠️ **深色版 `varsDark` 里不要覆盖 `--primary-deep`**：深色块本身没有定义它（靠回退拿浅色值 `#7a2f16`），一旦在深色皮肤里改掉，「橙字压橙底」那类场景（侧栏选中行等）对比度会失守。
  - 皮肤偏好**另存全局键 `culinary-idle.skin`**（与 `culinary-idle.theme` 同理、与存档位无关），让**尚未读档的启动页**也能用上；启动阶段仍然零写档。
- **设置面板**：`soundEnabled` / `bgmEnabled` 两个开关 + 音效/音乐两条音量滑杆 + 皮肤选择器（未解锁项显示 🔒、点击不生效并给提示）。
- **守卫**：`e2e-dark.spec.mjs` 增加**按皮肤循环**的体检（皮肤 id 直接从 `skins.js` 读，新增皮肤自动纳入）：**深色轮跑完整规则**（对比度/亮底/白边/金光上限）、**浅色轮只跑「残留品牌橙」**（浅色对比度已由 `system_test` C20 的静态 WCAG 断言守住），实测 72057 元素全过；`e2e-audio-skin.spec.mjs`（第 4 套 e2e）覆盖皮肤选择/深色版主色/锁定态/设置写盘 + 重载恢复 + BGM 启停与曲目。
  - ⚠️ **浏览器侧验证坑（本轮实测）**：dev 下 App 加载的是 `/src/game/core/sound.js?t=<HMR 版本号>`，在测试里 `import('/src/game/core/sound.js')` 会得到**另一个模块实例**——模块级状态（`bgmTimer` 等）全为空，断言会「看起来功能没生效」。正确做法是先从 `performance.getEntriesByType('resource')` 取出 App 实际用过的那个 URL 再 import。

### 11.24 皮肤「整份色板」收口 + 全站色值 token 化（2026-09-13，同日第二批）

**动机（用户实测）**：换肤后**页面前 20 个颜色与原味逐字节相同**——皮肤只覆盖了 4 个主色变量，而底色/面板/文字/描边/滚动条要么是未被皮肤覆盖的主题变量、要么是各视图里写死的暖色（如 `rgba(150,110,70,·)` 暖棕描边 243 处、`rgba(255,252,246,·)` 暖白面板 183 处、`#d95a38` 主色 62 处）。

- **token 化（约 1400 处）**：`main.css` 390 处 + 68 个视图 1000 余处硬编码色值 → CSS 变量；`<script>` 段一律不碰，**canvas / JS 内联样式**改走新增的 `src/game/core/cssVar.js`（`cssVar(name, fallback)` / `cssRgb(name, alpha)`，读 `<html>` 上的变量，非浏览器环境返回 fallback）。
- **两类主色 token（关键区分）**：`--primary` / `--primary-rgb` 是**主题感知**（浅 `#d95a38` / 深 `#e0704a`）；`--primary-tint` / `--primary-tint-rgb` **两主题同值**（`#d95a38`）——深色规则里历史写死的浅色橙归后者，否则深色下会跟着变主色，等于偷偷改了原味渲染（首轮就踩到：400 处深色淡彩变色）。脚本按 postcss 规则上下文判定深浅，并**跳过变量定义行与注释行**（否则会把 `--primary: #d95a38` 替换成自引用 `var(--primary)`，变量整体失效——实测抓到过）。
- **皮肤改为「主色 + 推导出的整份中性色板」**：每套浅/深各一版，含 `--bg`/`--card`/`--bg-soft`/`--sidebar-bg`/`--lock-bg`/`--border`/`--scrollbar`/`--text`/`--text-dim`/`--muted`/`--panel*-rgb`/`--tint*-rgb`/`--ink-rgb`/`--scrim*-rgb`/`--deep*-rgb` + 主色家族与 `--*-rgb` 三元组；**深色主色按钮**（`--btn-primary-bg/border/hover/disabled-rgb`）按「白字 ≈5:1」推导（`towardContrast`），深色主色本身取「强调文字够亮 / 按钮白字够暗」的窗口（`#4a9c80` / `#c97293` / `#bb8734` / `#7d92ac`）。**不随皮肤的**：语义色（good/warn/bad/info）、金色描边与金光（`--border`/`--border-strong`/`--gold-glow*`）、琥珀与绿色说明按钮。
- **深色侧「淡彩底上的文字」四档 token**：`--on-primary-tint`（`#ffb98e`）/ `-2`（`#ffb08a`）/ `-3`（`#ffc9ad`）/ `-4`（`#ffd2b8`），皮肤按主色提亮推导。
- **验收（可复用的方法）**：① **指纹比对**——把「每页每个元素的计算色 + 全部 CSS 变量」导出 JSON，重构前后比对，**原味浅/深 40 页 0 差异**；② **残留品牌橙探针**（非原味皮肤 × 浅/深 × 7 页）从 302 处降到 **0**；③ 逐皮肤截图目视。
- **新增守卫**：`system_test` **C20 皮肤组 9 项**（id/名称唯一 · 门槛递增 · 原味为空 · 浅/深变量齐备 · 深色面板仍为深色 · 浅色版不得覆盖 `--primary-deep`/深色版不得覆盖 `--primary-deep` · `--primary-rgb` 与 `--primary` 同值 · 逐皮肤×浅深 WCAG 六对）；`e2e-dark` 逐皮肤两轮 + 品牌橙判定；`minigame_ui_audit` 断言改为**要求 token 形式**（写死色值 FAIL）；《小游戏UI标准》§6 同步改为 token 口径。
- ⚠️ **排查「残留品牌橙」时注意 CSS 过渡**：切换皮肤后 0.18~0.25s 内元素呈现「上一套 → 本套」的中间色，扫描前要等 ~700ms（本轮先误判过一轮）；同理只改 store 的 `settings.theme` 不一定切主题（watcher 只在值变化时应用），测试里要同时设 `document.documentElement.dataset.theme`。

### 11.25 皮肤扩到 15 套 + 设置面板重构（2026-09-13，同日第三批）

- **皮肤 5→15 套**：新增 樱花 🌸 / 抹茶 🍵 / 海盐 🧂 / 靛蓝 🫐 / 番茄 🍅 / 桂皮 🥮 / 豆沙 🍡 / 松露 🍄 / 紫苏 🍇 / 姜黄 🟡；门槛重排为 **0/4/8/14/20/28/37/47/58/70/83/97/112/128/145**（平滑曲线，最高 145 项在 191 项成就内可集齐）。**加一套皮肤的成本 = 两个色值**（浅色主色 + 深色主色），其余整份色板由 `buildLight/buildDark` 推导（含 `--*-rgb` 三元组、面板、文字、描边、遮罩、按钮派生色）。三套深色主色因「白字对比 <3」被自动体检抓出并调深：抹茶 `#8f9440`→`#7f9440`、海盐 `#5aa4b3`→`#4d93a2`、姜黄 `#c2a338`→`#b08f26`（对应 `system_test` C20 的 WCAG 断言）。
- **设置面板重构**（`SettingsPanel.vue`，原 15 行平铺 → 4 页签）：🎮 **游玩**（对决与补给 / 挂机 / 成长 三组）· 🎨 **画面**（主题分段按钮 / 皮肤网格 + 「已解锁 x/15 · 当前成就 N」/ 界面缩放 + 高清晰模式）· 🔊 **音频**（音效组 / 背景音乐组 / 合成说明）· 👤 **角色**（玩家名字 / 存档与导入导出入口 / 说明）。容器 `.settings-tabs` + `.settings-body`（`max-height: min(62vh,540px)` 内部滚动）+ 每组一张 `.set-group` 虚线卡。**设置项与绑定一律未改**（纯版式重构）。
- **守卫同步**：`e2e-dark` 皮肤轮改为 **15 套 × 5 页 × 2 主题**（时限 300s，实测 104507 元素）；设置弹窗**逐页签扫描**（原来只扫默认页 → 新页签会是盲区）；`e2e-audio-skin` 需先点「画面」页签、磁贴数断言改为 `SKINS.length`、成就种子提到 160。
- **「残留品牌橙」判定升级为有原则版**：只有「像经典橙族」且**无法用该皮肤自身 token 解释**的颜色才算残留——皮肤 token 的 RGB 集合由 Node 侧从 `skins.js` 导出后传入扫描器。否则番茄红（`#bf3b2b`，与品牌橙的暗色仅差 ~7/9/1）会被误报为残留。

### 11.26 厨神之路「图谱」视图（可拖动缩放的节点图，2026-09-13，同日第四批）

**动机（用户提议）**：原页面是 4 张卡片、每张 3 层 × 3 个节点的列表布局，看不出「路与路、层与层」的整体形状。改成游戏里常见的**科技树画布**：一层 `transform` 罩住整张图，拖动平移、滚轮/双指缩放、节点与连线在同一坐标系。

- **分层**：`src/game/data/daoGraph.js` 是**纯函数布局**（`daoGraphLayout()` → `nodes / links / lanes / bands / width / height / focusX`），**只读** `daoTree.js` 的 `DAO_PATHS/DAO_NODES/DAO_TIER_REQ`；`src/components/DaoTreeGraph.vue` 只负责画与交互。四路 = **四条并排路带**（各有标题 + 进度条），每路 3 层 × 3 节点；连边 = 同列为主、相邻列为副（表达「任意 2 个即可进下一层」）。**层带横向贯穿四条路**，层门槛直接写在左侧标签栏（「第 2 层 · 需本路 2 个」）——不用点开就知道门槛。
- **三处打磨（v2.1 第八轮）**：① **节点图标随半径缩放**（`--dtg-icon` = 半径×0.78 → 24.2/25/27.3px，成本越高的节点图标越大，与半径分档呼应）；② **道途标题加淡色底片胶囊**（按字数估宽，不依赖运行时文本测量 → 布局稳定可复现；底色用面板色 0.78 + 支线色 45% 描边）；③ **可解锁节点加一圈细外环**（半径 +5、支线色 55% 不透明），与「未达条件」的虚线灰环一眼区分，不再只靠呼吸光晕。
- **标题内移 + 聚焦居中 + 去掉层数文字（v2.1 第七轮，用户要求）**：
  ① **道途标题挪到中心附近**（半径 = 根半径 + 78 ≈ 118，夹在自己那条干枝与相邻干枝之间）——原来写在各扇区外缘，得把画布拖到外圈才看得见；现在一屏聚焦中心就能同时读到四条路的标题与 `n/9` 进度。半径取 118 是算出来的：相邻扇区中心相距 ≈ 1.41×半径，要 ≥ 标题宽度（~120px）才不叠字。
  ② **🔍 聚焦 = 中心「根」居中**（原来取「中心与最忙扇区的中点」，导致根偏心）。实测偏差 `dx=0, dy=0`。
  ③ **环上不写「第 N 层」文字**（用户要求）：三圈只留导轨虚线，层门槛改由悬浮卡（「需本路 2 个」）与详情卡表达；`rings` 里随之只留 `tier/radius`。
  ④ 中心「根」也**不再写名字**（身份由页面标题与顶部牌匾表达），腾出位置给四条标题，避免与下方两条标题叠字。
  - ⚠️ **视口尺寸必须与 viewBox 同源**：原来 `vh` 量的是**外层容器**（含工具栏 ~41px），而 SVG 实际更矮 → viewBox 被拉伸、居中偏移（实测偏 56px）。现统一取 `svgH`（= SVG 的 CSS 高度 = viewBox 高度），内容 1:1 不变形。
- **圆形纯图标 + 两步解锁（v2.1 第六轮，用户要求）**：
  ① **节点改回圆形、且画布上不再有任何文字**——去掉名称与 `✓/＋/✕` 徽标，节点只剩「圆形徽章 + 居中图标」；名称、效果、成本、门槛全部收到**悬浮提示卡**与**点击后的详情卡**里。画布因此干净很多，密度也更低（节点半径仍按成本分档 29/32/35）。
     状态只靠**环的形态**表达：已解锁 = 实心主色 + 白内环；可解锁 = 支线色实线环 + 呼吸光晕；未达条件 = 灰盘 + 虚线灰环 + 降透明度。（`daoGraph.js` 里随之删掉 `labelGap`/`diamond` 与 `label*`/`side` 字段——`system_test` C21 有一条**「画布节点是纯图标」契约**守着：组件里出现 `dtg-name`/`dtg-badge` 或节点带标签字段即 FAIL。）
  ② **解锁改成两步、且失败要有反馈**：第一次点击节点**只打开详情卡**（实测解锁数不变），必须再点卡里的**「确认解锁（N 枚）」**才真正解锁；**按钮不置灰**——条件不够（印记不足 / 未达层门槛）时**允许点**，点了在卡内给出红色「解锁失败：<原因>」+ 错误音 + 系统日志，避免「对着灰按钮猜原因」。成功则显示「已解锁「XX」」+ 奖励音。
- **改成辐射式（v2.1 第五轮，用户指出「科技树应该是中心点开始向四周发散」）**：布局从「四条并排路带 × 三层横排」改为**极坐标**——中心是「根」，**四条道途 = 四个扇区**（中心角 −135°/−45°/45°/135°，跨度 68°），**层数 = 三圈同心环**（半径 206 / 332 / 458），节点由内向外发散。术语随之改为 `sectors`（扇区，含楔形路径 `d` 与 `titleAt`）/ `rings`（环，含半径与标签位）。
  - **不再追求一屏看完**（用户明确说不需要）：一屏只看到内两圈是**预期行为**，靠拖动/滚轮探索；🔍 聚焦 = 以 90% 可读比例居中到「中心与进度最集中扇区的中间」，⤢ 全览 = 47% 收全树。
  - **标签沿半径旋转**（`labelDeg` = 角度，左半圈翻转 180° 且右对齐）——水平文字在环形排布下会**切向相撞**，改成沿半径摆放后相邻节点标签互相平行，这是辐射树的经典画法。
  - 环标签（「第 1 层 / 第 2 层 · 需本路 2 个 / 第 3 层 · 需本路 5 个」）放在**扇区之间的空隙**（0° 轴），既不会压节点也不压中心；扇区标题 + `n/9` 进度写在各扇区外缘。
- **参照《龟龟潜海记 Shelldiver》重做视觉（v2.1 第四轮，用户指定参考）**：用户给了本机安装的游戏路径，我读了它的**本地化文件**（`localizations/chinesecn_zh_language_file.json`：368 条里 256 条是技能节点文案，可见其树 ≈128 节点、按「氧气/泡泡枪/等离子/商店/转化器/储藏/资源/生物价值/机器人/海星」等支线组织，顶阶节点名带 `[rainbow]` 标记）与它的**官方技能树截图**（GameMaker 打包的 `data.win` 贴图是压缩的、抠不出原图，故用官方商店截图学视觉语言）。照搬的五个特征：
  ① **菱形节点 + 支线色粗描边**（旋转 45° 圆角方，边长 = 半径×1.415 → 外接尺寸与原圆一致，布局不动）；
  ② **连线与节点同支线色**（既有的语义 token：采撷 `--good-strong` / 火工 `--warn-strong` / 厨武 `--bad-strong` / 经营 `--gold`），**已解锁段实线、通往未解锁段虚线、全未解锁极淡**；
  ③ **状态徽标**：`✓` 已解锁 / `＋` 可解锁 / `✕` 未达条件（成本不再挤在节点上，改到提示卡与详情卡的**成本药丸**里，与参考一致）；
  ④ **深底图标水印**（`<pattern>` 铺四路图标，6% 不透明度）+ 顶部**进度牌匾**（「🛤️ 轮回天赋树 · 5/36 节点」+ 主色进度条）；
  ⑤ **金边提示卡**（`2px var(--border)`）+ 右下角 **4 条操作提示胶囊**（拖动/滚轮/点击/悬停），取代原来的一长句说明。
  皮肤仍然全量生效：已解锁节点实心用 `--primary`（换水墨即变蓝灰），**支线色是身份色、不随皮肤**（与参考一致）。
- **有机感（v2.1 第三轮：用户反馈「节点太规整」）**：① **半径按成本分档**（1/2/3 枚 → r 29/32/35，越关键越大，同天赋树的 notable 画法）；② 节点位置带**确定性抖动**（`rand01(id)` 派生，±4.5px/±3px，不引入随机 → 截图与守卫都可复现）与**行内微弧**（中间列略高 5px）；③ 连线是**斜向贴圆边 + S 形控制点**（不再是上下直线）；④ 左侧加**装饰性根节点**「🛤️ 厨神之路」+ **四条干枝**（各自接进路带第一层行的左缘，只表达「四路同源」，**不参与状态与点击**）；⑤ 每条路一条**淡色路面**（路别色 7% 透明度纵带）+ 路带边框改成虚线淡描边，弱化「表格感」。
- **焦点框（用户反馈「选中后是方形框」）**：Chromium 会给可聚焦的 SVG 元素画 `outline: auto 5px` 的**矩形默认焦点框** —— 鼠标点节点就会冒出一个方框。现 `.dtg-node:focus, :focus-visible { outline: none }` 一律屏蔽，键盘导航改用**自绘的主色环 + 光晕**（与项目里 `button:focus:not(:focus-visible)` 的做法一致）。
- **视觉（v2.1 第二轮重做，参照主流天赋树/PoE 规划器画法）**：节点是**圆形徽章**（圆盘 + 状态环 + 居中标 emoji + **下方名称** + 右下**成本徽标**），状态靠**环的形态**区分——已解锁＝实心主色 + 白色内环 + 实线连线；可解锁＝淡彩底 + 实线主色环 + 呼吸光晕（alpha ≤0.16）；未达条件＝灰盘 + 虚线灰环 + 降透明度。连线是**两层轨道**（粗底轨 + 细芯线），「已解锁→已解锁」为实线亮色、「已解锁→可解锁」为亮色虚线。**路别强调色**用既有语义 token（采撷 `--good-strong` / 火工 `--warn-strong` / 厨武 `--bad-strong` / 经营 `--gold`），只用在路标题与进度条上，不与皮肤主色打架。底纹是**细点阵**（`<pattern>`，低对比），另加**状态图例**（已解锁 / 可解锁 / 未达条件）。
- **交互**：`pointer` 事件做平移（鼠标与触摸同一套，触摸拖动靠浏览器隐式捕获）、`wheel` 以**光标为锚点**缩放（0.4~2.4，即 40%~240%）、双指捏合缩放；**拖动超过 4px 就不再触发节点点击**。视图按钮：**🔍 聚焦**（默认，95% 可读比例，居中到「已解锁×2 + 可解锁」得分最高的那条路——36 个节点全塞进一屏只有 ~60%，字会看不清，主流天赋树也都是「先看局部」）/ **⤢ 全览**（整张图收进视野，实测 59%）。**悬停**节点会亮起它的环、同时高亮与它相连的连线、其余连线淡出，并在指针旁弹**提示卡**（名称 / 路·层 / 成本 / 效果）；**点击**在下方展开**详情卡**（含层门槛与解锁按钮，未达条件时显示原因）。画布高度按内容宽高比自适应，不留大片死白。
- **玩法零改动**：解锁仍走 `player.daoUnlock`，详情卡只是展示 + 调用它；**不新增存档字段**，视角（k/tx/ty）只在内存里。原列表布局作为「📋 列表」页签**原样保留**（层门槛、成本、进度等文字信息在画布上不好读全，两者互补）。
- **颜色全走 token**：节点/连线/簇框的颜色写在组件 `<style>` 里用 `fill: var(--primary)` 等，换肤即时联动（实测原味 → 水墨：连线 `rgb(224,112,74)` → `rgb(125,146,172)`、已解锁节点底同步变化）。
- **两个实测坑**（都写进 AGENTS.md 了）：① SVG 的 `fill="var(--x)"` **属性写法不解析 `var()`**，必须写在 `<style>` 里；② `rgba(var(--lock-bg), .5)` 这类**把十六进制变量当三元组**的写法非法 → 声明被丢弃、**回退黑色**（当时把「锁定」节点画成了黑块，截图抓到）。
- **守卫**：`system_test` **C21 十八项**布局断言（节点数与数据一致 / 每路 9 个每层 3 个 / 坐标有限不重叠 / 同路 y 随层递增 / 连线端点存在且无自环 / 画布容纳全部节点与路带 / 四条路带并排不重叠 / 三条层带与节点行对齐 / 状态字段齐备 / **根节点与四条干枝齐备且干枝终点落在路带内** / **半径随成本分档递增** / 根节点不与层栏标签区重叠 / **顶部牌匾横跨路带** / **菱形边长 = 半径×系数** / 连线均标注所属道途 / **层级越外圈半径越大（含抖动不倒挂）** / **每个节点落在自己扇区角度内** / **四扇区角度互不重叠** / **根在画布中心且干枝连到第 1 层** / **三条环半径递增且都在根之外** / **扇区标题夹在根与第 1 环之间（聚焦中心即可见）** / **标签沿半径朝外（有旋转角与左右对齐）**）；`e2e-dark` 扫描器**纳入 SVG 的 `fill`/`stroke`**（品牌橙判定与对比度，SVG `<text>` 前景取 `fill`）并**豁免纯 emoji 文本**（否则节点图标恒误报）；`e2e-text` 的点开页内 `.*-tabs`、`e2e-test` 的 390px 全页无溢出守卫都天然覆盖新视图（SVG 放在固定尺寸 + `overflow: hidden` 容器内）。
- **实测**：默认聚焦 95%（1440×900，画布高按内容自适应 322px）/ 76%（390×844，无横向溢出）；全览 59% 一屏看全 36 节点；拖动改 `translate`、滚轮以光标为锚点缩放；悬停弹提示卡并高亮相关连线；点节点弹出「夜之灯 · 第 2 层 · 成本 2 枚 · 可解锁（当前印记 5）」；8 已解锁 / 13 可解锁 / 15 未达条件分类正确；换肤（原味→水墨）后节点/连线/环全部跟随；控制台零错误；`e2e-dark` 扫描量升到 **106497 元素**（新增的 SVG 图元都纳入体检）。

## 11.27 山海食经（v2.1 新增：整屏画布的收集科技树，2026-09-13）

**动机（用户提出）**：想要一个「整个页面就是画布、里面是庞大科技树」的新功能页；方向定为**收集驱动**（把「图鉴收集」这条本作最核心的收集线接上一个新产出），奖励**只用固定数值**，以免碰已标定的百分比乘区。

- **规模与形态**：10 条收集线 × 6 环 × 3 节点 = **180 节点**，辐射式（中心「根」+ 10 扇区 + 6 圈同心环）。布局 `game/data/shanhaiGraph.js`（纯函数），画布复用 `components/DaoTreeGraph.vue`（v2.1 给它加了 `fill` 模式：实测高度 + 浮动工具栏）。
  - **角度预算**：10 扇区 = 每系 36°；环内 2 段 + 扇区间隙 1 段等宽 12°（环内弦距 = 2R·sin6° 需 ≥ 72px）→ 第 1 环半径 420 起步（320 时实测 60 对重叠），6 环 [420, 540, 668, 804, 948, 1100]，画布 ~2460×2620，**一屏看不完是设计意图**（默认聚焦中心 90%，全览 ~29%）。
- **条件口径**：该线**已收集件数**（`player.collected`）+ 该技能**等级**；明确不用 `actionsDone`（不持久化）与 `collectionPct`（2210 分母被稀释）。阈值**按各线物品总数取比例**（6% / 15% / 30% / 50% / 70% / 90%，第 4~6 环叠技能 20/45/75 级），由**生成器** `scripts/gen/gen_shanhai_tree.mjs` 从真实技能实例算出（渔获 72 件 → 5/11/22/36/51/65；锻造 365 件 → 22/55/110/183/256/329），已登记 `gen_drift_audit`（产物逐字一致）。
- **纯条件点亮（不消耗资源）**：`player.shanhaiUnlock(id)` 只校验条件；**不新增货币**。唯一新存档字段 `shanhaiUnlocked: string[]`（懒建、旧档回退空数组）+ `stats.shanhaiUnlockedTotal`（成就/统计用）。两步点亮：点节点看条件 → 「确认点亮」；条件不足**不置灰按钮**，点了给红色原因（沿用厨神之路第六轮的口径）。
- **奖励（flat，零百分比）**：背包 +60 格、仓库 +130 格、冷库 +10 格、离线收益时长上限 +5 小时、四条采集线（采摘/垂钓/狩猎/挖掘）与农耕各「每次动作 +1 件」。容量类**点亮时一次性发放**，走 `player._grantShanhaiCaps()` 原语（背包→仓库→冷库逐级找位；该原语返回各档**实际落地量**，而 `expandInventory/expandBank` 只回布尔、`expandColdStorage` 还把 1000 金币成本与 +1 绑在一起，都不能直接复用）；离线上限走 `player.offlineMaxHours()`（**唯一出口**，各段自带天花板）；采集 flat 加在 `GatheringSkill.yieldBatch()`（`yieldQuantity`/`expectedYield` 同源 → 在线/离线一致），农耕加在 `FarmingSkill.harvest`。**全树总量受守卫上限约束**（C22）。
- **浮层布局（实测修过重叠）**：工具栏左上、总进度 HUD 右上、相关页面**左下**、节点详情（未选中时是一句提示）右下；`z-index ≥ 4`。首版把相关页面也放在左上 → **与工具栏重叠**（用户截图指出），已挪到左下并在 ≤720px 隐藏（内容审计只做静态检查，不受影响）。相邻浮层两两不重叠有守卫（临时探针：实测 4 个浮层 0 重叠）。
- **道途标题位置（用户两次要求）**：放在**中心附近、各自那条干枝上**（半径 215 = 夹在根 r44 与第 1 环 r420 之间）——10 扇区相邻弦距 ≈ 0.62r，标题底片宽约 110px → r ≥ 178，取 215 留余量（实测相邻中心距 133px、零重叠）。**别再放回第 1/2 环之间（r=480）**，那样要拖到外面才看得见；C22 有守卫拦。
- **整屏画布**：`.main-scroll` 在 `activeView === 'shanhai'` 时加 `.main-scroll--bleed`（flex 列 / padding 0 / overflow hidden），视图根 `flex:1; min-height:0` —— 用 flex 而非 `height:100%`，因为 `<NewbieGuide/>` 是滚动区里的流式兄弟（会把它顶出并被裁掉）。工具栏（左上）、总进度 HUD（右上）、节点详情（右下）、相关页面（左下）全部**浮在画布上**，`z-index ≥ 4`（金色描边环是 3）。
- **⚠️ 实测踩到的坑**：画布组件读的状态字段是 `owned`/`can`（厨神之路的命名），而山海食经的派生状态里叫 `unlocked` → 首版**已点亮节点被画成「未达标」**（页面不报错、只是全灰）。修法：视图映射 `owned: st.unlocked === true`，组件 `nodeClass` 兼容两种字段名。
- **旧档对账补发（同一轮修复，用户问「旧档已经点了但没加怎么办」）**：容量奖励由「一次性发放」升级为**记账 + 读档对账** —— 点亮时把「已点亮节点应发的容量总量」写进 `stats.shanhaiCapGranted`；`applySave` 末尾调 `settleShanhaiCaps()` 比较应发与账本的差额并补发（同一套顺位规则），补完即更新账本 → **幂等**（重复读档不会重复发）。因此**旧档一读档就自动补齐**：实测「2 个背包节点已点亮、无账本、上限 20」→ 读档后背包 22 ✓；买满 100 的旧档 → 3 格顺位转投仓库 100→103 ✓；二次读档数值不变 ✓。这也是 C15（离线幂等）那条教训的同款纪律：**凡是「一次性发放」都配账本 + 对账**。
- **容量奖励满上限不蒸发（用户实测报「点了加背包的但上限没变」后修）**：背包硬上限 100，若玩家已在杂货铺买满，则树上 +1 会被原实现**静默吞掉**（节点照样点亮、背包纹丝不动、也没有提示）。现改为**顺位转投**：背包满 → 转仓库、仓库满 → 转冷库、三档都满才如实告知「未生效」，并把**实际到账**写进点亮反馈（`shanhaiUnlock` 返回 `landed`）。C22 有守卫。
- **守卫与同步**：`system_test` **C22 十七项**（结构 / 条件口径 / 奖励白名单 / **全树总量上限** / 可行性 / 新档 0 可点亮 / 收集够即点亮且**不扣金币**、容量即时到账 / 满上限顺位转投 / 条件不足给原因 / 旧档对账补发 / 对账幂等 / 存档往返 / 画布几何零重叠）；四处登记（VIEW_KEYS / App 分派 / 左栏磁贴「山海食经」📖 / e2e-dark VIEWS）+ 攻略总览条目与关键词 + **成就 +4（195 项 / 96 称号）** + StatsView 收集栏一行；生成器登记进漂移审计。

## 11.28 「所有加上限的以及被加上限的」全面审计（2026-09-13，v2.1）

**触发**：用户报「点了两次加背包格子的，但打开背包看并没有增加上限」，追问「旧档已经点了但没加怎么办」，最后要求「所有加上限的以及被加上限的都要全面检查有没有问题」。审计口径 = 把**每一个会提高上限的入口**与**每一个会被提高的上限**及其**全部消费方**（判定、显示、读档、离线、UI 文案）过一遍。

**修掉的 8 类问题**（按严重度）：

| # | 问题 | 事实 | 修法 |
| --- | --- | --- | --- |
| 1 | **读档夹取遇到非法值 → 上限彻底失效** | `Math.min(saved.x ?? 默认, MAX)` 若存档里是字符串 → `NaN` → 之后 `used >= NaN` 恒假 | 新增 `caps.safeCap/safeList`，全部读档点改用它（非有限数回退默认、负数归 0、超限夹顶） |
| 2 | **离线窗口两套算法、UI 文案全错** | `bootstrap` 自己写「12h + 饼干」，而玩家实到 **35h**（还有厨神之路 +6h、山海食经 +5h）；三处 UI 却写「12h + 饼干」 | `player.offlineMaxHours()` 成唯一出口（基础 12 + 饼干 ≤12 + 道途 ≤6 + 食经 ≤6），`bootstrap` 只调它；UI 文案改为按 `OFFLINE_CAP` 生成 |
| 3 | **容量奖励被上限静默吞掉** | 见 §11.27 末段（顺位转投 + `landed` 回执 + 记账对账） | 同上 |
| 4 | **冷库满静默失败** | 单件冻存调用方不看返回值 → 玩家以为存进去了 | 满容发 `cold:full` 事件 + 日志，返回 `false` |
| 5 | **背包满 + 信箱也满时无提示** | `gainItem` 转投信箱失败后只发 `inventory:full`，堆叠截断路径不提示 | 补通知；并明确「判断发不出去一律用 `canGainItem` 预检，不看 `gainItem` 返回值」 |
| 6 | **游戏商店购买「未入库」不退币** | 买了加容量的商品但物品没进包，游戏币照扣 | 失败即退款（游戏币） |
| 7 | **17+ 处上限字面量各写一遍** | 商店 desc、读档、发放、UI 文案各一份，改一处必漏别处 | 新建 `game/data/caps.js` 单一来源（CAP_MAX=硬顶 / PAID_CAP_MAX=金币路径上限 / CAP_BASE / COLD_EXPAND_COST / OFFLINE_CAP / DERIVED_MAX），消费方全部引用（硬顶与金币上限的分工见 §11.29） |
| 8 | **读档数组不截断 + 死代码** | `plots/spirits.active/menu/mail.list` 超长照收；`expandColdStorageFree` 是空槽（从未被调用） | 读档统一 `safeList` 截断；删除死代码 |

**核实为「不是缺陷」的两项**（记录以免下次重复排查）：① 仓库**没有堆叠上限**（`bankCap` 只限格数），故游戏商店直接写 `bankCap` 不是 bug；② 厨神之路的离线上限 `+6h` 与 `OFFLINE_CAP.daoMaxHours` 一致（dao 数据是铁律冻结层，未动，只在 `offlineMaxHours()` 里加了防御性夹取）。

**新增守卫 C23（10 项，已反向验证）**：常量单一来源 / 满上限拒绝且不越界 / 读档非法值不产生 NaN / 派生上限数组读档截断（出战位、菜单格、农田、并行数）/ `offlineMaxHours()` = 基础 + 三段各自夹取且幂等 / `bootstrap` 只有一处算法（静态扫源码：必须调 `offlineMaxHours()` 且不得出现自算 `12 * 3600_000`）/ `safeCap` 拒绝 NaN 与字符串 / 冷库满发 `cold:full` / `player.js` 无裸容量字面量 / 冷库扩容价格共用常量。
**反向验证**（必须做）：把 `safeCap` 改回 `return n` 并把 `bootstrap` 改回自算 `12h` → C23 立刻报 **`实际 116h`** 等 4 项 FAIL；恢复后 760/0 全绿。

**纪律（新增上限时必须照做）**：常量进 `caps.js` → 读档用 `safeCap/safeList`（**绝不写裸 `Math.min(saved.x ?? d, MAX)`**）→ 消费方（含 UI 文案）引用常量 → 上限类失败必须有反馈（事件/日志/退款）→ 跑 C23。


## 11.29 「背包节点加到仓库」错位修复（2026-09-13，v2.1 当天第二次实测）

**用户报告**：「现在我背包叫厨藏，仓库还是叫仓库，但是科技树加背包格子加的是仓库的，是不是加厨藏和仓库的都出现了错位」。

**复现（真实 UI，Playwright 点进去看）**：厨藏已买满 100（杂货铺金币上限）时，点「采撷·初识录」（面板写 `🎁 背包格数 +1`）→ 点亮回执变成「仓库 +1 格（背包已满，顺位转投）」、厨藏仍是 100/100、仓库变 101。**不是字段错位**（`shanhaiTree` 节点 → `shanhaiEffectSum` → `_grantShanhaiCaps` 三处映射逐一核对无误），而是**硬顶没跟着新来源抬**：背包硬顶就是 100，而商店恰好也能买到 100 → 对「已买满商店」的玩家，**每一个**背包节点都只能顺位进仓库。

**修法（一行数字 + 一条纪律）**：
- 新增 `PAID_CAP_MAX = { inventory: 100, bank: 500, cold: 100 }`（**金币购买路径**的天花板，与今天完全一致，商店/冷库的价格节奏一行未改）；`CAP_MAX` 抬成**硬顶** = 初始 + 金币满 + 全树满 → **背包 160 / 仓库 630 / 冷库 110**（仓库也要抬：500 + 130 = 630，否则后 30 个仓库节点会继续往下顺位）。
- `expandInventory/expandBank/expandColdStorage` 停在 `PAID_CAP_MAX`；只有山海食经的 `_grantShanhaiCaps` 用 `CAP_MAX`。读档夹取（`safeCap`）用 `CAP_MAX`（合法存档可以超过商店上限）。
- UI 三处文案同步：商店卡片显示 `当前/金币上限` 并在超过时标「（商店已满）」、商品描述写明「金币可买到 100 格；山海食经还能再往上加」；冷库按钮同样停在金币上限；背包弹窗底部把两条来源都写出来（商店 20→100、100→500；山海食经再往上到背包 160 / 仓库 630 / 冷库 110）。

**守卫（都在 `system_test`）**：
- C23 新增 3 项：**硬顶 ≥ 金币上限 + 山海食经全树容量**（`需 背包100+60=160 / 仓库500+130=630 / 冷库100+10=110`，失败时直接把这个算式打出来）、金币路径天花板严格低于硬顶、金币扩容仍停在商店上限（95→100 而不是 105）。
- C22 新增 1 项：**商店买满后背包节点仍落在背包**（不再错位进仓库），并把原来两处「买满 = 100」的断言改成真硬顶 `CAP_MAX.inventory`（100 现在只是商店上限）。
- **反向验证**：把 `CAP_MAX` 改回 100/500/100 → 立刻 4 项 FAIL（含错位那条与不变量算式）；恢复后 765/0。
- ⚠️ **账本记的是「应发」不是「到账」，这是刻意的**：中途试过记「实际到账」（想让装不下的奖励下次读档自动补），结果那个装不下的奖励**每次读档都重试并顺位砸进下一档** → 仓库随读档次数增长，被 C22 的幂等断言当场抓到。记「应发」= 幂等（重复读档不重复发），代价是三档同时满时被吞的奖励不再补发——而硬顶不变量保证正常玩法下不会出现「装不下」。
- **旧档说明**：此前「商店买满时点亮的背包节点」已经把容量给了仓库/冷库（价值没丢，只是记在了另一档），账本记的是应发、不会重复补，故不回滚也不重发；修好之后**新点亮**的节点一律落在标签写的那一档。

## 11.30 山海食经扩到「大后期四档」（2026-09-13，v2.1 同日第三批）

**用户提出**：「我觉得山海食经还能加，加到每个技能 100 级的时候、转生 1 次的时候、转生 5 次的时候、转生 10 次的时候」。

**内容**：在原 6 环之上新增 **4 环 × 3 节点 = +120 节点**（后随密度调整为 **400**），接上游戏既有的两条大后期轴：

| 环 | 名称 | 门槛（该线） | 每线 3 节点奖励 |
| --- | --- | --- | --- |
| 7 | 圆满 | 技能 **100 级**（收集沿用 90%） | 背包 +2 / **每次动作 +1 件**（采集线；制作线改仓库 +4）/ 冷库 +1 |
| 8 | 轮回 | **转生 ≥ 1** | 背包 +2 / 仓库 +4 / 冷库 +1 |
| 9 | 历劫 | **转生 ≥ 5** | 背包 +3 / 仓库 +6 / 冷库 +1 |
| 10 | 悟道 | **转生 ≥ 10** | 背包 +3 / 仓库 +8 / **离线上限 +1h**（仅采撷线终点）/ 冷库 +2 |

- ⚠️ **转生环只写 `prestige`、不写 `level`**：`prestigeSkill()` 把等级重置为 `1 + 传承（≤20）`，若里程碑环同时要求「当前 100 级」就**永远点不亮**（C22 有专项断言拦这类写不通的条件）。语义上「圆满→轮回」的顺序恰好是自然的：先在满级时点亮第 7 环，再转生去够第 8~10 环；转生后练回 100 级（上限 120）也能补点第 7 环，已点亮的不会掉。
- **奖励仍只用固定数值**，并把两个剩余设计位**用到恰好封顶**：① 每条采集线的「每次动作 +1 件」补到**每技能 2 件**（`SHANHAI_EFFECT_CAPS.flatYieldPerSkill`）；② 全树离线上限补到**恰好 6 小时**（`OFFLINE_CAP.shanhaiMaxHours`）——**最后 1 小时放在采撷线的终点节点**（`pick103`），其余线该槽位发冷库（离线上限全树只有 6 个名额，前 6 环已用掉 5 个）。C23 新增「**恰好封顶**」断言：少了是浪费设计位，多了会被逻辑夹掉＝发了个读不到的奖励。
- ⚠️ **`flatYield` 只有采集类读得到**（`GatheringSkill.yieldBatch` / `FarmingSkill.harvest`）→ 制作线（烹煮/烘焙/酿造/调味/锻造）该槽位一律改发大额仓库（第 6 环本来就是这么处理的，本轮沿用）。
- **硬顶按不变量抬高**（§11.29 建立的纪律）：全树总量（见 §11.30.1 的最终值 背包 +230 / 仓库 +390 / 冷库 +58）→ `CAP_MAX` = **背包 330 / 仓库 890 / 冷库 158**（= `PAID_CAP_MAX` 100/500/100 + 全树），C23 的不变量断言自动复查。
- **画布与首屏**（节点数后由 §11.30.1 调到 400）：环半径 6 → 10 圈（`[420 … 1788]`，画布 ~3800px，环间步长 ≥120 保证不重叠；C22 的零重叠断言逐对复查 300 个节点）。**新增 `focusScale` prop**：原先「聚焦」宽屏固定 0.9，在 10 环下首屏只能看见最内一两环 → 山海食经改传 **0.55**（窄屏仍 0.62 保证可点），实测首屏能看到中心 + 前 4~5 环的完整「巨树」感；`全览`（28%）看全貌。
- **文案/内容同步**：成就新增一档「六合贯通」（180 节点），全篇阈值 180 → **400**（成就 **196** / 称号 **97**）；统计页分母改为动态（`player.shanhaiStates().length`）；攻略总览条目改写（10 环、里程碑、离线恰好 6h、每技能封顶 2）；节点详情面板新增**转生次数**条件行（含「转生要求先满 100 级，故里程碑只看转生次数」的解释），并修掉条件行变多后「确认点亮」被挤成两行的问题（按钮 `nowrap` + 原因行 `flex:1; min-width:0`）。
- **守卫**：C22 扩到 **二十项**（含「转生环不得写等级」「里程碑严格递增 100→1→5→10」「前 6 环严格递增、第 6~10 环持平」「画布 300 节点/10 扇区/10 环零重叠」）；C23 扩到 **十五项**（含「全树离线小时与每技能 +1 件恰好封顶」）。**实测**：`system_test` 768/0 · 11 套 CI 全绿 · 15 项 e2e 全过（深色体检 121356 元素）· `gen_drift_audit` 重跑一致。

### 11.30.1 密度与画布性能（同轮，用户第二条反馈）

用户第二条反馈：「从第六圈开始就显得有点空了，从第六圈开始应该有五个节点可以点」+「页面优化好像有点问题，应该是 emoji 太多的原因」。

- **密度**：节点排布改为**按环给槽位数** —— 前 5 环每环 3 个（收集曲线，少而明确）、**第 6~10 环每环 5 个**（含大后期四档）→ 全树 **300 → 400 节点**；后缀扩到 `录/谱/典/章/卷`，槽位数由生成器的 `RING_SLOTS` 导出为 `SHANHAI_RING_SLOTS`（画布与守卫都读它，别再写死 3）。角度预算复核：24° 扇区内铺 5 个 → 步长 6°，第 6 环弦距 = 2·1100·sin3° ≈ 115px > 节点直径 ~62px（C22 的零重叠断言逐对复查 400 个节点 ✓）。奖励表随之改为「环 → 5 槽位」的函数表（`RING6_PLUS`），容量按深度递增；全树总量变为 背包 +230 / 仓库 +390 / 冷库 +58 / 离线 6h / 每技能 +1 件 ×2，硬顶按不变量抬到 **330 / 890 / 158**。
- **性能：先量化、再消融，最后靠「换图标」根治（2026-09-13 用户第二条反馈）**：
  - 用户原话：「页面优化好像有点问题，应该是 emoji 太多的原因」+「emoji 闪来闪去很影响观感」+「emoji 也不好看，不如换成对应技能物品图片里选」。
  - 量测口径：先量「元素数 / 静止 / 拖动 / 缩放」的 rAF 帧时长，再**逐项消融**（隐藏 emoji / 停动画 / 两者都停 / 只剩圆盘）。
  - 结论（emoji 版本、300 节点 / 90 个可点亮节点）：贵的是**彩色 emoji 的字形光栅化 + 上百个 opacity 呼吸动画**；两者都关掉时拖动 p95 从 50~66ms 回到 16.7ms（只做其中一项 p95 仍 33ms）。
  - **第一版方案（已废弃）**：交互期冻结（拖动时隐藏 emoji 与底纹、停动画，松手恢复）。用户随即反馈**闪烁难看**——隐藏/恢复 400 张图标本身就是可见的视觉抖动。
  - **最终方案：把图标从 emoji 换成物品图**（一举解决三件事）：① 生成器给每个节点算 `iconItem`（该线该深度分位处的物品）→ 画布用 `public/images/items` 的 PNG 渲染，**越深的环展示越珍稀的物品图**；② 画面里不再有 emoji 字形 → 没有字形光栅化成本，也**不需要冻结**；③ 顺带符合用户审美（食材图鉴气质）。
  - **实测（同机、121 个可点亮节点 = 121 个呼吸动画）**：**无冻结 p95 16.8ms（最优）< 冻结全开 p95 33.3ms** —— 冻结在新图标下**反而更慢**，故**整层删除**（连底纹隐藏一起）。同时把图标占比 0.78→0.85、"未达标"不透明度 0.62→0.72（物品图偏浅色，压太暗看不清）。
  - ⚠️ **两个测量陷阱（都踩过）**：① vite dev server 的 HMR 重编译会让基线假性变慢（据此一度得出「冻结提速 2 倍」的错误结论，复测差异消失）；② CDP 逐次 `mouse.move` 的往返开销会掩盖真实渲染成本，量纯渲染成本需在页面内合成 `PointerEvent` 驱动（并桩掉 `setPointerCapture`）。
  - 守卫：C22 新增「每个节点都有 `iconItem` 且图片文件真实存在（400/400）」；性能不进 CI（CI 无真实 GPU），两条规则（图标走物品图、别再复活冻结）写进 AGENTS 防回归。

## 11.31 「汇金」链（分支之间的金币节点）+ 提示卡错位修复（2026-09-13，v2.1 同日第四批）

**用户提出**：「各分支之间从第六圈开始加奖励金币的节点」+「右边字多了之后左边会错位」。

- **汇金链**：相邻两条收集线之间原本留着 12° 空隙（分支节点只铺在扇区中心 ±12° 内），从第 6 环起每环在空隙里放一个**金币节点** → **10 空隙 × 5 环 = 50 个**（全树 400 → **450** 节点），沿空隙射线（两扇区中心角 +18° 的中线）由内向外串成一条链，金色支线色（`pathId: 'gold'` → `lane-gold` = `var(--gold)`）。
  - ⚠️ **第一版做错了**：我按「环内 5 个节点的正中间那个改成金币」实现，用户随即纠正「我说的是各分支**之间**」。教训：把「中间」这类方位词落到坐标之前先确认参照物（环内 vs 分支之间）。
  - **成对条件**（「位于两条线之间」的语义必然）：`req.skill` + `req.skill2` → 收集取**两线合计**（门槛 = 两条线各自 90% 门槛之和），等级/转生取**两线较低者**（两条线都得达标）。派生在 `shanhaiProgress`（`node.gap != null` 分支），空隙定义 `SHANHAI_GAPS` 由生成器产出。
  - **数额**（依据既有经济标定：分店 50k→6M、名厨挑战单场 ≈12k、成就头部 10k~260k）：**6k / 18k / 48k / 120k / 300k**（按环），全树合计 **4.92M** ≈ 一座城南分店(400k) 的 12 倍 —— 对「10 条线全部转生 10 次」这种毕业级投入是合理回报。
  - **发放与账本**：点亮即 `gainGold` 到账并累加 `stats.shanhaiGoldPaid`（记应发总额）；读档 `settleShanhaiGold()` 按差额补发 → **幂等**；顺带充当**旧档迁移**（老档无该字段 → 读档一次性补齐）。金币无上限，所以不像容量那样会被顶掉。C22 新增 4 项（链的结构与位置 / 数额递增与总量 / 成对条件「只满足一条线不给点亮」/ 到账与补发幂等 + **存档往返不重复发**）。
  - **HUD**：默认视角（55%）下汇金链在屏幕外，故 HUD 补一行「🪙 汇金 x/50 · 相邻两线之间、第 6 环起 → 金币」，免得玩家以为没做。
- **提示卡错位（用户截图）**：`.dtg-tipcard-foot` 是「`🪙 25` 成本药丸 + 原因文字」，原因折行时 ① `align-items:center` 把药丸挤到两行中间、② 药丸被挤窄到「图标 / 数字」上下折行（截图里那个竖排小圆牌）。修法（三件套）：行容器 `align-items: flex-start`、药丸 `flex:0 0 auto + white-space:nowrap`、折行文字 `flex:1 1 auto; min-width:0`。**同一问题在道途详情底栏（`.dao-detail-foot`，按钮也会被挤）与山海食经详情底栏（`.sh-panel-foot`）都存在，三处现已统一。**
- **回归**：`system_test` **773/0**（C22 二十四项 / C23 十五项）· 11 套 CI 全绿 · 15 项 e2e（深色体检 130365 元素）· `gen_drift_audit` 一致。

## 11.32 厨神之路：删掉进度牌匾 + 外环「觅珍环」（2026-09-13，v2.1 同日第五批）

**用户提出**：「轮回天赋树那个进度条框删了，在最外围加环绕一整圈的觅珍抽卡券，每个节点100张」。

- **删牌匾**：画布顶部那条「🛤️ 轮回天赋树 x/36 节点 + 进度条」横幅是 `daoGraph` 的 `plaque`，现从布局返回值与 `DaoView` 的 prop 中一并移除（进度信息由页面头部「已投入 N / 已解锁 x/48 节点」与折叠表承担，信息没丢，只是画布不再压一块横幅）。C21 的牌匾断言改为「**已删除**」断言（防回退）。
- **外环「觅珍环」**：`daoTree.js` 新增 `DAO_OUTER` —— **12 个节点环绕一整圈**（每 30°、半径 596 = 第 3 层之外、互相连成**闭合圈**、支线色用主色 `lane-outer`），**每个奖励觅珍抽卡券 ×100**。
  - **奖励用既有字段** `player.mijian.tickets`（觅珍页顶部就显示「🎟️ 券 N 张」、小花店也卖、抽卡时优先抵扣金币）→ **不新增物品**，因此没有图鉴三查事项；发放走与山海食经金币同款的三件套：解锁即时到账 + `stats.daoTicketPaid` 记账（记应发）+ 读档 `settleDaoTickets()` 对账补发（幂等、兼作旧档迁移；`stats` 必须随存档往返，C24 有断言）。
  - **不消耗轮回印记**（cost 0），门槛 = **全树已解锁的道途节点数**（3/6/9/…/36，最深一个要求整棵树点亮）→ 是毕业级收尾奖励，也**不与既有四路争印记**（四路全满仍是 72 枚）。门槛刻意排除外环自身（`daoUnlockedTotal`）以免自引用。
  - **数值标定**：100 券 ≈ 小花店百连价 39,000 游戏币 → 12 个 = 1,200 券 ≈ 95 万金币等值的物品，与山海食经「汇金链」的 4.92M 金币相比偏小，不会冲垮经济。
  - **UI**：详情卡显示「觅珍环 · 外环（需全树已解锁 N 个道途节点）· 不消耗轮回印记」+ 一行奖励；列表视图新增「觅珍环（外环）」一节 + 折叠表新增一行；画布悬浮提示卡同步（外环节点显示「外环 / 需全树已解锁 N 个」而不是「第 4 层 / 需本路 N 个」）。
- **顺手清掉一处历史遗留**：`scripts/ci/system_test.mjs` 里 `process.exit()` **之后**还挂着一份 C10/C11 的重复副本（**从未执行**的死代码，34 行）——已删除。⚠️ 往该文件追加守卫时**必须插到汇总行之前**（追加到文件末尾会被 `process.exit` 挡掉、静默不执行）。
- **回归**：`system_test` **780/0**（C21 增至 24 项、新增 C24 四项）· 11 套 CI 全绿 · 15 项 e2e（深色 130439 元素）· `gen_drift_audit` 一致。

## 11.33 山海食经：删牌匾 + 外圈「珍券环」+ 最小缩放修正（2026-09-13，v2.1 同日第六批）

**用户提出**：「山海食经也要：轮回天赋树那个进度条框删了，在最外围加环绕一整圈的觅珍抽卡券，每个节点100张」。

- **牌匾（进度条框）全局删除**：山海食经画布上那块牌匾的标题**写死着「🛤️ 轮回天赋树」**（组件级硬编码，在食经页上就是错标题）——确实是「进度条框」。现 `shanhaiGraph` / `daoGraph` 都不再返回 `plaque`，组件里的渲染块与 `.dtg-plaque*` 样式一并删除（两处都不再需要，不留死代码）。进度由页面右上的 HUD（山海食经：含图例 + 汇金/珍券两条支线进度）与页头（厨神之路）承担。
- **外圈「珍券环」**：最外侧**环绕整棵树一整圈**的 **10 个节点**（每 36°、对准 10 条线的射线、半径 1980 = 第 10 环之外、互相连成**闭合圈**、支线色主色），每个奖励 **觅珍抽卡券 ×100** → 全树 450 → **460** 节点，券合计 **1,000 张**（≈ 小花店百连价 39,000 币 ×10 ≈ 78 万金币等值，比汇金链的 4.92M 金币小一档，不冲垮经济）。
  - **条件用新的 `kind: 'progress'`**：门槛 = **已点亮节点数**（45/90/…/450，最深一个要求把 450 个分支+汇金节点全点亮；**排除珍券环自身**以免自引用）。派生在 `shanhaiProgress` 单独分支；面板文案显示「已点亮 x/N 个」而不是「已收集 x/N 件」。
  - **发放与账本**：与厨神之路外环同款——解锁即时到账 + `stats.shanhaiTicketPaid` 记账 + `settleShanhaiTickets()` 读档对账（幂等、兼作旧档迁移）。**两个账本各自独立**（`daoTicketPaid` / `shanhaiTicketPaid`）。HUD 增一行「🎟️ 珍券 x/10 · 最外圈环绕整圈 → 每节点 100 张觅珍抽卡券」。
- ⚠️ **顺带修掉一个「看不见」的坑：最小缩放卡住了最外圈**。`DaoTreeGraph` 的 `MIN_K` 原为 **0.28**，而食经扩到 10 环 + 珍券环后画布约 **4224×4276**，在 ~980×830 视口里「全览」需要 **≈0.19** —— 于是**第 10 环与珍券环根本进不了视野**（玩家会以为没做）。现 `MIN_K = 0.16`；实测全览 18% 时 10 个珍券节点可见 9 个（第 10 个被 HUD 挡住，可拖动查看）。**以后加环/加圈必须复核**：`MIN_K ≤ min(视口宽/画布宽, 视口高/画布高)`。
- **回归**：`system_test` **783/0**（C22 新增 2 项珍券环断言、C24 新增 1 项券账本断言）· 11 套 CI 全绿 · 15 项 e2e（深色 130999 元素）· `gen_drift_audit` 一致。

## 11.34 天气运势：恶劣天气减益 + 运势缓和（2026-09-13，v2.1 同日第七批）

**用户提出**：「天气运势我觉得得调整要有恶劣天气debuff」。原天气系统 6 种天气**全是增益**（+8%~+15%），「每日变量」等于每天白送一点。

- **新增 3 种恶劣天气**（共 9 种，恶劣占 1/3 的日子；`system_test` 断言「400 天内 9 种都抽得到」）：

| 天气 | 减益（真实生效） | 补偿项（换个赛道） |
| --- | --- | --- |
| 🌪️ 台风 | 采集产量 ×0.78、餐厅收入 ×0.90 | 对决经验 ×1.18 |
| 🔥 酷暑 | 采集经验 ×0.85、制作经验 ×0.88 | 餐厅收入 ×1.15 |
| 🧊 寒潮 | 采集产量 ×0.82、采集经验 ×0.85 | 制作经验 ×1.15 |

- **减益就写倍率 < 1**：与增益走同一条乘区（`weatherBoost` 直接返回，技能侧本来就是乘法）——**一行数据搞定，不动任何技能代码**。
- **三条硬约束（都有断言）**：① 每条恶劣天气必须有「换赛道」的补偿项；② 必须有**硬乘区**减益（经验/收入），只削「额外产出几率」对早期玩家等于没影响；③ 减益 ≥ 0.70（−30% 以内）。
- **今日运势等级 = 恶劣天气的对冲**（`FORTUNE_LEVELS`：🌟大吉 ×0.5 / ✨吉 ×0.75 / ⚖️平 ×1）：公式 `1 − (1 − v) × penaltyScale`，**只缓和减益、绝不加重**（`penaltyScale ≤ 1` 有断言），增益项原样。等级与幸运食材同源、按日期确定性派生，**不加存档字段**。页面上直接显示「今日运势 · 吉 —— 恶劣天气减益打七五折（按 75% 计）」。
- ⚠️ **顺手修掉一个在线/离线不一致**：减益会把 `GatheringSkill.yieldExtraChance()` 压成负数；在线 `yieldQuantity` 在 `extraChance ≤ 0` 时返回基础产量（**不倒扣**），而离线 `expectedYield` 原先会直接相加 → 坏天气下**离线比在线还亏**。现离线也 `Math.max(0, …)`，并加了专项断言。（这类「两条路径漂移」是 C16 一直在守的老问题，天气一引入负数就又被踩到了。）
- **离线不受天气影响**（`OfflineProgress` 不读 weather）：坏天气只在在线时生效，不会在离线结算里被追罚。
- **UI**：hero 改成按 5 条赛道逐条列倍率（<1 标红、>1 标绿）+「⚠️ 恶劣天气」红标与红边 title；一周预告与「全部天气」表标记恶劣日；`StatsView`「今日天气」与「今日」页待办条同步 ⚠️。
- **回归**：`system_test` **791/0**（天气组新增 7 项：3 种恶劣/补偿项/硬减益/幅度带/全年覆盖/运势只缓和/压负同口径）· 11 套 CI 全绿 · 15 项 e2e（深色 131021 元素）· 实拍浅色/深色两版恶劣日页面确认观感。

## 11.35 文案本地化：清掉裸露的英文标识符「tier」（2026-09-13，v2.1 同日第八批）

**用户提出**：「我发现很多页面都出现 tier，没有对应中文替换吗？」。

- **排查方式**：先给 `e2e-text.spec.mjs` 加一条「英文标识符裸露」运行时规则（扫 60 页 + 6 弹窗的可见文本）→ **0 命中**，说明泄漏都在**有状态才渲染**的文案里（订单/评论家/宴会消息）。于是写了个一次性静态扫描：抽取 `src/**/*.{js,vue}` 的全部字符串字面量、筛出「含中文 + 含内部字段名」的，得到 **9 处真泄漏**（其余是 `${qty}` 这类正常插值）。
- **改了 9 处 + 1 处格式**（术语统一为「**档位**」/「**N 档以上**」）：
  | 位置 | 原文案 → 新文案 |
  | --- | --- |
  | `player.js` 美食评论家到访 | `想要 tier ≥ N 的{类别}` → `想要 N 档以上的{类别}` |
  | `player.js` 提交不符合 | `（需 tier ≥ N 的{类别}）` → `（需 N 档以上的{类别}）` |
  | `player.js` 宴会交付 | `需 tier ≥ N 的{类别}` → `需 N 档以上的{类别}` |
  | `player.js` 食客订单差料 | `还差 N 份{类别}（tier ≥ M）` → `…（需 M 档以上）` |
  | `RegularsView` 招待选菜 | `请先选一道{类别}（tier ≥ N）` → `…（需 N 档以上）` |
  | `TodayView` 评论家待办 | `要一份 tier ≥ N 的{类别}` → `要一份 N 档以上的{类别}` |
  | `guide.js` ×4（常客/评论家/宴会/限时大订单） | `类别+tier`、`tier ≥ N`、`高 tier 料理`、`tier 门槛` → `类别与档位`、`N 档以上`、`高档料理`、`档位门槛` |
  | `LogView` 转生详解 | `评论家要求的 tier 也同步降低` → `…的档位…` |
  | `michelin.js` 评级说明 | `菜单中料理的最高 tier 之和` → `最高档位之和` |
  | `itemDetail.js` 物品详情 | `['档位', 'T3']` → `['档位', '3 档']`（并 `if (tier != null)` 挡掉 `Tundefined`） |
- **新增常驻静态守卫**（`content_sync_audit.mjs`）：**两处来源都扫**（35k+ 条）——① 字符串字面量；② `.vue` **模板文本节点**（引号感知地剥标签与属性）；判定前先剔除 `${...}`（花括号配对计数，能吃嵌套模板）与 Vue `{{...}}` 插值。**反向验证**：字面量类改回 `tier ≥` → FAIL 点名 `player.js:3179`；模板类改回 `最低 tier` → FAIL 点名 `BanquetView.vue 模板:18`。
- ⚠️ **第二轮才补上「模板文本」（用户报「宴会页面还有 tier，其它功能页面你再看看」）**：第一版静态守卫只扫 **JS 字符串字面量**，于是宴会 ×6、常客 ×6、餐厅 ×3、同业榜 ×1（`最低 tier`、`tier ≥ N`、`每 tier 60 分` 这类**模板静态文本**）整批漏掉；运行时扫描也扫不到，因为它们要**有状态**（有宴会/委托/评论家到访）才渲染。**教训：加守卫时要问「这类内容还可能从哪来」——JS 字面量 / 模板文本 / 数据文件 / 富文本 v-html，四者都要覆盖。** 顺带做了一次「面向玩家文本里的全部 ASCII 词」普查（163 个不同词），确认剩下的只有 `Lv5` 这类既定语（刻意保留）、富文本里的 `class="hl"`、以及物品 id/文件名，**没有别的英文词漏在界面上**。
- ⚠️ **过程教训**：本轮用 Python heredoc 拼接含 ``/`` 的 JS 正则，Python 把它们当退格/八进制转义吃掉 → 写出一个**静默失效的假守卫**（报告「扫描 0 条」）。含转义的代码一律用 Edit/Write 工具或 Python 原始字符串 `r'''…'''` 写。已写进 AGENTS。
- **回归**：`system_test` **791/0** · 11 套 CI 全绿（含新增静态守卫）· 15 项 e2e（含运行时扫描规则）· 深色 131021 元素。

## 11.36 v2.1.1：生成器跨环境不可复现的修复（2026-09-13）

**触发**：v2.1.0 发布后 CI 红在 `gen_drift_audit`：`shanhaiTree.js` 被判定「**未登记的漂移**」，CI 侧生成器产出 2 个不同的 `iconItem`（例：`smith_寒铁_legs` ⇢ `smith_ext2_05`），而本地复跑却「一致」。

- **根因**：`gen_shanhai_tree.mjs` 给「同一等级的物品」做 tie-break 时用了 **`localeCompare`**，而它的结果**依赖运行环境的 locale/ICU**：实测 `'smith_寒铁_legs'.localeCompare('smith_ext2_05')` 在本地（zh-CN）= **−1**，按**码位**比较 = **+1** → 本地与 CI 排序不同 → 选中的图标物品不同 → 产物 209 行 `iconItem` 有差异。
- **修法**：加 `const cmpId = (a, b) => (a.id < b.id ? -1 : a.id > b.id ? 1 : 0)`（**码位比较，语言无关**），替换生成器里全部 3 处 `localeCompare`（含汇金链的空隙排序）。
- **验证**：`GEN_OUT_DIR` 指到两个临时目录，分别用 `LANG=C LC_ALL=C` 与 `LANG=zh_CN.UTF-8 LC_ALL=zh_CN.UTF-8` 各生成一次 → 两份**逐字节相同**、与仓库产物也相同；`gen_drift_audit` 从 FAIL 转为「一致 7」；`system_test` 791/0（C22 的「图标物品都存在图片文件」照旧通过）。玩法与数值零改动——只影响「节点图标挑哪个物品」。
- **纪律（已写进 AGENTS）**：生成器里**任何「同键排序」都必须用码位比较**；新增/修改生成器后，用两种 locale 各跑一次比对，作为「跨环境可复现」的自查。

## 11.37 v2.1.3：用户实测 7 项（2026-09-14）

用户一句「我发现问题」列了 7 条，其中 **4 条是同一类病：数据/代码都在，但没有任何东西读它**（不报错、不白屏、既有守卫全绿，只有玩家点进去才发现）。

### 11.37.1 `var(--accent)` 从未定义（**我在 v2.1.0 引入的回归**，最严重）
- **现象**：默认皮肤下小游戏界面按钮「文字和背景都变白了，看不清」（用户带截图）。
- **根因**：v2.1.0 token 化时，把按钮的 `linear-gradient(#e8703f,#c9542e)` 换成了 `var(--accent)` / `var(--accent-strong)`，而**这两个变量在 `main.css` 里根本没有定义**。CSS 的行为是**整条 `background` 声明被丢弃**（不是回退到某个颜色，而是**没有背景**）→ 按钮变透明；文字仍是 `#fff` → 白字压白底。
- **为什么既有守卫全都没拦住**：`minigame_ui_audit` 只要求「别写死色值、要用 token」→ `var(--accent)` 形式**完全合规**；深色体检只量对比度（前景 vs 实测背景），不检查「背景是否真的存在」；`content_sync_audit` 当时**没有任何 CSS 变量定义检查**。这是 token 化的系统性盲区。
- **修法**：`main.css` 调色板块补 `--accent: #e8703f` / `--accent-strong: #c9542e` / `--accent-rgb: 232, 112, 63`（**默认值等于原字面量**，指纹比对证明原味渲染逐值不变），皮肤侧由 `skins.js` 的 `buildLight/buildDark` 按主色推导（`shade(p, 0.12)` / `shade(p, -0.08)`），并加进 `SKIN_REQUIRED_KEYS`（否则「变量齐备」断言会漏）。
- **同一轮扫出并修掉 4 处同类**：`--amber` / `--amber-strong` / `--warn-soft` 未定义、`--tint` 回退值写错。
- 🔒 **新增守卫**：`content_sync_audit`「`var()` 引用的 CSS 变量都有定义」——扫 `src/**/*.{js,vue}`（**剥掉整行注释与行内 `/* */` 片段**，否则文档注释里当示例写的 `var(--x)` 会误报），收集 `--x:` 定义与 `setProperty('--x')` 运行时赋值，未定义即 FAIL。当前扫 70 个变量、0 缺失。

### 11.37.2 消耗品没有使用入口（增益剂 / 保鲜剂是「死物品」）
- **现象**：玩家问「经验增益剂和产量增益剂不知道在哪使用」。
- **根因**：`items.js` 里这些物品带 `use: { buffXp / buffYield / refreshSpoilMs }`，`player` 里也有 `buffs` 状态，**但全项目没有任何消费方** —— 物品只能躺在背包里。
- **修法**：`player.useConsumable(itemId)`——重复使用**取更强倍率并顺延时长**（不叠加、不覆盖成更弱）；保鲜剂刷新背包腐坏计时 + 冷库续时。`player.usableOf()` 判定可用性。UI：背包详情与物品详情弹窗加「使用 1」按钮（`ownedCount` 命名避开与 `owned` 的属性名冲突）、`StatusPanel` 加「🧪 生效中」卡片显示倍率与剩余分钟。`action_sweep` 动作数 **311 → 330**。
- **纪律**：新增任何带 `use`/`effect` 字段的物品，必须同步落一个**玩家点得到**的入口；新增存档字段必须有人读、有人显示。

### 11.37.3 奇遇 `costItem` 没有消费方 + 幽灵 id
- **现象**：奇遇里「消耗某材料」的分支**不消耗材料也能白拿奖励**。
- **根因**：`encounters.js` 的分支带 `costItem`，**没有任何组件读它**；同时 `rat` 奇遇引用了**不存在的物品 id `spice`**（应为 `peppercorn`）。
- **修法**：`EncounterModal` 加 `affordable(choice)` / `costText(choice)` → 材料不足的分支**置灰、点不动、给原因**；选中后**先扣料再发奖**。修掉幽灵 id。审计扩展：奇遇检查**同时校验 `effect.items` 与 `costItem` 的物品 id**（22 个事件全覆盖）。

### 11.37.4 奇遇 8 → 22 个（用户要求「再加十几种」）
新增 14 个（🎣河边的老渔翁 / 🍜深夜面摊 / 🐔走失的母鸡 / 🏮庙会灯谜 / 🧧街坊的红包 / 🐝后山的野蜂巢 / 📜旧书摊的菜谱 / 🌧️突来的阵雨 / 🥬菜农的推车 / 🐕门口的流浪狗 / 🕯️鬼市的夜话 / 🎪流动戏班 / 🧂盐商的私话 / ☕隔壁的茶摊），奖励**只用现存物品**（避免图鉴三查新增来源），每个都带「代价分支 / 免费分支」的选择张力。`system_test` 的数量断言同步（**21 个三选一 + 1 个二选一 = 22**，这条断言跟着数量走，改动时勿忘）。

### 11.37.5 堆肥可重复施
- **根因**：`fertilize` 只判「地块有没有肥」，没判「新肥是否更好」→ 同种肥料可反复施、白扣材料且毫无效果。
- **修法**：`FERTILIZER_RANK = { compost: 1, richCompost: 2 }`，`rank(新) <= rank(现有)` 直接拒绝（**更好的可覆盖升级**）。UI 上已施同种肥的地块不再显示该按钮。
- ⚠️ **首版我写成「施过就拒绝一切再施」→ 立刻踩坏 `system_test2` 的「施沃肥覆盖堆肥」「沃肥后枯萎概率 0%」两项**，说明**语义是「升级覆盖」不是「禁止再施」**。改施肥规则前先看这两条断言。

### 11.37.6 觅珍券为 0 时消失
- 右上的券数原先「为 0 就不渲染」→ 玩家看不到自己有券。改为**恒显示「🎟️ 券 0 张」**。判据：`0`／`false` 常是**有效信息**，该显示；`null`/`undefined` 才是「没有这一项」，该隐藏。

### 11.37.7 排版与观感两项
- **统计页**：原 `grid-template-columns: repeat(auto-fill, minmax(240px,1fr))` → 1440px 下排成 **6 列**，而分区内容按顺序灌入 → 实测前三列各 1812px、后三列只有 636px（右下大片空白 + 288px 窄列把行挤得很紧）。**修了两轮**（详见 §11.38）：先改 CSS 多列（`columns:3`），仍不齐；最终改为**显式分列 + JS 贪心装箱**（列数 3/2/1 自适应），并把 39 行的超长「生产」分区按语义拆成「生产 / 经营」两块。实测 1440px 列高 **1404 / 1184 / 1078**（差 326px，原 850px）、390px 单列无横向溢出。顶部亮点卡从「5+1 落单」改成整行均分。**判据是各列像素高度差 + 分区/行数守恒，不是目测。**
- **设置面板**：复选框/滑块/输入框/下拉四种默认控件观感不一 → 统一为一套「玻璃框」（滑块轨道与滑钮重做、复选框走主色）。改设置面板**只动版式、别改绑定**（`v-model` 与 `id` 不动），改完必跑 `e2e-audio-skin`（它要先点「画面」页签才能操作 `.skin-chip`）。

**回归**：`system_test` **795/0** · 11 套 CI 全绿（`content_sync_audit` 17 → **25** 项、`action_sweep` 311 → 330 动作）· 15 项 e2e 全绿（扫描元素 131113）· `vite build` 通过。

## 11.38 v2.1.4：统计页分列改为「显式装箱」（2026-09-14，v2.1.3 发布后同日补）

**触发**：v2.1.3 把统计页从 6 列 grid 改成 CSS 多列（`columns:3`）后，浏览器实测仍不齐——**列高 1387 / 1184 / 964**，第三列底部仍空 ~450px。

- **根因**：多列布局对 `break-inside: avoid` 的**整体卡片**只能顺序装箱（不能用最优装箱），于是「谁排在前面谁先把首列顶高」。叠加上一层原因是 **「生产」一个分区塞了 39 行 = 1812px**，单块比目标列高（≈1150px）还高，任何分列方案都配不平。
- **修法两步**：
  1. **拆分区**：39 行的「生产」按语义拆成 **「生产」15 行（采集/制作/精通/牧场/班底/产地/分店/风味/大赛）+「经营」24 行（餐厅/渠道/活动/长线进度）**，分区 6 → 7 个，行数守恒 72 行。**没有删任何一行统计项。**
  2. **改成显式分列**：`.stats-columns` 改为 CSS Grid（列数由 `--stat-cols` 变量给），列数随窗口 **3/2/1** 自适应（`resize` 监听），每个分区**依次投给当前最矮的列**（权重 = 行数 + 2 ≈ 标题与内距）。
- **实测**：1440px 列高 **1404 / 1184 / 1078**（差 326px，改前 850px）· 1000px 两列 1832 / 1973 · 390px 单列、无横向溢出 · 分区 7 / 行 72 守恒。
- ⚠️ **别再改回纯 CSS 方案**（两轮都栽在「整体卡片不能跨列」上）；判据是**各列像素高度差 + 分区/行数守恒**，不是目测。
- 玩法零改动（纯展示层；分区的成员与统计项一字未改，只是搬运与分列）。

## 11.39 v2.1.5：设置控件统一（补 v2.1.3 没修完的部分，2026-09-14）

**触发**：用户报「设置面板**其它页**的控件没有统一」——v2.1.3 的 ⑦ 只统一了「🎮 游玩」页看得见的那几个，漏了两处。

- **① 选择器写错（真因）**：v2.1.3 写的是 `.settings-body input[type="text"], input[type="number"], select`。
  **属性选择器匹配不到「没有 `type` 属性」的 input**——「👤 角色」页的改名框 `<input v-model="nameDraft" maxlength="16">` 恰好没有 type，
  于是**整条规则都没命中**，它退回浏览器默认外观（实测 h=19px / 圆角 0 / 白底 / 灰边 `rgb(118,118,118)`），夹在 30px 玻璃框与按钮中间格外突兀。
  修法：选择器改为 `input:not([type="checkbox"]):not([type="range"])`（覆盖 text/number/无 type/将来任何文本型 input），并给该框补 `type="text"`；
  同时把四种控件的**高度锁到 30px**（原先下拉 31px、数值框 30px，差 1px）。
- **② 复选框仍是系统原生**：v2.1.3 只给它加了 `accent-color`，所以它仍是系统圆角方块——跟旁边的玻璃框仍是两种材质，**四个页签都一样**（用户说的「其它页」正指这个）。
  现自绘：`appearance: none` + 同底（`--bg-soft`）/同描边（`--border`）/5px 圆角，选中填 `--primary` + 白勾（`::after` 画的转角方块，不用 SVG，避免 data-URI 里写死颜色），并有 `:hover` 描边与 `:focus-visible` 主色焦点环。
- **③ 顺带收口同类缺陷**：列表页的筛选复选框（`AchievementsView` / `DecorView` / `GearView` / `QuestsView`）原本也是**原生 13px 方块**，紧挨着 24px/6px 圆角的玻璃搜索框——同一处毛病。
  抽出公共类 **`.ui-check`**（规则写在 `main.css`，选择器为 `.ui-check, .settings-body input[type="checkbox"], .switch-row input[type="checkbox"]`），四页各加一个类即可；**以后新写复选框加 `class="ui-check"` 就自动统一**。
- **验收方式**：逐页签 dump 每个控件的 computed（`height/borderRadius/borderColor/backgroundColor/appearance`），要求四类控件同款；不能靠目测（19px 那个框在截图里只是「有点小」）。
- **回归**：`system_test` 795/0 · 11 套 CI 全绿 · 15 项 e2e 全绿（`e2e-dark` 逐页签扫描设置弹窗，扫描元素 131258）· `vite build` 通过。玩法零改动。

## 11.40 v2.2.0：「挂机产线」从 5 个扩到 9 个 + 8 品级蜂蜜（2026-09-14）

用户原话：「我想在功能页的挂机产线加入三个类似于采集队、产地的功能」→ 讨论后定为 **A 商队线 / B 菌房 / C 灵田**，
随后追加 **蜂场（8 品级蜂蜜，图片由用户提供）/ 温室 / 网箱**，并明确 **蜂场并入温室、网箱并入牧场**。

### 11.40.1 先修 5 处既有不一致（用户指定「先修复五个问题」）
1. **今日待办的「采集队归来」是死代码**：`TodayView.vue` 调 `player.expeditionUnlocked()` **漏传 `lineId`**，
   而该函数无参时 `getExpedition(undefined) → null → false` ⇒ 两张采集队卡片**从来没显示过**。改为 `EXPEDITIONS.some((e) => player.expeditionUnlocked(e.id))`。
2. `automation.js` 文案「每 **15** 秒自动出售」→ 实为 **5** 秒（`player.js` 的 `_autoAccum < 5000`）。
3. `guide.js` 写「**3** 条线路」→ 实为 **5** 条（含面点采买队/腌酿商队）。
4. `guide.js` 写「**五个**产地」→ 实为 **7** 个（漏了云顶高原/竹海山乡）。
5. `guide.js` 地窖只写 12/24/48 → `CELLAR_TIERS` 实为四档（补 **96h ×4**）。

### 11.40.2 四个新系统的机制与分工
| 系统 | 与既有系统的差异（这是它存在的理由） | 离线语义 |
| --- | --- | --- |
| 🐫 商队线（调料调配 Lv20） | 地窖＝固定倍率、交易所＝手动买卖；商队＝**按「归队时刻」的行情**结算，**可能亏**（保底 75%），要投本金 | 绝对时间戳（同采集队） |
| 🍄 菌房（采摘 Lv12） | 牧场吃作物饲料；菌房吃**肥料**——给只用于农田的堆肥开第二条下游 | `lastAt` + 12h |
| 🌿 灵田（采摘 Lv45） | 采集队随机池 / 探索卡片 / 产地乘区；灵田＝**定向**长周期稀有材料（消耗种子天然限速） | 绝对时间戳 + 自动续种 |
| 🐝 温室蜂场（农耕 Lv20） | 温室＝农田的「加位 + 提速 25%」版并**伴生蜂蜜**；蜂箱＝稳定低阶蜜源 | 温室单次 1 轮；蜂箱同牧场 |
| 🐟 网箱（并入牧场） | 与畜栏同构、饲料换成海苔（项目里没有鱼饵类物品，刻意复用既有食材） | 同牧场 |

**商队定价**：直接复用 `exchange.js` 的 `priceMultiplier(itemId, cycle)`（每 4 小时一期、[0.60,1.60]×）。
期望倍率 ≈ 1.10（近路）~1.41（远路、当季），**刻意低于地窖（12h×1.5，单槽上限 16,000）**，定位是「有判断的贸易」而不是印钞。

**灵田数值对照**（种子店内价 → 每格每周期产出价值）：灵芝 27 金/24h → 180；松露 66 金/36h → 580；龙根 84 金/48h → 560；灵果 105 金/72h → 700。
**金币效率显著低于地窖**，产出的是「本来只能靠低概率掉落的稀有材料」。

### 11.40.3 蜂蜜：8 品级 + 双效 + 唯一来源
- 品级：浅芳蜜/青蕊蜜/繁花蜜/秋荆蜜/山槐蜜/野桂蜜/岩花蜜/百花臻蜜（图由用户提供，8 张 64×64 RGBA 原样放进 `public/images/items/tool/`）。
- **双效**：一瓶同时给「经验 ×N」与「产量 ×N」。为此修掉 `useConsumable` 的 `if/else`——**双键物品原先会被静默丢掉产量那条**。
  单瓶数值刻意弱于「两支同阶增益剂叠加」（×4.5 × ×4.5），定位是「省背包格子」而非更强。
- **品级由产出侧等级决定**：温室伴生用作物 `reqLevel`（小麦 Lv1 → 浅芳蜜；灵果 Lv90 → 百花臻蜜），蜂箱用花的 `reqLevel`（菊花 Lv12→2 品 … 桂花 Lv34→4 品）。**两条路径合起来才覆盖 8 品**。
- **唯一来源**：不进掉落/商店/抽卡/交易所/自动出售（`type:consumable` + `category:buff` ⇒ 三套平衡系统都不碰它，数值可自定死）；`itemSources` 两条来源都写「温室蜂场」。
- 旧档无需迁移：蜂蜜是纯新增物品，且**不作为任何配方材料**（`itemUses` 为空）。

### 11.40.4 上限单一来源（顺手收敛）
- `caps.js` 新增 **`IDLE_CAP_HOURS`**（牧场/分店原先各写一份 12h 字面量，已删）与 **`FACILITY_MAX`**（商队 3 / 菌房 3 / 灵田 3 / 温室 6 / 蜂箱 3 / 网箱 4）、**`CARAVAN_CARGO_CAP`**（30,000）。
- 读档一律 `safeList` / `safeCap` 夹取（沿用 C23 的纪律），旧档缺字段回退默认。

### 11.40.5 实测踩到的三个坑（都已进 C25 守卫）
1. **温室离线狂刷**：首版按「离线 12h 能补多少轮」算，小麦 67.5 秒/轮 ⇒ 一次上线收 **640 轮**（+640 小麦 +69 瓶蜜）。
   改为**单次只收 1 轮**（与农耕「成熟即收、不累积」同口径）。守卫断言 `wheat === 1 && greenhouseCycles === 1`。
2. **`spendItem` 归零会删键** ⇒ 断言里读出来是 `undefined` 而不是 `0`（写 C25 时踩到两次，改用 `?? 0`）。
3. **dev server 陈旧模块图会伪装成代码错误**：连跑数小时 HMR 后，页面报 `HONEY_TIERS is not defined`（所有引用处都有 import），
   重启 dev server 即恢复。e2e 首项失败时先按这个排查，别急着改代码。

### 11.40.6 回归与登记
- `system_test` **795 → 832**（新增 **C25 共 37 条**）；`action_sweep` **330 → 368** 个动作；图鉴三查 **2210 → 2218 件** PASS；`content_sync_audit` 六项同步 PASS。
- e2e 四套全绿（`e2e-dark` 的 `VIEWS` 与 `e2e-text` 的 `VIEWS` 都已加 4 个新页）。
- 统计：物品 2218 · 功能页 **51 → 55** · 成就 **196 → 202** · 称号 **97 → 101** · 年鉴类 12 → 15。

## 11.41 v2.3.0：挂机产线「改产乘区」+ 菌房与灵田合并（2026-09-14）

用户反馈原话：「灵田那四个灵植在采集技能到的时候就能无限获取还能比种植快，完全没啥好处，还有农耕、菌房也是……
另外灵芝、松露、龙根、灵果这四个并不等级相近统一，而且只有四个灵植精华也不太合理……菌房和灵田可以并为同一页」。

### 11.41.1 先把问题量化（这是本轮最关键的一步）
| 产出 | 我做的产线（每格每天） | 直接采集（每天） | 差距 |
| --- | --- | --- | --- |
| 灵芝 | 2 件（24h→2） | 挖掘 Lv60 ≈ **15,400** | ÷7700 |
| 灵果 | 2 件（72h→2） | 采摘 Lv90 ≈ **10,800** | ÷5400 |
| 蘑菇 | 8 件（6h→2×4） | 采摘 Lv20 ≈ **21,000** | ÷2600 |

**结论：靠「调高产量」救不了**——调到同量级会把固定材料经济冲垮（材料等级/配方是铁律），调不到就等于没有。
**唯一成立的设计是：产线必须产「采集拿不到的东西」**——① 独占物 ② **乘区**（作用在那条 10⁴/天 的基数上）③ 金币/便利。
顺带说明**农耕为什么没问题**：它的价值是「不占 0-3 的并行挂机槽」，速率与一条采集线同量级（约 1.9 万/天），是真正的第二条线。

### 11.41.2 改法
1. **菌房 + 灵田合并为「灵圃菌房」页**（功能页 55 → 54）：三区块 = 菇床（吃肥料出菌菇）/ 灵圃（种稀有种子出灵植）/ **萃露炉**（把两者酿成菌灵露）。
2. **8 档统一阶梯「菌灵露·Ⅰ~Ⅷ」**，**每档锚定一个真实材料的采集等级**（蘑菇 20 / 茯苓 38 / 灵芝 60 / 松茸 70 / 木耳 71 / 银耳 75 / 松露 80 / 龙根·灵果 90）——
   这直接解决「等级不统一」；8 档与蜂蜜 8 品级同构。效果逐级递增：Ⅰ~Ⅳ 经验+产量，**Ⅴ 起加采集间隔 −6/10/15/20%**，**Ⅶ 起加餐厅收入 +25/50%**。
3. **两条新乘区轴**：`采集间隔`（乘在 `GatheringSkill.intervalMs` 的**最终结果**上；只在「按比例」那支乘会静默漏掉「精通固定间隔」的玩家）与 `餐厅收入`（挂在唯一出口 `restaurantHourlyIncome`）。
   `buffs` 由两轴扩到四轴；`useConsumable` 改为**轴表驱动**（`BUFF_AXES`，`better: 'min'|'max'` 决定「重复使用取更强」的方向——采集间隔越小越快）。
4. **牧场 / 网箱产出重做**：每头动物、每条鱼都 ≥2 种产出，各加 1 件加工品（鸡油/猪油/羊酪/牛骨高汤/鱼酱/鱼子酱/虾油/鲍汁），落在不同乘区轴上。
5. **灵圃收获回收 1 颗同类种子** → 灵圃成为可持续的种子来源，产出也变成两种；菇床补到 2 种（加茯苓）。
6. 新增 **16 件物品**（共 2234 件）；全部 `consumable/buff`，不碰配方与铁律数据。

### 11.41.3 实测踩到的四个坑
1. **`useConsumable` 的条件也要用轴表**：只改了分支体、条件仍写 `buffXp || buffYield` ⇒ 只带采集/餐厅轴的物品（鲍汁、虾油）直接落到 else 报「该物品无法使用」（被 C25 抓到）。
2. **循环依赖**：`achievements → essences → items → essences`。`items.js` 在模块顶层读 `ESSENCE_ITEMS`，谁先被导入谁就拿到 TDZ 里的 const 而崩。
   修法：**`essences.js` 不得 import `items.js`**，物品名交给调用方（`essenceCostText(e, nameOf)`）。
3. **宽表要包 `.table-scroll`**：新页的 8 档表在 390px 下溢出 6px，被 `e2e-test` 的「手机 390px 无横向溢出」抓到（项目既有约定）。
4. **图片处理**：用户给的 8 张菌灵露自带 alpha（64×64，直接入库）；8 张加工品是 2048² 无 alpha 的白底/棋盘格图，用浏览器 canvas 做「四边泛洪去背 + 主体 bbox 裁切 + 最近邻缩放到 64×64」。
   其中 **`猪油` 无法分离**（白油碗身 ≈225 vs 浅棋盘格 ≈220，颜色上不可分；试过阈值泛洪、连通域、描边泛洪、只留油面四种都失败）→ 临时用「鸡油」图占位，等用户重新导出带透明背景的版本。

### 11.41.4 回归与登记
- `system_test` **834 → 850**（C25 追加 18 条）；`action_sweep` 368 → **372** 动作；图鉴三查 **2234 件** PASS；`content_sync_audit` 六项同步 PASS。
- 攻略总览「菌房 / 灵田」两条并成一条「灵圃菌房」并补菌灵露；成就 +2（含称号「萃露师」）；年鉴 +1 类（萃露）；统计 +1 行；图鉴来源与跳转全部补齐。
- e2e 四套 15 项全绿；深色体检扫 109465 元素。

## 11.42 v2.6.0：「效果总览」页 + 全项目效果注册表（2026-09-16）

**用户原话**：「我觉得今日缺一个 BUFF 页，统计当前所有正在生效的 buff、效果、debuff，**一定不能漏下任何一个，必须严查**。」

### 一、先审计：为什么「汇总页」这件事本身有陷阱

两轮穷尽式审计（逐个效果生产者与消费者、逐条读取入口）得出一个关键结论：**全项目只有 4 个真正的乘区聚合出口**

| 聚合出口 | 文件 | 聚合了什么 |
| --- | --- | --- |
| `Skill.addXp()` | `src/game/skills/Skill.js` | 食灵 / 奥义 / 公会 / 图谱 / 米其林 / 信仰 / 荣誉 / 厨神之路的经验、转生、增益剂、限时窗口 |
| `GatheringSkill.yieldExtraChance()` | `src/game/skills/GatheringSkill.js` | 奥义 / 公会 / 图谱 / 信仰 / 荣誉 / 厨神之路的产量、增益剂、节庆、天气、幸运食材、农耕精通联动 |
| `player.restaurantHourlyIncome` | `src/stores/player.js` | 菜系研究 / 增益剂 / 装潢 / 好感与常客小费 / 米其林 / 班底 / 信仰 / 荣誉 / 套餐 / 厨神之路 / 限时窗口 |
| `Combat.playerStats()` | `src/game/combat/Combat.js` | 装备（含词条/套装/宝石）/ 奥义 / 图谱 / 厨神之路 / 秘境 / 醉酒 / 战斗内增益 |

而**其余 20 多个来源是散装的**，只读上面四个出口必然漏项：

- `weatherEffects().farmYield` 只在 `FarmingSkill` 里读（**不在** `marketBoost()` 里，后者只覆盖 4 条赛道）；
- `festivalBoost().gatherYield` 只在 `GatheringSkill` 里读（同样不在 `marketBoost()` 里）；
- `farmMasteryGatherChance()`、`luckyItemBonus()`、`seasonalCropBonus()`、`shanhaiEffects().flatYield` 都各只有 1~2 个直读点；
- `daoEffects().seedChancePct` 在采摘 / 挖掘里各读两次；
- 料理回血三条（奥义 / 菜系研究 / 灶君）分散在 `Combat` 内；
- `gainGold()` 的金币加成（装备词条 + 厨神之路）与荣誉的 `goldPct` **不是同一口径**（后者只作用于餐厅时收）。

### 二、技术落点：一张登记表（而不是一段挑选逻辑）

`src/game/data/activeEffects.js` —— **85 行效果来源**，7 分组：

| 分组 | 行数 | 覆盖 |
| --- | --- | --- |
| 🌤 全局环境 | 9 | 天气（含恶劣减益）、运势缓和、幸运食材、节庆、限时窗口、四条增益剂轴 |
| 🌾 采集与产量 | 12 | 奥义 / 公会 / 图谱 / 信仰 / 荣誉 / 厨神之路 / 山海食经的产量、农耕精通联动、精通双倍与保底、采集队、产地派驻、垂钓命中 |
| 📈 经验与制作 | 11 | 食灵 / 奥义 / 公会 / 图谱 / 荣誉 / 米其林 / 信仰 / 厨神之路的经验、菜系研究、转生、经验倍率设置、对决追赶 |
| 🍽 餐厅与经营 | 15 | 餐厅等级、菜系研究收入、装潢、好感与常客小费、米其林、班底（含欠薪停工）、套餐、分店、地窖、金币获取、供应商、外卖、任务金币 |
| 🌱 农田与产线 | 17 | 天气×农田、当季作物、农具、肥料、精耕作物、萃露炉催化、枯萎、腐坏、四处缺料停机、温室伴生蜜、蜂箱、菇床料线、灵圃定向、商队保底、制作队列暂停 |
| ⚔️ 对决与挑战 | 16 | 食灵 / 装备 / 品鉴值上限 / 奥义 / 公会 / 图谱 / 厨神之路 / 秘境 / 料理回血 + **战斗内 7 条临时状态**（酱料增益、持续回血、饼干、醉酒、灼烧、中毒、面之束缚） |
| 🛏 离线与容量 | 4 | 离线上限（基础 12 + 饼干 + 厨神之路 + 山海食经）、离线效率 80%、徒弟加成、容量与槽位 |

每行 `read(player, ctx) → { on, text, why, left }`；`collectEffects()` 把它们按分组汇总成 `{ groups, dormant, stats }`。
**关键设计：未生效的项不隐藏**，收进 `dormant` 并在页面上以折叠区列出、逐条写明原因——这既是给玩家的信息，也是「没有漏掉任何一项」的**证据**。

- **只读**：不写任何状态、不新增货币；唯一新存档字段是统计量 `stats.effectsSeenMax`（历史同时生效最多项数）。
- ⚠️ **互斥不变量**：`groups[].items` 只装生效项、`dormant[].items` 只装未生效项。首版两边都装 → 「生效 + 未生效 = 总行数」不成立、页面重复渲染，被 C26 抓出（134 ≠ 85）。
- **踩到并修掉的 4 个实现坑**（都是「守卫先发现、人再修」）：`p.favorLevel` 是**方法**不是 getter（首版把函数体渲染进了文案，出现 `−NaN%`）；`getAllSkillInstances()` 返回**数组**而非对象（遍历键得到下标，技能名显示成 `4`）；`apprenticeOfflineBonus()` 返回的是**加成值 0~0.2**、不是效率 0.8~1.0（文案说反）；`BISCUIT_TASTE_RATE = 10` 已是百分数（乘 100 会变成 1000%）。

### 三、把「严查」变成可验证的守卫（本次最重要的产出）

1. **静态**（`content_sync_audit.mjs` 新增 3 项）：扫 `player.js` 里所有 `*Effects|*Boost|*Multiplier|*Mult|*Pct|*Bonus|*Factor|*Chance|*Modifiers|*Hours` 访问器（**28 个**），**每一个都必须在注册表源码里出现**；豁免表必须写理由、并按「函数是否还存在于 `player.js`」判过期；注册表行数 ≥40（防「表被清空后守卫恒真」）。
   **反向验证**（项目纪律）：抹掉 `weatherEffects` → 立刻报 4 条漏登记；把表清空 → 报 28/28。
   ✅ 这道守卫的现实价值是**当场抓到我 4 处「自己重算公式而不调用真实访问器」**（`schoolCraftXpPct` / `schoolIncomePct` / `schoolHealPct` / `luckyItemBonus`）——那种写法在公式改动后会让页面骗人，正是要拦的。
2. **运行时**（`system_test.mjs` **C26，15 项**）：注册表 id 唯一 / 分组有效 / 字段齐备 / 规模 ≥40；**新档与富档都必须跑得通且不许出现「读取失败」**；未生效行必须有原因；生效 + 未生效 = 总行数；文案不许有 `undefined` / `NaN` / 未求值插值；**汇总前后逐字段快照一致（只读契约）**；战斗内 7 条临时状态「无战斗全 off 且有原因 / 战斗中全亮」；`effectsSeenMax` 只增不减并随存档往返；成就与统计页取数口径一致。

### 四、页面与同步

`src/views/EffectsView.vue`（左栏「今日」组第二格，图标 🧿）：**减益提到最上方**（玩家最需要先看到「正在被扣什么」）→ 7 个赛道分组 → 每行 = 图标 + 名称 + 「增益 / 减益 / 规则」标签 + 来源 + 数值 + 剩余分钟 + 直达按钮；色值全部走主题变量，深浅色与 15 套皮肤自动适配（`e2e-dark` 逐皮肤浅+深全扫，**112,878 元素**通过）。
顶部状态块沿用 v2.5.4 的 `StatusChips`：生效 N 项 / 增益 / 减益 / 规则 / 本期未生效 / 登记总数。

四处登记（`VIEW_KEYS` / `App.vue` / `Sidebar` 今日组 / 两套 e2e 的 VIEWS）+ 攻略总览条目与关键词白名单 + 侧栏派生检查（55 页）+ 成就 +2（`effects25` 运转如仪 / `effects45` 万象加身，门槛按「新档约 15 项」定档）+ 称号 +1 + 故事新增需求（同时生效 12 项）+ 统计页一行。

**回归**：`system_test` **889/0** · 11 套 CI 全绿 · e2e 四套全绿（66 页标记扫描 0 命中）· build 通过。

## 11.43 v2.6.1：制作类精通可见化 + 档位表改为「派生」（2026-09-16）

**用户原话**：「我有个建议是关于厨房笔记功能页，为什么不像采集那样显示在卡片上以及加 📖 精通档位说明」。

### 一、先核事实（三条与直觉不同）

| 直觉 | 实际 |
| --- | --- |
| 厨房笔记卡片没显示精通 | **有**：`精通 Lv12 / 100` + 进度条 + `x / y 次 · 累计 N` + `经验 ×1.1 · 双倍 15% · 保底 +1` |
| 制作类到处都看不到精通 | **制作技能页的配方卡完全看不到**：`ProductionView.vue` 里「精通」原本 **0 处提及**（采集卡有、制作卡没有） |
| 采集页那张 `📖 精通档位说明` 是现成的公共组件 | 它是**手写死在 `GatheringView.vue` 里的 11 行数组**（`{lv:'5 级', xp:'×1.1', dbl:'1%', inv:'减 1/3'}`…），与 `mastery.js` 的函数各自独立 |

核对结论：手写表与 `mastery.js` **当前逐格一致**（没有数值错误），但它是一份**会脱节的副本**——改平衡时只改一处就会静默骗人。

### 二、改动

1. **`mastery.js` 新增派生层**：`MASTERY_TIERS`（11 档，字段 `xpMult/double/batch/intervalFactor/fixedInterval`，全部由既有函数算出）· `nextMasteryTier(level)` · `masteryToNextTier(level, count)`（还剩几次到下一档）· `masteryIntervalText(tier)`（间隔列口径）。既有函数一行未动。
   - 间隔列统一 `toFixed(1)`，使派生表与被删除的手写表**逐格全等**（已用临时脚本比对：`派生表 ≡ 原手写表 : true`）。
2. **新增共用组件 `src/components/MasteryHelp.vue`**：`📖 精通档位说明` 按钮 + 档位对照表弹窗。
   - 弹窗用 **`<Teleport to="body">`**：卡片祖先有 `backdrop-filter`，它会把 `position: fixed` 变成「相对该祖先定位」，不传送出去会错位。
   - `mode="craft"` + `:show-interval="false"`：制作配方没有采集间隔，少一列，避免误导；并补一句「制作类双倍＝一次做出两份、保底＝额外多出一份」。
3. **三处接入**：采集技能页（替换掉那 11 行手写表与它的弹窗，`GatheringView` 净减 30 行）· 制作技能页 · 厨房笔记。
4. **制作页配方卡补精通**（与采集卡同构）：`经验（含精通倍数高亮）+ 精通 Lv/100 + 进度条 + x / y 次 · 再 N 次到 M 级`。
5. **厨房笔记卡片补「下一档」**：`下一档 20 级（再 28 次）：双倍 5%→10%、经验 ×1.2→×1.4`（只列真的会变的项；满级不显示）。

### 三、守卫（`system_test` C27，14 项）

档位表必须由函数派生（手写即 FAIL：逐格比对 `t.xpMult === masteryXpMultiplier(t.level)` 等五个字段）· 档位覆盖 5~100 共 11 档且严格递增 · **单调性**（经验/双倍/保底不减、固定档不更长、比例不更大——保证「升级永远不会变差」）· 关键边界（5 级 ×1.1、50 级 30%+1、100 级 ×4 80%+2、固定档 20 级起 3.6s 且 **19 级为 null**，防「整段替换」回归）· 展示格式（`减 1/3` / `减半` / `≤ 3.6s` / `≤ 3.0s` / `≤ 2.0s`，**一位小数不能丢**）· 「下一档」次数与 `countForMasteryLevel` 对齐且满级为 null · 三处页面都用共用组件 · 组件从 `MASTERY_TIERS` 派生且自身不含档位数字 · **视图里不再出现手写档位数字** · 弹窗必须 Teleport · 制作配方确有 `masteryCount/masteryProgress` 且吃到同一组保底/双倍函数。

**回归**：`system_test` **903/0** · 11 套 CI 全绿 · e2e 四套全绿（66 页扫描 0 命中、深色体检 112,659 元素）· build 通过。玩法数值零改动。

## 11.44 v2.6.2：去掉制作卡上的「再 N 次到 M 级」（2026-09-16）

**用户原话**：「`· 再 40 次到 5 级`，这个显示去掉」。

这个后缀是 v2.6.1 我自己加进制作页配方卡的（不在用户要求里），实际让卡片更挤。现改为：

- **制作页配方卡只留「当前」精通**：`经验（含精通倍数高亮）+ 精通 Lv/100 + 进度条 + x / y 次`，与采集卡一致；`masteryOf()` 不再返回 `next`，`masteryToNextTier` 的 import 一并移除（该函数仍被厨房笔记页使用，不是死代码）。
- **升档规划交给两处**：「📖 精通档位说明」弹窗（采集页 / 制作页 / 厨房笔记三处都有，11 档对照表）与**厨房笔记**页卡片上的「下一档 N 级（再 X 次）：经验 ×a→×b、双倍 c%→d%」——厨房笔记本来就是逐张规划精通用的页面，那一行保留（用户若也要去掉，一句话即可）。
- **守卫**：C27 新增一条**反向断言**：制作页源码里一旦再出现 `masteryToNextTier` 或「再 {{…}}」即 FAIL（C27 共 15 项）。这正是「用户明确不要的东西也要被钉住」的用法——只靠注释挡不住下一个人。

**回归**：`system_test` **904/0** · 11 套 CI 全绿 · e2e 四套 15 项全绿（66 页扫描 0 命中、深色体检 112,659 元素）· build 通过。纯展示改动，玩法数值零改动。

## 11.45 v2.7.0：技能 20 → 22（伐木 / 采矿）+ 20 档木材贯通锻造与强化 + 山海 12 线（2026-09-16）

**用户原话**：「增加伐木技能，每个等级都有不同木头，用于其它适配（比如 75 级的装备材料需要 75 级的木材等等）；将矿物从挖掘独立出来为采矿技能。图片清单最后提供给我，我来补充。」

### 一、先说清事实（三份独立调研的结论）

| 事实 | 数字 |
| --- | --- |
| 装备是严格每 5 级一套、20 套覆盖 Lv1–100 | 铜1-5 / 铁6-10 / … / 琉璃71-75 / 鎏金76-80 / 钨81-85 / 锰86-90 / 钒91-95 / 萤96-100 |
| 矿已按「一矿对一套」分档（Lv16–80） | 13 座同名矿（钢矿@16 … 鎏金矿@76），命名 == 套名 |
| **365 条锻造配方里只有 18 条吃木材、Lv51 以上一条都不吃** | 木材只出现在 weapon/offhand/body/amulet 四类部位 |
| 木材此前只有 1 件（`木材`，value 4） | 来源 = 采摘 50% 附产 |
| 挖掘 = 83 = 43 根茎/菌类 + **41 矿物** | 矿物等级 Lv10–99，**没有 Lv1 目标**；铜矿/铁矿**只是附产**、无法定向采集 |
| 山海食经线↔技能 1:1，10 线 / 460 节点 | 加 2 线 → 480 分支 + 60 汇金 + 12 珍券 = **552** |

⇒ 结论：「75 级装备用 75 级木材」在既有配方里**没有落点**，必须改写部分配方材料（属固定层）——已获用户明确批准。

### 二、伐木：20 档木材

`src/game/data/timbers.js`：`TIMBERS`（20 档）· `WOODCUTTING_TARGETS` · **`timberOfLevel()` = 唯一的「等级 → 档位木材」入口**。
档位名：松木/杉木/桦木/榆木/橡木/樟木/楠木/花梨木/紫檀木/乌木/铁力木/阴沉木/降香木/龙脑木/琉璃木/鎏金木/星辰木/月华木/太虚木/太初神木（每 5 级一档，value 走 `valueBalance` 曲线 `2+2.5×level` 的 ±30% 带）。
物品 schema 只用品白名单键；类别沿用 `material`（自动豁免自动出售/交易所，且不需要新标签）。

### 三、采矿：从挖掘独立

- **分流在技能层**：`ExcavationSkill.js` 导出 `EXCAVATION_ALL_TARGETS`（83）→ `EXCAVATION_GROUND_TARGETS`（42，根茎/菌类）与 `MINING_TARGETS`（43 = 41 矿物 + **铜矿@1/铁矿@5**）。`expansion1/2.js` / `smithOres.js` 等**冻结产物一字未改**。
- **新增铜矿/铁矿目标的意义**：它们此前只作为挖掘附产出现，玩家**无法定向采集**；补成正式目标后采矿有了 Lv1 入口（与其它采集技能一致）。
- **附产按用户决定「保留原样」**：挖掘继续 50% 铜矿 / 30% 铁矿 / 2% 化石 / 10% 种子（所以铜铁矿有两条来源：采矿目标 + 挖掘附产）。
- 🔴 **最危险的一处**：`recipeBalance.js` / `valueBalance.js` 的等级锚清单必须同时登记三份新清单，否则矿物锚整体消失 → 365 条锻造 + 15 条保鲜配方会在**每次启动**被重算（静默失衡）。

### 四、配方改档与强化按档（用户授权的固定层例外，范围收窄）

`src/game/data/timberRecipes.js` 的 `retargetTimberMaterials()`（在 `balanceRecipeLevels()` **之前**调用）：

| 规则 | 结果 |
| --- | --- |
| ① 20 品质套里已在用 `木材` 的配方 → 换成该档木材（数量不变） | **18 条** |
| ② 逐档兜底（**v2.7.1 起改为「逐条覆盖」**）：每条装备配方都补上同档木材（数量 2~5 按档递增） | **347 条** |
| ③ 21 独立矿套 / 赛季装备 / 基础款 `木材` 及其既有用途 | **不动** |

⇒ **20/20 档都有同档木材需求**，20 套配方里**不再有任何 `wood` 残留**，且**不碰 reqLevel / 其它材料 / 任何数值**。
**装备强化**从写死的「铁矿 + 盐矿」改为「**同档木材 + 同档矿**」：档位由 `equipmentLevelOf()` 定（**优先产出它的配方 reqLevel**——实测 `ITEM_LEVEL` 会差 3 级），矿由 `BAND_MATERIALS` 从 20 套配方**派生**（`saltOre` 刻意排最后：它的 id 也以 `Ore` 结尾，会顶掉同名矿，实测踩过）。

### 五、存档迁移（幂等）

`applySave` 里以 `stats.splitMiningMigrated` 为账本做**一次性**迁移：采矿**继承挖掘的等级与经验**（矿物占 41/83 大头）、挖掘等级保持不动、矿物**精通键**迁到采矿、旧档正在挖的矿物**目标改挂到采矿**。
⚠️ **坑**：Pinia 的 `$patch` 是**深合并**，`delete` 的键不会消失 → 迁移里的删除必须在 patch 之后**直接赋值**（实测：否则矿物精通永远留在挖掘里）。
另一处**既有静默 bug 顺手修掉**：`skillTargets` 指向已不存在的目标时 `currentTarget` 返回 null → `tick` 直接 return、`computeOffline` 返回 null、并行挂机把它剔除，玩家只看到「技能不干活了」。现在回退到该技能首个目标。

### 六、山海食经 10 → 12 线

| 项 | 10 线 | 12 线 |
| --- | --- | --- |
| 分支 / 汇金 / 珍券 / 总数 | 400 / 50 / 10 / 460 | **480 / 60 / 12 / 552** |
| 金币总量 | 4,920,000 | **5,904,000** |
| 全树容量 | 230 / 390 / 58 | **276 / 456 / 70** |
| 三档硬顶 `CAP_MAX` | 330 / 890 / 158 | **376 / 956 / 170**（金币路径上限 `PAID_CAP_MAX` 未动） |
| 离线上限 | 6h | **8h** |
| 画布扇区角 / `spread` / 道途标题半径 | 36° / 24 / 215 | **30° / 20 / 252**（否则标题叠字，有 C22 断言） |

新线：**伐薪（伐木，20 件）** 与 **矿脉（采矿，43 件）**；掘藏线因矿物移出变为 42 件。**已有节点 id 全部保持不变**（`pick11`/`dig61`/`gap0`/`tk0`），玩家已点亮的内容不受影响。

### 七、内容同步

成就/称号随技能自动 +6/+2（214 / 106）· 采集公会门槛按 5→7 条线**等比重标定**（50/90/140/200 → **70/130/200/280**）· 食灵阶 1「采耕」域扩到 7 技能（**旧值一字未动，只新增 woodcutting/mining 两个键**——这正是「只向上兼容」的做法）· **矿物 param 的 200 条挖掘轶事加向上兼容键**（否则永久解锁不了）· 攻略/故事/荣誉/里程碑/图鉴来源与跳转/导航全部同步 · 20 张木材占位图（**待用户替换**）。

### 八、守卫

`system_test` 新增 **C28**：伐木 20 / 采矿 43 / 挖掘 42 的目标数 · 20 档木材与套装等级一一对应 · 改写后**不留 `wood` 残留且 reqLevel 未变** · 逐档都有木材需求 · 强化消耗按档（含 `equipmentLevelOf` 的口径）· 迁移幂等且旧档可读 · `currentTarget` 兜底 · 基础款 `木材` 与档位木材互不影响 · 图鉴三查。C22 全套数字随 12 线同步。

**回归**：`system_test` 全绿 · 11 套 CI 全绿 · e2e 四套全绿 · build 通过。

## 11.46 v2.7.1：全部装备配方逐条吃同档木材（2026-09-16）

**用户原话**：「所有装备所需材料都要加上对应等级的木材。」

v2.7.0 只做到「每档**挑一件**木器部位」（砧板；8 槽高段套用锅）⇒ 20/20 档有需求，但**大多数装备本身仍不吃木材**。本版改为**逐条覆盖**：

| | v2.7.0 | v2.7.1 |
| --- | --- | --- |
| 含木材的装备配方 | 32 条（18 换档 + 14 兜底） | **365/365 条**（20 品质套 176 + 21 独立矿套 189） |
| 覆盖口径 | 每档至少一件 | **每一件装备都吃它那一档的木材** |
| 21 独立矿套 | 不动 | **也纳入**（用户解除该限制） |

- 数量规则不变：`timberQtyForLevel()` 按档给 2~5（1-25 档 2 个、26-50 档 3 个、51-75 档 4 个、76-100 档 5 个）；原有 18 条保留其原有数量。
- **范围仍然只动 `ingredients` 的木材那一项**：不碰 reqLevel、不碰其它材料、不碰任何数值；仍是规范化函数（`data/timberRecipes.js`）而非手改产物；基础款「木材」及其既有用途一字未改。
- 效果：铜刀要松木、琉璃砧板要琉璃木、蓝晶戒指要太初神木——「N 级装备用 N 级木材」对每一件装备成立，伐木成为锻造的**通用前置**（这也是用户要求这条规则的本意：让 20 档木材都有稳定需求）。
- **守卫**：C28 相应加强——`365/365 条装备配方都含该档木材`、`每件恰好一种木材（不混档）`、`数量 1~5`、`189 条独立矿套逐条覆盖`、`配方向条目数与 reqLevel 未变`（C28 共 26 项）。
- 顺手清掉 v2.7.0 遗留的三个死函数（`bandRecipes` / `isBoardRecipe` / `isPotRecipe`——逐条覆盖后不再需要「挑部位」）。

**回归**：`system_test` **933/0** · 11 套 CI 全绿 · e2e 四套 15 项全绿 · build 通过。

## 11.47 v2.9.0：新增独立大类「副业」+ 首支技能「木工」（2026-09-16）

**用户决策**：「可以在左侧导航栏上面加个大类：副业，于是就是变成了：技能 功能 副业，不吃食灵经验加成，不加入山海食经」；物品规模走他选的**方案 A**（每技能 10 件、不新增材料、10 张图）。

### 定位（最重要的一条）
**副业不产食材，产「餐厅经营侧的乘区与独占品」。** 制作类把食材变成能吃/能用的东西；副业把**采集原料**变成**采集拿不到的、只能自己做的**经营侧加成。
木工 = 伐木木材 → 木器 → 手工装潢。**判据**：如果一个副业技能只是在「多一条产材料的线」，那它永远没有意义（采集是无限的，量级差 3~4 个数量级，见 §11.41 的同一结论）。

### 第三页签
- 左栏从「技能 / 功能」两栏 → **三栏**（`SKILL_CATEGORIES` 加 `tab: 'skill' | 'side'`，内容一律由 `skillCategoriesOfTab(tab)` 派生，Sidebar 与守卫共用）。
- 三格在 170px 窄栏的可用宽约 142px ⇒ `.sidebar-tabs` 的 gap(4px)/padding(6px 4px)/字号(11px)+`nowrap` 是算过的。
- **修掉一个连带缺陷**：从搜索/图鉴跳到「木工」时 `ui.activeView` 本来就是 `skill` ⇒ 只 watch `activeView` 不会触发，页签留在「技能」、**木工那一行看不见**。现同时 watch `player.activeSkill`，且 `selectSkill()` 按技能大类切页签。

### 数据与配方
- 10 件木器：木碗架 Lv1 松木×2 / 木勺架 Lv11 桦木×2 / 木调料架 Lv21 橡木×3 / 木餐盘 Lv31 楠木×3 / 木酒架 Lv41 紫檀木×3 / 木屏风 Lv51 铁力木×4 / 木长桌 Lv61 降香木×4 / 木雕挂屏 Lv71 琉璃木×4 / 木香案 Lv81 星辰木×5 / 神木供案 Lv91 太虚木×5。
- 配方构造走 `raiseRecipeLevels`：材料本就是**同档木材**（最高材料锚 == reqLevel）⇒ `h - 5 < reqLevel`，抬级分支恒不触发，即**每次启动重算都不改动这些配方**（C32 有「技能构造后零漂移」断言）。用 `balanceRecipeLevels` 会多一条「全部材料超纲就抬级」的隐式改动路径，故不采用。
- 技能复用 `ProductionView`（`type === 'production'`）⇒ 制作队列 / 精通档位 / 自动续队 / 厨房笔记**全部自动生效**，零额外接线。

### 装潢重构（木器的唯一去处）
- 装潢体系是**既有**的：`restaurant.decor` 是 id 数组，`restaurantHourlyIncome` 按 `RESTAURANT_DECOR_BY_ID[id].effect` 求和 ⇒ **收入公式零改动**。
- **`RESTAURANT_DECOR` 保持只含商店 300 件**（分类页签 / `catCount` / 性价比排序一行都不改）；手工装潢只并入 `RESTAURANT_DECOR_BY_ID`，另导出 `DECOR_TOTAL = 300 + 10` 作为页头分母（否则做满会显示 `305/300`）。
- 手工装潢带 `craftedFrom: { itemId, qty }` 且 `price: null`；`player.craftDecor(id)` 是唯一入口，返回 `'ok' | 'owned' | 'denied' | 'bad'`。
- ⚠️ `restaurantDecor.js` 是 `gen_restaurant_decor.mjs` 的**产物**：import 与 `DECOR_TOTAL` 写进生成器，不手改产物（手改会被重跑冲掉，`gen_drift_audit` 也会报「未登记的漂移」——本轮就是这样被守卫抓回来的）。

### 独占品口径（防捷径）
木器是 `type:'ingredient'` + `category:'furniture'`，**默认会被当成普通食材**，必须显式排除 5 处（全部引用 `WOODWORK_CATEGORY` 常量）：觅珍 5 池 + 素材池 / 鲜味食材礼包 / 珍馐阁售卖 / 商队货舱 / 自动出售。交易所是显式 allow-list，不用改。

### 固化进守卫的两条设计决策
1. **不吃食灵经验加成**（`SPIRITS[].effect.xpPct` 是显式枚举，天然不含）——C32 断言 160 只食灵的经验域都没有它。
2. **不进入山海食经**（山海线↔技能 1:1 白名单，加线会改写已定稿的 552 节点布局与 `CAP_MAX`）——C32 断言 `SHANHAI_NODES` 里没有副业技能。
另：**木工刻意不计入 `craftLevels`**（公会「制作总等级」）——该访问器的门槛按「6 个制作技能」标定，把木工算进来会让已标定门槛静默上浮；木工的公会任务改用 `skill` 类型单列。

### 图鉴三查的 `kind:'decor'`
`itemUses()` 对木器返回 `{ outputId: 装潢id, kind:'decor' }`——**装潢 id 不在 `ITEMS` 里**：`ItemDetailModal` 对它渲染为纯文本（无物品详情可跳），`item_triple_audit` 的坏链校验改为「`kind==='decor'` 校验 `RESTAURANT_DECOR_BY_ID`，否则校验 `ITEMS`」（是**加强**而非放宽）。

### 顺手修掉的两个既有显示缺陷（浏览器截图核验时发现）
1. **制作页未解锁配方显示负成功率**：`successChance()` = 基础值 + (等级 − 配方等级)×2% ⇒ 等级不够时算出负数，页面上出现「成功率 -41%」。现 `level < reqLevel` 时显示「Lv71 解锁」。
2. **制作卡「效果」行对无效果产物恒为「原料」**：现数据驱动，有对应手工装潢的产物显示「装潢：餐厅收入 +3.4%」。

### 守卫
`system_test` **C32（24 项）**（内容见 AGENTS.md 同名小节）；`e2e-dark` 补**左栏三页签扫描**（此前逐页只 `setView` ⇒ 副业页签在整套体检里**一次都不会被渲染**，是盲区；扫元素 110,066 → **111,287**）。

**回归**：`system_test` **1000/0** · 11 套 CI 全绿（含 `gen_drift_audit` 一致 7）· e2e 四套 15 项全绿（66 页 0 命中）· build 通过 · 浏览器实测「木材 →木工 木器 →装潢 +24%」全链路。

**v2.9.1（同日）**：10 件木器**正式美术入库**（用户给的 10 张 2048×2048 原图，按时间顺序）。源图四个坑与处理方案（亮度找主体核心 + 只留最大连通域 + 补洞 + 膨胀收边 + 预乘 alpha 面积平均降采样）见 AGENTS.md 的「🖼 物品图入库」小节；工具落在 `scripts/gen/process_item_images.py`，下一批 38 张直接复用。

## 11.48 v2.10.0：副业补齐四支（陶艺 / 编织 / 刺绣 / 蜡烛制作）（2026-09-17）

用户把四组图丢进 `D:\迅雷下载\{ty,bz,cx,nz}` 并说「开始继续」；物品规模沿用 **方案 A**（10/10/10/8 = 38 件）。

### 最重要的一条设计原则：**一支只接一条乘区出口**
木工把产物接进「餐厅装潢」是**因为那里正好是装潢体系**；**照抄四遍就会变成四个「加餐厅收入」**，玩家看不出差别
（与「菇床/灵圃只是一个农场」的质疑同源）。故本轮刻意**一支一出口**：

| 副业 | 唯一出口 | 每件 | 满级 | 出口落点（代码） |
| --- | --- | --- | --- | --- |
| 🏺 陶艺 | 地窖单槽价值上限 | +1,500 | 16,000 → 31,000 | `player.cellarSlotValueMax()` |
| 🧶 编织 | 餐厅小费 | +2% | +20% | `restaurantHourlyIncome` 的小费因子 |
| 🪡 刺绣 | 米其林「招牌绣屏」（第七维，权重 1） | +12 分 | +120 分 | `michelinScore()` 的额外分项 |
| 🕯️ 蜡烛制作 | 夜市狂潮窗口时长 | +1h | 16:00→次日 06:00 | `marketEvents.js` 的 `marketEventsWithNightExtension()` |

**好处**：数值不散落（每支只有一个写入点）、一眼能看出它改了什么、守卫可以逐条断言「做一件 → 数值立刻变化」。
**刺绣刻意不动既有六维权重与星级门槛**（那是标定过的），只作为额外加分项计入总分。
**蜡烛延长窗口**用「结束小时 = 22 + n，越过 24 就拆成 `[[16,24],[0,余]]`」——与既有「午夜食堂」跨夜写法一致，
于是 `hoursText()`、排班表、命中判定**都不用改**；`MarketView` 改为读 `player.marketEventsNow()`，保证**页面显示的时段与实际加成同源**。

### 材料口径
**五支都以「伐木的同档木材」为基材**（伐木成为全部副业的共同前置，也给 20 档木材再添一个稳定需求），另加一味专属辅料：
陶艺=**矿物**（釉料）· 编织=**茎叶类蔬菜**（植物纤维）· 刺绣=**水果**（果染）· 蜡烛=**肉类**（熬脂）。
硬约束：① 全部是既有物品（**0 新增材料**，与方案 A 一致）；② 每条满足 `材料等级 ≤ 配方等级 + 5`（含木材锚）
⇒ `raiseRecipeLevels` 对它们是**恒等变换**（C33 有「零漂移」断言，防平衡函数在启动时**静默改写配方**）。
⚠️ **蜂蜜等产线独占品不得当配方材料**（AGENTS 既有铁律：蜂蜜只能来自温室蜂场、不进配方）。
⚠️ **不要用「蜂蜡」之类不存在的材料**：既有的「蜡」类物品一件都没有，硬凑会逼着新增材料 + 新增图，与方案 A 冲突。

### 通用作品机制
- `player.craftWork(itemId)` → `'ok'|'owned'|'denied'|'bad'`（消耗 **1 件产物**，不花金币）；登记进新存档字段 **`sidelineWorks: string[]`**。
- 各轴加成由 **`sidelineEffectTotal(axis)` 从数组求和派生**，**不另存派生数值**（否则两处真相必然漂移）；读档时 `filter(id => !!SIDELINE_WORKS[id])` 过滤非法 id。
- 作品面板 `SidelineWorkPanel.vue` 挂在**副业技能页下方**（`ProductionView` 末尾）——**做完就地能做成作品**，比另开一页好找；四个出口页面各加一句提示与跳转。
- 定义集中在 **`data/sidelineWorks.js`**：`DEFS` 每行 `[等级, 物品名, 辅料 id, 木材数量, 辅料数量]`，物品/配方/作品表全由它展开。**加一支新副业 = `DEFS` 加一段 + `SIDELINE_AXES` 加一条轴**。

### 顺带完成一处「上限单一来源」收口
地窖单槽价值上限原先写死在 `cellar.js`（`CELLAR_MAX_BASE_VALUE = 16000`），被 5 处消费（校验 + 视图的输入夹取 / 满额估算 / 两处文案）。
按既有 caps 规矩收进 **`caps.js`**（`CELLAR_SLOT_VALUE_BASE/_MAX`），5 处统一走 `player.cellarSlotValueMax()`——
否则做满陶器后地窖页仍按 16,000 报价，**等于奖励没生效**（C33 断言满配 == 硬顶 31,000）。

### 38 张美术入库：图片工具的判据改对了
早先的办法是「按**亮度**找主体核心（`min <= 185`）+ 取最大连通域」。它在深色主体上看着没问题（前两批侥幸全过），
但在本批的**浅色主体**上**静默截断**：浅蓝的叠布只剩 **1.6%**、粉白的香薰烛只剩 **5.9%**，图标变成一条残片——而**图鉴不会报错**，只显示一张怪图。
根因：**亮度分不开「浅色物体」与「灰投影」**。改用 **色度（`chroma = max − min`）**：

```
bg_cand = (min >= 235) | (chroma <= 10 且 min >= 190)   # 近白 或（淡 且 中性灰）
        → 从画布边缘洪水填充 → 去掉面积过小的孤立块
```
实测主体（无论多浅）**色度中位 43~171**，背景/投影/水印只有 **2~5** ⇒ 灰投影与水印去掉、浅蓝的布与粉白的烛完整保住。
**降采样默认 `box`（预乘 alpha 的面积平均）**：二值 alpha 经面积平均后边缘自然得到 1px 抗锯齿（最近邻会把抗锯齿白边采成「白点」——用户三批都报过）。

### 守卫
`system_test` **C33（27 项）**：四技能注册 · 38 件产物与作品定义 · 38 条配方材料等级与**零漂移** · 五处独占过滤 · 不吃食灵经验 · 不进山海食经 ·
**四条出口「做一件 → 数值立刻变化」** · 米其林第七维不改既有六维权重 · `craftWork` 四种语义 / 幂等 / 消耗 · 满配合计与硬顶 ·
蜡烛延长**真的改变命中判定**且被夹在 0~上限（不会变成全天）· 存档往返 / 旧档回退 / 非法 id 过滤 · 图鉴三查 · 效果总览登记 · 任务登记。
`e2e-dark` + `e2e-text` 各补**副业技能页扫描**（`skill` 视图只渲染当时激活的那一个技能 ⇒ 五支共用一个 ProductionView，新的作品面板此前是盲区；深色扫描元素 111,368 → **113,083**）。
`action_sweep` 加 `craftWork`；`item_triple_audit` 的坏链校验扩展 `kind:'work'`；`content_sync_audit` 当场抓出两个新访问器（`tipBonusPct` / `nightMarketExtraHours`）**未登记效果总览**并补齐。

**回归**：`system_test` **1025/0** · 11 套 CI 全绿 · e2e 四套 15 项全绿（66 页 + 5 个副业页 0 命中）· build 通过 · 浏览器实测全链路。

## 11.49 v2.10.1：用户实测三件事（左栏空白 / 装潢下调 / 多余产物的出口）（2026-09-17）

用户原话：「① 点了技能后，左边的技能列表就消失了 ② 餐厅装潢的 300 件装潢的加成应该下调，加成大手工的太多了 ③ 木工、陶艺、编织、刺绣、蜡烛制作是技能，但是做了一件后就能用了，多制作出来的没用了」。

### ① 左栏空白 —— v2.9.0 引入的真 bug
第三页签落地时，`SKILL_CATEGORIES[].tab` 写成了**单数 `'skill'`**，而 `Sidebar.vue` 三个 nav 的 `v-show` 判的是 **`'skills'`**。
于是 `tabOfSkill()` 在点**任何**技能时返回 `'skill'` —— **三个列表的 v-show 全为假，左栏整个空白**。
副业那一支的 key 恰好也是 `'side'`（对得上），所以我上一版的浏览器核验只点了副业技能，**没抓到**。

**修法**：tab 值统一成侧栏既有的 key（`'skills'` / `'side'`）。
**防复发（两道，都是「拿守卫去核」而不是「靠手点」）**：
- `system_test` C32 新增**派生断言**：从 `Sidebar.vue` 源码正则抽出全部 `sideTab === 'X'` 字面量，
  要求每个 `SKILL_CATEGORIES[].tab` 都在其中、且每类都能被 `skillCategoriesOfTab` 取到（不重不漏）。
- `e2e-test` 新增「**逐个激活全部技能**，每次左栏都必须有 ≥1 个可见 nav」。

**教训**：跨文件共用一组 key 时，**必须让守卫去核**。手点只会点到你恰好想到的那一条路径。

### ② 装潢数值下调
商店 300 件合计 **+469%**、手工 10 件合计 **+24%** ⇒ 手工在装潢总乘区里只占 **4%**，10 件手工感觉不到。

| | 单件 | 合计 | 说明 |
| --- | --- | --- | --- |
| 商店 300 件（改前） | +0.5%~3% | +469% | 总价 9,588 万金 |
| 商店 300 件（改后） | **+0.25%~1.3%** | **+209%** | **价格一分未动**（标定过的金币沉淀口） |
| 手工 10 件（改前） | +0.6%~4.2% | +24% | |
| 手工 10 件（改后） | **+2%~11%** | **+65%** | 单件明显强于商店最贵那件 |

结果：装潢合计 ×5.93 → **×3.74**，**手工占比 4% → 24%**（10 件顶商店 300 件的三成）。
改的是**生成器**（`gen_restaurant_decor.mjs` 的 `EFF_MIN/EFF_MAX`）与 `woodworking.js` 的手工系数 —— 两处单点可平移，
**不要散着调单件**（`restaurantDecor.js` 是生成器产物，手改会被重跑冲掉）。

### ③ 多余产物的出口
产物原先按「等级的曲线值」定价：Lv1 陶碗只值 **5 金币**，而材料（松木×2 + 铁矿×2）值 **40** ⇒
「半价卖给杂货铺」这条出口**名义上存在、实际等于零**（这也是用户说「多制作出来的没用了」的根因）。

**做法**：新增 `applySidelineProductValues()`（`valueBalance.js`），令
**副业产物的 value == 其配方材料的价值合计**；它必须在 **`applyValueBalance()` 的材料平衡之后**执行
（材料 value 会被上面那段改写），接在 `applyAlchemyRatioCap()` 后、`resetMijianPoolCache()` 前。
⇒ **卖一件产物 = 把这份材料整包卖掉**（产物 ×0.5 == 材料 ×0.5），多余产物永远不浪费；
**零新机制、零新货币、零新存档字段**。

**安全性（改 value 前必查的三条）**：① 产物已排除出全部池子与自动出口（抽卡/礼包/珍馐阁/交易所/商队/自动出售）；
② **没有任何配方或炼金把它们当材料**；③ 图鉴兑换点数看的是**收集百分比**而不是 value。
C32/C33 各加了一条「产物价值 == 材料合计」断言。

**另外两个备选（未采用，留档）**：
- **可重复制作 + 递减收益 + 每件封顶**：直接加深永久加成（信息更爽），但要重标定 4 条轴的上限；
  而且**蜡烛那条不能叠**——夜市窗口延长到 18h+ 就失去「时段」意义（时长天然有上限），会出现「到顶后又是死件」。
- **改成限时 buff 消耗品**：产出永远有需求，但五支全变「维护型」、失去永久成长，改动最大。

## 11.50 v2.11.0：副业「量产阶梯」（2026-09-17）

**用户提问**：「要升满级肯定要制作很多个吧，那也不可能全部都用到」——先把它量化，结论会推翻一整类方案。

### 量级（已核实）
| | 数值 |
| --- | --- |
| 升到 100 级所需累计经验 | **3,182,176,592**（曲线按「99→100 = 3 亿」缩放，RuneScape 指数形状） |
| 用最高档配方（Lv91，+1208 xp） | **≈ 263 万次**制作 |
| 叠满经验加成（精通 ×4 / 转生 +20%/层 / 食灵 / 奥义 / 公会 / 米其林 / 信仰 / 荣誉 / 厨神 / 节庆 / 限时窗口） | 仍 **几万 ~ 十几万次** |
| 「作品」能吸收的量 | **10 件** |

⇒ **任何「把具体物件用掉」的设计（每槽配一件陶器、每件绣品换一次招牌、织物送常客…）吸收量都是个位数到几十件，在这个量级下等于没设计。**
**所以产物不该被设计成「要被用掉」的东西，而该按「数量」计价。**

### 机制：五支各一条深阶梯
- 12 档，门槛 ×2.5 递增：10 / 25 / 60 / 150 / 400 / 1k / 2.5k / 6k / 15k / 40k / 100k / 250k **点**
- **计点按档位加权**：`点 = 1 + ⌊配方等级/10⌋`（Lv1 陶碗 1 点 → Lv91 龙凤陶瓮 10 点）。高段产物不会被当柴烧。
- 每档：木工 **+2% 装潢** · 陶艺 **+750 地窖上限** · 编织 **+1.5% 小费** · 刺绣 **+10 招牌分** · 蜡烛 **+0.03 夜市倍率**
- 存 `stats.sidelinePoints`（进 `defaultState.stats` ⇒ 随 `stats` 展开自动兼容旧档，**不需要迁移脚本**）

**蜡烛那条刻意只加倍率**：它的**时长**已封顶 +8h（16:00–次日 06:00），再延就没有「时段」这个玩法了。

### 守住「转生不能自罚」
`prestigeSkill` 在满 100 级时把等级重置为 `1 + carry`，`carry = ⌊100×0.05⌋ = 5` ⇒ **回到 6 级**。
所以**任何轴都不能挂「当前技能等级」**——那会让转生 = 自罚，玩家永远不敢转生这五支，而转生是全局主要长期目标（每层 +20% 经验、上限抬到 120、还给轮回印记）。
阶梯吃的是**累计投入点**（只增不减、转生不碰）⇒ **转生只赚不亏**。
守卫用**行为断言**（比源码扫描稳）：把五支等级全改成 1，六条轴的派生值必须**一分不变**；再断言「转生后上限不下降、还能继续投入」。

### 把「满加成会不会平衡崩坏」变成可执行断言
逐条算了满配放大，并写成 **C34 的比值守卫**：

| 轴 | 满配放大 | 阈值 |
| --- | --- | --- |
| 装潢加成（木工阶梯） | +24% | ≤1.25× |
| 小费（编织） | +18% | ≤1.6× |
| 夜市倍率（蜡烛） | ×2 → ×2.36（1.18×） | ≤1.25× |
| 招牌分（刺绣） | +120 分 = 3★ 门槛的 **39%** | ≤40% |
| 地窖上限（陶艺） | 16,000 → **40,000** | == 硬顶 |

**为什么不会崩坏**（结构性理由，不只是数字）：① 所有阶梯增量都加在**既有乘区的既有层**上，**一个乘区都不新增**；② 地窖那条是**单槽吞吐上限**而非单价——它不改「一瓶酒陈酿 = 价值 ×4」的比率，只决定一次能塞多贵的酒；③ 米其林评分本来就**无上限增长**（订单数永久累加），3★ 是迟早的事，刺绣只是加速，不可能「破坏」评级。

### 上限
`caps.js` 的 `CELLAR_SLOT_VALUE_MAX` = 基础 16,000 + 作品 15,000 + 阶梯 9,000 = **40,000**（C34 断言「陶艺满配 == 硬顶」）。

### 两层分工（最终形态）
| 层 | 吃什么 | 给什么 | 量级 |
| --- | --- | --- | --- |
| **作品** | 每件**一次**（指定的那一件） | 该轴一大档 | 10 件，收集向 |
| **量产阶梯** | 任意产物**按件数**累计 | 该轴一小档 ×12 | 吃几十万件，量产向 |
| **回收** | 满档后多余的产物 | 半价卖回材料钱（止损兜底） | — |

### 顺手修掉的三个展示缺陷（浏览器核验时发现）
1. 图鉴详情出现**两行同名「类型」**（`ItemDetailModal` 用 label 当 `:key` → Vue key 重复）→ 副业产物那行改叫「副业产物」。
2. 蜡烛的阶梯量**错用作品轴口径**，显示成「每档 +0.03 **小时**」（实际是倍率）→ 改用阶梯自己的 `unit()`。
3. 技能名拼出**「蜡烛制作制作」**（`skillName` 本身已含「制作」）→ 不再拼接。

### v2.11.1 补丁（同日，用户实测两处 UI 问题）
1. **木工的作品层是空的**：v2.10.0 起木工没有作品层（产物直接变成「手工装潢」，在餐厅装潢页操作），
   面板只渲染阶梯、而阶梯上那条分隔虚线还在 ⇒ 看起来像「上面漏了内容」。
   现补上作品层（10 件木器就地「做成装潢」，走 `craftDecor`），虚线只在真有作品层时才画。
2. **副业页去掉等级段分类与折叠**：产物只有 8~10 件，原先「按 5 级一段 + 默认全部折叠」⇒ 切成 10 段且全折叠，
   每次进来看不到卡片。现副业五支走**平铺**（`ProductionView.flatMode`），标题按产物改称
   「陶器配方 / 木器配方 / 织物配方 / 绣品配方 / 蜡烛配方」；制作类保持原样。
   顺带修：同一组件在制作类技能间复用 ⇒ 切换时重置折叠状态。

## 11.51 v2.12.0：副业第一批扩充 —— 五支「干净轴」副业（2026-09-17）

### 筛选标准（先写死，再挑候选）
1. **必须占一条此前没人占的乘区** —— 否则做出来就是「又一支加餐厅收入」（这是五支老副业踩出来的教训）。
2. 一支只接**一条**出口（数值不散落、可写比值守卫）。
3. **绝不读「当前技能等级」**（`prestigeSkill` 会把满 100 级打回 6 级 ⇒ 转生自罚）。
4. 材料只用既有物品，且 `材料等级 ≤ 配方等级 + 5`（否则平衡函数会静默改写配方）。

### 轴的现状审计（决定「还能加什么」的关键）
逐个查代码确认「这条轴有没有主」：垂钓稀有率（`RARE_CHANCE` 全项目仅 2 处引用）· 狩猎弹药消耗（`ammoItemId`，**注释原文就写着「后续可由其他技能制作」**）· 订单到访间隔（`ORDER_MIN_MS` 仅 1 处）· 节庆加成（`festivalBoost()` 是纯函数）· 交易所/商队行情（`priceMultiplier(物品, 期次)` 与玩家无关）· 宝石镶嵌效果（`gemsBonus()` 只有 1 个消费方）· 采矿副产（**采矿目前完全没有附产**）—— 以上**全部无主**；
而离线效率（徒弟传承 +0.4%/级、封顶 +20%）、采集产量/经验、制作成功率、对决属性、装潢/地窖/小费/招牌/夜市**都已有主**，不再选。

### 本批五支
| 副业 | 产物 | 占的轴 | 每件 | 满配 | 唯一写入点 |
| --- | --- | --- | --- | --- | --- |
| 🏹 制箭 | 猎具 | 狩猎省箭 | +2% | 38% | `HuntingSkill`（在线扣箭 + 离线动作上限**两处**） |
| 🎣 制网 | 渔具 | 稀有鱼概率 | +0.02pp | 0.5% → 0.94% | `FishingSkill.rareChance`（在线/离线同源） |
| 🧴 香道 | 香品 | 订单到访提速 | +1.5% | 间隔 −27% | `nextOrderDelay(speedPct)`（**两个调用点**） |
| 🧧 年货 | 节礼 | 节庆加成放大 | +1% | +19.6% | `player.festivalBoost`（只放大 >1，不放大减益） |
| 🔮 玉作 | 玉器 | 宝石镶嵌效果 | +1.5% | +33% | `gemsBonus` 的合计上乘一层 |

材料沿统一形态「**同档木材 + 一味专属辅料**」：制箭取矿物（箭头）· 制网取茎叶蔬菜（网绳）· 香道取香料（香方）· 年货取腌味年货（礼盒干货）· 玉作取贝玉珍珠（玉料）。

### 三条值得记住的教训
1. **新类别必须进 `SIDELINE_ITEM_CATEGORIES`**：本轮漏了 5 个类别 ⇒ 新产物会漏进抽卡池/礼包池；被 C35 的「都在独占清单里」断言当场抓出。这正是「独占品口径单一来源」那条纪律要防的事。
2. **测试自身的三个坑**：`freshPlayer()` 会把 `festivalBoost` **打桩成无节庆**（拿它测节庆只会测到桩）· 新档没选目标 ⇒ `computeOffline` 返回 null（狩猎首个目标 Lv8，等级也要给足）· 订单间隔是**随机抽样**，两次抽样比大小会随机失败（改比 300 次均值）。
3. **省箭这类「续航轴」必须检查离线路径**：离线结算按持有陷阱数封顶动作数，只改在线扣箭会让在线/离线不一致 —— 这也正是它比「省钱」更有价值的原因（它真的提高离线吞吐）。

### 守卫
`system_test` **C35（20 项）**：五支注册与 10 件产物 · 五个新类别都在独占清单 · 五条轴的**实际读取点**满配后必须变化 · **行为断言**（省箭真的放宽离线动作上限、年货只放大增益、订单均值真的缩短）· 不读当前等级 · 比值守卫 · 存档往返与旧档回退 · 图鉴三查 · 效果总览 · 任务登记。

## 11.52 v2.13.0：副业第二批两支（货签 / 采掘器具）（2026-09-17）

候选清单的最后两支。至此**副业共 12 支、11 条互不重叠的轴**（木工与陶艺共用「装潢/地窖」两类不同轴，其余各占一条）。

| 副业 | 产物 | 占的轴 | 每件 | 满配 | 唯一写入点 |
| --- | --- | --- | --- | --- | --- |
| 🪙 货签 | 货签 | 交易所卖出价 | +1.5% | +33% | `sellPriceOf(item, cycle, bonusPct)`（**两个调用点**） |
| ⛏️ 采掘器具 | 器具 | 采矿附产 | +2% | +38% | `GatheringSkill.yieldExtraChance`（**只对采矿**） |

### 两条接线各自的关键点（都值得记住）
1. **价格/时间类新轴：先数「这个函数有几个调用点」**。`sellPriceOf` 有 2 个（`player.js` 的**实际结算** + `ExchangeView` 的**展示**），
   `nextOrderDelay` 也有 2 个 —— **两处必须传同一个值**，否则页面显示与到账不一致（v2.12.0 与 v2.13.0 各踩过一次同类）。
2. **附产/产量类新轴：一律挂 `GatheringSkill.yieldExtraChance`**。在线 `yieldQuantity` 与离线 `expectedYield` 都读它
   ⇒ **只改一处就自动在线/离线同源**，不需要像弹药那样改两处。
3. **只想影响单条采集线时用 `this.id === '<skillId>'` 判定**，并配一条「其它采集线不受影响」的断言（C36 有）。
4. 两条轴都夹上限：卖出价 ≤50%；附产被 `yieldExtraChance` 既有的 1.0 夹取自然摊薄（极高产量加成下本项会被摊薄，这是可接受的自限）。

### 为什么选这两条轴（承接 v2.12.0 的轴审计）
- 交易所/商队行情：`priceMultiplier(物品, 期次)` **与玩家完全无关** ⇒ 无主；且交易所**每日每件限 60 件 + 货池每 4 小时轮换** ⇒ 吞吐有上限，**不会像「直接产金币」那样通胀**（这也是我没选商队的原因：商队货舱无同类上限）。
- 采矿附产：**采矿原本完全没有附产**（化石/种子都留在挖掘）⇒ 等于白送一条轴。

### 守卫
`system_test` **C36（13 项）**：两支注册与 10 件产物 · 两个新类别都在独占清单 · 卖出价真的变高且买入价随之水涨船高（价差口径不变）· 加成夹在 0~50% · 采矿附产只对采矿生效 · **在线/离线同源** · 不读当前等级 · 比值守卫 · 存档往返与旧档回退 · 图鉴三查 · 效果总览 · 任务登记。

## 11.53 v2.14.0：副业最后一批四支（造纸 / 乐器 / 制皂 / 钱庄）（2026-09-17）

**这批四支的共同点：都接在既有系统的既有数值上**——不新造页面、不新增货币、不碰任何既有曲线。
（用户此前问「制皂/乐器/钱庄/造纸 要做什么系统」，并强调「不要频繁操作」；答案是：**这四条轴都能直接挂在既有数值上，零操作**，不需要造新系统外壳。）

| 副业 | 产物 | 占的轴 | 每件 | 满配 | 唯一写入点 |
| --- | --- | --- | --- | --- | --- |
| 📜 造纸 | 文房 | 徒弟离线效率上限 | +0.8pp | +14pp（基础封顶 +20% ⇒ 34%） | `player.apprenticeOfflineBonus()` |
| 🎼 乐器 | 乐器 | 常客好感增速 | +1.5% | +27% | `player.favorGain()`（5 处好感来源统一走它） |
| 🧼 制皂 | 皂品 | 订单赏金 | +1% | +19.6% | `makeOrder(player, goldBonusPct)` |
| 🏦 钱庄 | 账具 | 金币获取 | +0.8% | +15%（**夹 ≤15%**） | `player.goldGainPct()` |

### 两条纪律性接线（比数值本身更值得记）
1. **「散在多处的同一个数值」必须先收敛成一个出口，再挂新轴**。好感原本在 **5 处**各写一次 `favor.xp +=`（订单/上菜/常客/宴会/…），
   新轴若只挂在其中一处，就会出现「订单一处生效、上菜另一处没生效」的**静默不一致**。
   做法：新增 `player.favorGain(n)`，5 处全部改走它；C37 用**源码断言**钉住「只剩 1 处直写（即出口自身）」。
2. **夹取要放在唯一出口里**。钱庄的 ≤15% 放在 `goldGainPct()` ⇒ 效果总览显示的数字与实际到账一致；
   若只在消费方夹取，页面会显示 15.2% 而实际给 15%（首版就是这样，C37 抓出）。
3. **制皂刻意避开香道的轴**：订单「到访间隔」是香道的，制皂只吃「单笔赏金」——两支都在订单这条线上，必须错开。

### 顺手修掉一个真 bug
`favorGain` 与 `goldGainPct` 首次落盘时**被放进 Pinia 的 `getters` 块**（应是 `actions`）⇒ `this.goldGainPct?.()` 报
「is not a function」。`system_test` 的**金币词条用例**（洗练出 +100% 金币词条后 `gainGold(100)` 应为 +200）当场抓到 —— 这就是既有回归网的价值。

### 守卫
`system_test` **C37（16 项）**：四支注册与 10 件产物 · 四个新类别都在独占清单 · 四条轴的**实际数值**变化
（含「徒弟要满级才测得出 20% 以上」这种前提易错点）· 好感写入收敛到唯一出口（源码断言）· 好感倍率真的生效 ·
订单赏金进 `makeOrder`（300 次均值）· **金币显示 == 实际到账且夹 ≤15%** · 不读当前等级 · 比值守卫 · 存档往返与旧档回退 · 图鉴三查 · 效果总览 · 任务登记。

### 副业收官
**16 支 / 15 条互不重叠的轴**（木工与陶艺各占一条；其余各一条），两类形态齐备：
**「产独占品 → 接一条新轴」**（v2.10.0/v2.12.0/v2.13.0 共 12 支）与**「接既有系统的既有数值」**（v2.14.0 四支）。

## 11.54 v2.14.1：副业 158 件产物美术全部到位（2026-09-17）

纯美术入库 + 文档对齐，**零玩法/数值改动**。

### 交付
用户把源图按技能分成 11 个文件夹（制箭 / 制网 / 香道 / 年货 / 玉作 / 货签 / 采掘器具 / 造纸 / 乐器 / 制皂 / 钱庄），
每个 10 张、以 `像素游戏物品素材 (N).png` 编号。本批 **110 张**，加上 v2.9.1 的 10 张（木器）与 v2.10.0 的 38 张
（陶艺/编织/刺绣/蜡烛），副业 **158 件产物**（15 支 ×10 + 蜡烛 8）**全部为正式美术，占位图零残留**。

### 工具补两项（`scripts/gen/process_item_images.py`）
- **`--sort name`**：源图按 `(1)…(10)` 编号时**不要用 mtime** —— 复制/解压时间会漂移，`(10)` 会排到 `(2)` 前面、整批错位。
  改取名字里的数字做**自然排序**。
- **`--filter <子串>`**：跳过混进夹子里的旧批次残留（制箭文件夹里混着一张上一批的 `生成8种食材…(2).png`）。
  **精确排除即可，不要删用户的原图**。

### 判据
沿用 v2.10.0 的**色度版**（近白 **或**（淡 **且** 中性灰）+ 边缘洪水填充 + 弃小连通块 + 预乘 alpha 的 BOX 降采样）。
本批主体全是深色/高饱和物件，未再触发「浅色主体只剩残片」那类问题。

### 验收（与「脚本只能证伪」配套）
尺寸/模式/alpha 全量程 + **两两 MD5 不同** + **对照 HEAD 确认「替换前这 110 个文件是不是同一个 blob」**
（实测它们**曾是同一个 blob** = 占位图 ⇒ 替换后互不相同，这才叫「占位图零残留」；MD5 脚本本身说不出语义）+
`item_triple_audit` PASS（2413 件）+ **两张对照图逐张目视**（每个技能一行、按等级升序）。

### 顺手对齐文档规模数字
README 头部那行摘要自 **v2.10.0** 起就没跟着版本更新（一直写 27 技能 / 2303 物品 / 1296 配方 / 229 成就 / 111 称号 / 89 效果行），
现按实际数据校正为 **38 技能（采集 7 / 制作 6 / 对决 6 / 辅助 3 / 副业 16）· 2413 物品 · 1406 配方 · 262 成就 · 122 称号 · 101 效果行**。
⚠️ **以后每次改技能/物品/成就/称号/效果行数，都要顺手看一眼 README 头部那一行**——它不在任何守卫的覆盖面里。

## 11.55 v2.14.2：修「打包的 exe 里很多图片不显示」（2026-09-17）

用户报「打包的 exe，打开后发现很多图片都不显示，是不是打包时漏掉了，全面检查一下」。

### 先分清两件事（结论：**不是打包丢文件**）
| 怀疑 | 实测 | 结论 |
| --- | --- | --- |
| 打包漏了图片 | `app.asar` 内 `dist/images` **2125 张 / 33.8MB，与源 dist 逐数一致** | ✗ 不是打包的问题 |
| 图片路径解析错 | Electron 打开**已打包的 exe**后逐页扫：198 个页面 / 1790 处图片引用 / **160 处损坏**，全部是写了**根绝对路径** `/images/…` 的素材 | ✓ 就是它 |

### 根因
Electron 加载的是 `file:///…/resources/app.asar/dist/index.html`，**根绝对路径 `/images/…` 会解析到磁盘根**
（`file:///D:/images/…`）→ 404；而 `ItemImg` 的 `@error` 会把破图 `display:none` **静默隐藏**（页面不报错、控制台干净）。
dev（vite server 在站点根）与 GitHub Pages 下都看不出问题 —— 所以这个 bug 只在 exe 里现形。

**受影响的是 42 个 URL**：拼图照片池（`ptPhotos.ptUrl`，30 张）、笨鸟先飞（`fb-bg`/`fb-pipe-top`/`fb-pipe-bottom` + 8 张鸟）、游戏币图标 `icon-coin.png`（4 个页面）。
**物品图（2105 张）一张都没坏** —— 因为 `itemImage()` 从一开始就返回相对路径（注释里写着「兼容浏览器与 Electron file://」）。
实测对照：同一批 URL **相对写法 OK、绝对写法 FAIL**。

### 修法
新增**唯一出口 `assetUrl()`**（`src/game/data/itemImage.js` 导出）：把根绝对路径转成文档相对路径，`itemImage()` 也改走它（含 `it.image` 覆盖值）。
6 处引用全部改掉：`ptPhotos.ptUrl`、`FlappyBirdView`（5 处，含 3 处 `new Image().src`）、
4 个页面的 `icon-coin`（静态 `src="/images/…"` 改绑 `:src="assetUrl('/images/icon-coin.png')"`——
**静态写法还会被 Vite 当 import 而在构建期报错**，绑定表达式则不会）。

### 三道守卫（补上这个盲区）
1. `scripts/ci/image_path_audit.mjs`（**进 CI**）：`src/**` 与 `index.html` **禁止根绝对资源 URL**（剥注释后扫描，只放行 `assetUrl(...)` 内的）+
   静态引用的图片文件必须真实存在。已**反例验证**（改回绝对路径立刻 FAIL 并点名到行）。
2. `scripts/ci/exe_image_audit.mjs`（**本地发布 exe 前必跑**）：用 Electron 打开构建产物 / 已打包 exe，遍历 65 视图 + 16 副业页 + 6 弹窗 + 全部小游戏入口，
   **把每个 `<img>` 与 CSS 背景图真的 load 一次**再判定损坏（修前 160 处损坏 → 修后 **0**）。
3. `lmewexe/verify_asar.cjs`（**进 Release 流程**）：断言 `app.asar` 内 `dist/images` 数 == 源 dist（2125），并钉住 `items/food`、`items/tool`、`items/pt`、`birds` 非空。
   ⚠️ 用 Node 解析 asar 头部而**不调 `asar list` CLI**：release 目录名含中文，PowerShell 5.1 会把中文参数按 ANSI 读成乱码 → CLI 拿到错路径（实测踩过）。

### 两条可推广的教训
1. **「本地/线上都好」≠「打包后好」**：dev 与 Pages 的页面都在站点根，正好掩盖根绝对路径；凡是**路径解析依赖页面位置**的写法，都要在 `file://` 下过一遍。
2. 用户报「很多图不显示」时，**先证明「打包有没有丢文件」，再查「路径解析」**——两步分开做，才不至于瞎改打包脚本。

## 11.56 v2.14.3：修「线上版与 exe 的样式跟本地完全不一样」（2026-09-17）

用户原话：「我发现 github 在线版和 exe 版本的样式跟本地版本的样式完全不一样，是什么情况，比如窗口透明度」。

### 症状与根因
**毛玻璃在构建产物里全部失效**（`.card`/顶栏/侧栏/弹窗变成半透明硬色块），dev 正常 ⇒ 「构建产物 ≠ 源码」。

根因链（逐条实测）：
1. Vite 8（rolldown 版）的 CSS 压缩器是 **lightningcss**（`esbuild` 已不再随 vite 提供，`cssMinify: 'esbuild'` 直接报 `Cannot find package 'esbuild'`）；
2. lightningcss 对**同时写了标准与前缀**的规则去重时**留下 `-webkit-`、删掉标准版**——**任何 `cssTarget` 都一样**（`{}`/`chrome110`/`safari18`/`safari16.4` 实测输出一致）；
3. **新版 Chromium 不认 `-webkit-backdrop-filter`**（实测 Chromium 151 算出 `none`）。
   于是主样式表里 28 处毛玻璃面全部失效；`mask` 的标准写法同理被删（流光效果受影响）。

### 修法
`vite.config.js` 设 **`cssMinify: false`**（原样输出作者写的 CSS）。代价：主样式表 gzip 22.6KB → 45.7KB。

### 定位手法（值得复用）
**`vite preview` 跑构建产物 + 逐元素计算样式指纹比对**：65 视图 + 16 副业页（82 个页面、每页全元素 × 21 个属性）——
修前差异集中在 `backdropFilter`，修后 **0 处差异**；并在**已打包的 exe** 里核对 `.card` 算出 `blur(14px)` + 截图目视。
比看截图可靠（截图分不清 blur 是否生效）、比重读构建配置快（我先猜 esbuild/target，查错方向，实测三轮才锁定 lightningcss）。

### 守卫
`scripts/ci/css_output_audit.mjs`（CI 排在 `npm run build` 之后）：前缀敏感的标准属性在 `main.css` 的出现次数必须 ≤ 产物次数 ·
产物里必须有标准写法的 `backdrop-filter` 且不允许「只剩 `-webkit-`」· 断言 `cssMinify: false` 还在。**已反例验证**（删掉该行重构建 → 4 项全 FAIL）。

## 11.57 v2.15.0：真实音频 BGM（13 首）+ 右下角玻璃播放器（2026-09-17）

**起点**：用户要 10 首「不炸裂、舒缓、贴主题」的 BGM。先出了一份规格与提示词文档（`BGM音乐提示词（10首）.md`：逐首给
用途 / 情绪 / BPM·调式·配器 / 英文与中文提示词 / 负向提示词 / 结构建议，外加「Suno 只取前 200 字符 / 每首生成 4~6 版挑最平的那版 / 交给我做响度归一」等实操要点），
用户按提示词生成后把文件放进 `D:\迅雷下载\BGM`，并提出**要一个右下角的玻璃播放器**。

### 素材与入库
13 首 mp3（10 场景正曲 + 3 首「其二」变奏），44.1kHz 立体声、183~199kbps、95~228 秒，共 **45.4MB**。
入库为 `public/audio/bgm/{title,day,night,kitchen,kitchen2,diner,duel,boss,meditate,market,market2,memory,memory2}.mp3`。

### 🔴 响度：这批是「母带级」成品曲，必须两层衰减
实测全曲 **RMS −14 ~ −18.3 dBFS、峰值 −0.4 ~ −2.9 dBFS**（也就是「拉满的商业音乐」电平），
而游戏里的反馈音效是很轻的合成短音 —— 直放会**音乐盖过游戏**。故：
- 每首 `trim`（把 RMS 校到同一基准 −17dBFS，逐首实测值填进 `bgmTracks.js`）；
- `BGM_MASTER_TRIM = 0.6` 再整体压一档；
- 最终音量 = `settings.bgmVolume × BGM_MASTER_TRIM × trim`（默认 0.35 时 ≈ 0.206，实测相符）。

### 引擎（`game/core/sound.js`）
HTMLAudioElement 池 + `loop` + **1.2 秒交叉淡入淡出**；**文件缺失/解码失败 → 标记该曲不可用并回落到原合成音序器**
（day/night/battle 三首合成曲**保留至今专当兜底**：打包漏文件时游戏也不会突然没声音）。
新增 `bgm.current/tracks/available/usingRealAudio/realVolume` 供播放器与守卫读取。

### 场景编排（`App.vue` 的 `bgmScene()`，单点判定）
首领战「山海盛宴」> 普通对决「厨艺切磋」> 按功能页分组（餐厅=堂前烟火 / 夜市节庆=夜市灯火 / 修行=食经修行 / 图鉴故事=旧谱余香 / 制作=灶火慢烹）
> **主题昼夜兜底**（采撷昼日 / 静夜灶前）。
⚠️ **「昼/夜」是唯一一对主题相关的曲**，其余场景各有专属曲、不随主题换 —— 首版把「技能页」一律判成 day，
深色主题下就没有夜曲了，被既有断言 `e2e-audio-skin`（「深色 → 夜曲」）当场抓出并修正。

### 播放器（`components/BgmPlayer.vue`）
右下角玻璃胶囊（与 `.card` 同材质），点击**向上展开** 14 行（自动 + 13 首，每行带用途说明）、点行即播、含音量滑块与开关。
**手动选曲优先于场景**（`settings.bgmTrack`，null = 自动）；读档校验 id 合法性（脏 id 会静默变哑巴，照 `sidelineWorks` 的过滤写法）。
组件**只改 settings**，不直接调 `bgm.play` —— 唯一调用点仍是 `syncBgm()`。

### 顺手修掉的两处排版冲突
右栏 `.app-status` 加 `padding-bottom:52px`（否则事件日志被胶囊压在底下）；平板/手机（≤940px）右栏收起时**回顶按钮上移到 `bottom:64px`**
（否则与胶囊叠在一起）；播放器滑块并入 main.css 里那组「统一控件」选择器（否则又是浏览器默认蓝滑块）。

### 体积代价（做之前先知道）
13 首 45.4MB ⇒ `dist` 46MB → **92MB**、桌面版 zip 153MB → **约 199MB**、Pages 部署同量级。要瘦身只能重编码（本机无 ffmpeg）或砍变奏曲。

### 守卫
`scripts/ci/bgm_audit.mjs`（进 CI，16 项）：清单字段唯一 · 文件都在且非空（>10KB）· **目录里每个音频都被登记**（防「加了文件却没人能选到」）·
scene 有中文标签 · 场景映射自洽 · `trim`/`BGM_MASTER_TRIM` 非 0 且默认音量下 ≤0.25 · 组件从曲库取清单且不直接调 `bgm.play` · App.vue 覆盖全部场景。**已反例验证**。
`lmewexe/verify_asar.cjs`（Release 流程）扩展为**同时断言音频数量**；`e2e-test.spec.mjs` 新增端到端用例（右下角 → 展开 14 行 → 选曲后真实音频在播且音量 <0.4 → 自动跟随场景 → 选曲写进存档）。


## 11.58 v2.15.1：修「切曲重叠」+ 加暂停（2026-09-17）

用户：「点其它音乐，原本音乐不会中断，会重叠播放；需要暂停按钮」。

### ① 切曲重叠（真 bug）
交叉淡变原先只有**一个全局 `realFadeTimer`**，而切曲要同时做两件淡变：「旧曲淡出」+「新曲淡入」——
后者的 `clearInterval` 把前者**打断**，旧曲的完成回调（`pause`）永不执行 ⇒ 旧曲以原音量一直播，两首叠着响。
**修法**：淡变定时器**按元素存**（`Map<el, timer>`，取消只取消自己那份），并补一条不变量——
改音量会打断淡变、可能留下「淡到一半的旧曲」，故 `setBgmVolume` 顺手把「不是当前曲目」的元素停掉复位。
不变量本身可测：`bgm.playingCount()` 任意时刻必须 ≤ 1。

### ② 暂停 / 继续（新增）
引擎加 `pause()`（淡出、**保留播放进度**）/`isPaused()`/`playingCount()`/`position()`；
暂停状态存 `settings.bgmPaused`（**持久化**，与 bgmEnabled 开关是两回事），由 `syncBgm()` 判定：暂停时只更新「该放哪首」不出声，
继续时**从原进度接着放**。UI：胶囊左侧独立播放/暂停钮（右侧仍是展开钮），面板头部同款。

### ③ 守卫
`e2e-test.spec.mjs` 播放器用例扩到 4 组断言（连切 3 首后 playingCount===1 · 暂停后 0 且在响 · position 保留 · 继续不回落）；
`bgm_audit` 加 H 组 4 项（引擎接口齐备 · **淡变定时器必须是 Map** · syncBgm 处理暂停 · 播放器有暂停钮），已反例验证。

### ④ 顺带修一个「守卫被注释骗过」的坑
两个审计原先用 `replace(/\/\*[\s\S]*?\*\//g, '')` 剥注释；而源码里
`// 用 HTMLAudioElement 播 public/audio/bgm/*.mp3` 这行的 `/*` 被当成块注释开头、**吃掉后面 10 行代码** ⇒ 断言对着残缺源码判断（可能假绿）。
新增共用 `scripts/ci/lib/comments.mjs`（三态扫描：字符串/行注释/块注释），两处守卫改用它并各自反例复验仍能 FAIL。

### ⑤ 测试自身的过时假设
`e2e-audio-skin` 原按合成版「关掉 BGM 立即停」（200ms）断言；真实音频是 1.2s 淡出 → 改为等 1.5s 并注明原因。

## 11.59 v2.15.2：常驻控制条 + 播放模式 + 上一首/下一首（2026-09-18）

用户：「🔊 开 的按钮应该常驻显示在播放器上，然后要加上随机播放单曲循环顺序播放的按钮，还有上一首下一首的按钮」。

### 胶囊 = 常驻迷你控制条

收起态就是一条完整的控制条：`[🔊 开关] [⏮ 上一首] [⏸/▶ 暂停继续] [⏭ 下一首] [🔂/🔁/🔀 播放模式] [曲名 ▴]`（6 个控件全部常驻，
原先「只在展开时露出的 开/关」按钮已删除）。点**曲名那半**才向上展开选曲面板；面板底部多了模式按钮行（带文字说明）与音量。

### 播放模式（`settings.bgmMode`，默认单曲循环）

- **单曲循环**：唯一使用 `el.loop = true` 的模式（保持 v2.15.0 的默认听感不变）；
- **顺序播放**：`el.loop = false`，放完由 `ended` 事件接列表下一首（末尾绕回第一首）；
- **随机播放**：放完随机换一首，且**抽样不会等于当前这首**（否则「下一首」看起来没反应）。

纯函数放在 `bgmTracks.js`（`BGM_MODES` / `nextBgmMode` / `nextTrackId(cur, mode, rand)` / `prevTrackId`，`rand` 可注入 ⇒ 守卫能确定性断言）；
**接线**：引擎只管 `setMode`（改 `el.loop`）与在 `ended` 时回调，**「下一首是谁」由 `App.vue` 的 `onBgmEnded` 决定**（写 settings → 仍由 `syncBgm()` 播放）
—— 引擎不写存档，「`bgm.play` 只有一个调用点」这条纪律不破。



### 上一首 / 下一首

以 `settings.bgmTrack ?? bgm.current()` 为基准在曲库里走一格（随机模式下等于换一首），点一下就**转为手动选曲**（面板里点「自动」可切回跟随场景）。

### 引擎新增的播放器 API

`setMode/mode/onEnded`（模式与放完回调）+ `duration()/seek(sec)/looped()`。后三个不只是给 UI 用——**守卫用 `seek(duration-0.3)` 驱动真实的 `ended` 事件**，
从而端到端验证「顺序播放会接下一首、随机会换一首、单曲循环会重头」，比 mock 更接近真实。

### 守卫

`bgm_audit` 新增 I 组 14 项（模式齐备/轮换绕回/next·prev 绕回/随机不自抽/接线/默认值/引擎 el.loop/收起态全套常驻控件），**已反例验证**（让 shuffle 可能抽回当前这首 → FAIL）；

`e2e-test.spec.mjs` 的播放器用例加 4 组断言（6 个控件常驻 · ⏭/⏮ 走格 · 模式轮换且 `looped()` 同步 · 顺序播放推到结尾自动接下一首且仍只有 1 首出声）。



## 12. UI / 布局

- 顶部功能导航（固定不随滚动）：**7 个高频入口**（餐厅/公会/赛季/竞技场/试炼塔/大赛/觅珍）+ 右侧功能组（活动/统计/图鉴/攻略 + 签到/搜索/背包/装备/设置/存档，`设置`/`存档` 为纯图标）**固定贴右**；其余功能页在**左侧栏「🧩 功能」页签**（与「🌾 技能」「🪚 副业」两个页签互斥显示、各自占满高度；**2026-09-16 v2.9.0 起左栏为三页签**：技能（采集/制作/对决/辅助 4 类）· 功能 · **副业**（当前仅木工，见 §11.47）；组内按 8 类分成方块磁贴（**一行四个**、全部展开，共 51 个功能页）：今日（今日待办/天气运势/吉祥物/任务中心/信箱/行情/奇遇图鉴）· 采买与转化（商店/珍馐阁/炼金）· 挂机产线（采集队/产地/牧场/地窖/自动化）· 研究与收集（厨房笔记/风味册/菜系研究/**山海食经**/装备总览）· 餐厅经营（评级/班底/分店/交易所/宴会/外卖/供应商/常客/套餐定食/同业榜/厨友/餐厅装潢）· 挑战与休闲（厨神试炼/厨具赛/名厨挑战/食神秘境/小游戏）· 记录与回顾（里程碑/年鉴/故事/卡牌对战/系统日志/赛季回顾/荣誉殿堂/成就与称号/图鉴兑换）· 成长与信仰（食灵物语/传承/**厨神之路**/信仰/节庆），2026-09-10 改版、2026-09-11 补九页、**2026-09-13 补「厨神之路」「山海食经」**（v2.0 转生天赋树 + v2.1 收集科技树），原分页导航取消）
- **导航红点**：赛季（有可领奖励）、签到（今日未签）
- **清晰度（2026-09-10）**：① 次要文字对比度达标——`--text-dim` → #6b5347（7.0:1）、`--muted` → #7a6f68（4.8:1）；② 全站最小字号 12px（原 10~11px 共 121 处已抬升）；③ 内容区遮罩 0.92 + 卡片落影/内高光，边界锐利；④ 设置「高清晰模式」`<html data-crisp="1">`：关闭全部 backdrop-filter + 内容区近实底 + 卡片实色 + 次要文字再加深；**亮/深双档位各自成套**（亮：内容区 rgba(255,252,247,.98)/卡片 #fffdf9/文字 #57402f；深：内容区 rgba(22,15,10,.985)/卡片 #241a13/文字 #e7d8c5，侧栏背景图叠 0.94 暗层）
- **深色模式守卫（2026-09-10）**：`e2e-dark.spec.mjs`（CI 执行）**先种入代表性数据**（8 槽装备/词条/宝石/背包/货币/各系统状态）再逐页扫描 47 页面 + 6 弹窗（扫描元素量自检门槛 2000，当前约 19400），断言「无对比度问题 + 无硬编码亮底」；元素尺寸门槛 24×12（覆盖小芯片），白名单仅限渐变底抽卡按钮与禁用态。修过的深色缺口：顶栏活动药丸、主色按钮、侧栏技能选中态、侧栏页签/功能磁贴、装备弹窗面板与**装备总属性小方块**、觅珍池主题色、抽卡标签。
- **深色边缘（2026-09-10 第三轮）**：亮色的白描边/白内高光在深色下改「极淡白描边 0.10 + 深色落影 rgba(0,0,0,.45)」，覆盖 18 类容器；守卫新增白边判定。
- **深色金边（2026-09-10 第四轮）**：深色档描边统一暖金——`--border` rgba(232,180,95,.26)（容器/表格/输入/分隔）、`--border-strong` rgba(240,196,118,.46)（按钮/徽章/磁贴）。
- **深色金光（2026-09-10 第五轮）**：金边之上加淡外发光（`--gold-glow` 0.12 / `--gold-glow-strong` 0.22，选中态加强），守卫含「金光 alpha ≤ 0.35」上限检查。
- **布局骨架与主题壁纸（2026-09-11，v1.7.1）**：三栏 = **三块直角面板**（`.app-sidebar` / `.app-main` / `.app-status`，`border-radius: 0`），**贴顶贴底贴左右**，只留面板之间 10px 竖缝；
  壁纸只铺 `.app-layout` 一层（亮 `bg-start.jpg` / 深 `bg-start-night.jpg`），**只在缝里露出**；三块面板底色 0.82/0.82/0.88（深色 `rgba(30,21,15,…)` 同构）+ **金色描边环**（`::after` + `mask` 只画 2px 环，按栏位渐隐：左栏向右、右栏向左、中区两端）；
  顶栏/底栏不再是独立浮块，而是中区面板内部的上/下分区（`1px` 细分隔线）；侧栏/右栏/中区滚动条改**透明材质**（轨道透明、滑块半透明）；
  **性能结论**：环上不要加任何动画——`background-position` 流光或 `opacity` 脉动都会让滚动帧时长从 16.6ms（60fps）掉到 44~56ms（~18-23fps），`will-change`/`translateZ` 提升无效，故金边保持静止。
  深色体检自本轮起把**三块面板与两条栏本身**也纳入扫描（原先只扫后代，漏掉了「深色下右栏变白底」这类面板级问题）。
- **弹窗**：签到（7 天循环奖励）、搜索（物品/对手/首领全局查找）、背包/仓库、存档、设置、随机奇遇
- **新建存档默认待机**：不自动开始挂机，由玩家选择挂机目标后开始
- **设置项**：对决自动进食（阈值可调）、挂机并行上限（0-3）、音效开关、**界面缩放档位滑条（80%-120%，`settings.uiScale` 随存档，启动自动应用）**
- **卡片式分段布局**：采集/制作/炼金页按类别/等级分段卡片（图片+属性+操作按钮贴底，可折叠）；**分类快速导航条**（一键跳到分段）；**一键回到顶部按钮**（主区域右下角，滚动超 300px 显示）
- 三栏：左侧技能导航（200px）+ 中间主区域（满宽）+ 右侧状态面板（280px，<1100px 隐藏）；右侧面板含 角色状态/挂机任务（停止/继续/✕关闭）/装备/事件日志（背包、仓库已移至顶部弹窗）
- **视图懒加载**（代码分割）：8 个视图 `defineAsyncComponent` 独立 chunk，主入口 48KB（gzip 17.7KB），进入页面时才加载对应视图
- 背包/仓库为弹窗；响应式（<1100 两栏 / <720 单栏 + 底部导航 + 技能抽屉）
- **制作页**：锻造=「锻造配方」/保鲜=「保鲜配方」/其余=「食谱」；列：成品/分类/效果/等级/材料/成功率/操作；**效果独立列**（统一规则：料理回血、饮品调味能量+醉酒、酱料增益、装备八槽位分类+属性摘要、保鲜/离线时长、原料兜底）
- **采集页**：状态行内联「距下次产出」进度条；统计框已移除，**双倍概率/成功率的实时值 + 完整机制提示语**（含附产物）并入目标列表标题行（｜ 分隔，同字号底部对齐）
- **全中文化**：用户可见文案无英文残留（首领/生命值/增益/经验/等级…）；分类 `CATEGORY_LABEL` 覆盖物品与食谱
- **成就进度条**：未解锁成就显示当前/需求进度（技能/对决/收集/探索/经济/赛季各类型）
- **攻略页**：**总览 + 六阶段**（2026-09-10 新增总览）——总览把全部功能按 6 类（核心循环 / 经营与产出 / 对战与挑战 / 养成与增益 / 长线与收集 / 休闲与元玩法）列成 69 条，每条含作用说明、解锁条件与**建议游玩阶段**徽章；六阶段（新手/前期/中期/后期/大后期/毕业）按对决等级自动定位，含目标/行动清单/里程碑/提示

## 13. 数据规模总表

| 项目 | 数量 |
| --- | --- |
| 技能 | **27**（采集 7 / 制作 6 / 对决 6 / 辅助 3 / **副业 5**：木工/陶艺/编织/刺绣/蜡烛制作；左栏 3 个页签：技能 / 功能 / 副业） |
| 物品总数 | **2303**（食材 539 / 料理 389 / 饮品 127 / 调料 106 / 种子 199 / 道具 47 / 装备 736 / 食灵 160） |
| 装备 | **736**（含 40 季 × 8 件套共 320 件赛季限定、同名矿/独立矿完整套、BOSS、扩充金属；套装表 81 套） |
| 采集目标 | 采摘 142 / 垂钓 72 / 狩猎 70 / 挖掘 42 / **伐木 20** / **采矿 43** |
| 食谱 | 烹饪 294 / 烘焙 97 / 腌制 90 / 调酒 127 / 调料 97 / 锻造 365 / 保鲜 18（含入门 1 + 肥料 2）+ 食灵召唤 160 + **副业 48（木工 10 + 陶艺 10 + 编织 10 + 刺绣 10 + 蜡烛 8）** = **1296** |
| 对手 / 首领 | 220 / 28（另有名厨挑战 10 位 / 竞技场镜像 / 秘境与塔动态生成） |
| 奥义 / 食灵 / 探索 | 32 / 160 / 200 |
| 公会 / 公会商店 | 20 家（4 类型需求 + buff 递增）/ 24 种 |
| 赛季 | 40（560 天循环，**每季 10 任务**〔`getSeason()` 的生效值；直接读 `SEASONS[].missions` 会得到 8/9/10 混合，因为任务由 `seasonContent.js` 覆盖生成〕/10 档/8 件套/46 个目标 param） |
| 成就 / 称号 / 主线任务 | **229** / **111** / 500 |
| 每日/周常任务 + 奇遇 | 每日 3 任务（**17 池**）/ 周常 1 目标（**14 池**）/ 奇遇 **22** 事件（21 三选一 + 1 二选一） |
| 无尽挑战塔 / 厨艺大赛 | 对决 99 解锁·层数无上限（对手等级封顶 140）/ 15 个主题池轮换（`YYYYMM % 15`，单一日历年内只会见到 12 个）·月分 3 档 |
| 食灵羁绊 / 餐厅好感 | 30 天满级（5 级 +20%）/ 20 级封顶（+57% 小费） |
| 餐厅装潢 | **310**（商店 300 种，收入 +0.5%~3%/件，随价格递增 **+ 手工 10 件**，+0.6%~4.2%，合计 +24%，由副业·木工产出） |
| 炼金配方 | 1499 条（价值微亏平衡：同级合成≈1.1~1.3×产出；补全矿物链+加工产物 28 条；所有种子可炼金；新增高端矿物多催化配方 8 条）；**`valueBalance` 收尾必须跑 `applyAlchemyRatioCap()`**（2026-09-12 加：按物品等级夹取会把产物抬到高于投入——实测 45 条违规，9 清水 18 金→1 苏打水卖 27 金＝无限刷钱；现把产物价值压回「投入价值总和」上界，C14 守卫） |

> **本表于 v2.10.0（2026-09-17）按代码逐项实测校准**（v2.6.0 首次校准；v2.9.0 校准物品/技能/采集目标/食谱/成就/称号/任务池/装潢；v2.10.0 再校准：技能 23→**27**、物品 2265→**2303**、食材 501→**539**、食谱 1258→**1296**（副业 48）、成就 217→**229**、称号 107→**111**、每日池 15→**17**、周常池 11→**14**）。
> 校准口径（以后复查照此跑）：物品表 `ITEMS` 按 `type` 分组（`ingredient`/`food`/`drink`/`spice`/`seed`/`consumable`/`equipment`/`spirit` 共 8 类，**无第 9 类**）·技能实例的 `targets` / `recipes`（`createSkillInstances` 后读，生成器产物已并入）·`COMBAT_REGIONS` 的 `opponents` 与 `COMBAT_BOSSES`·各数据模块的数组长度（`SEASONS` / `AOJIS` / `SPIRITS` / `EXPLORATION_TARGETS_ALL` / `GUILDS` / `GUILD_SHOP` / `ENCOUNTERS` / `RESTAURANT_DECOR` / `ALCHEMY_RECIPES`）·`ALL_ACHIEVEMENTS`（= `buildSkillAchievements()` 60 + `ACHIEVEMENTS` 148）·`QUESTS`（= `QUESTS_BASE` 48 + `QUESTS_EXT` 452）。

## 14. 测试与质量保障

| 套件 | 断言 | 覆盖 |
| --- | --- | --- |
**目录（2026-09-10 归类）**：`scripts/ci/`（门禁 11）· `scripts/gen/`（生成器 13）· `scripts/sim/`（标定模拟 3）· `scripts/legacy/`（smoke_test，非门禁）

| 套件（`scripts/ci/`） | 断言 | 覆盖 |
| --- | --- | --- |
| `system_test.mjs` | **795**（+ 修复前 system_test2 70/77、continuity 7/7 恒真的断言；含 C14 炼金价值比 / C15 离线幂等 / C16 离线·在线一致 / C17 `combat:end` 载荷 / C18 精通升级不减速 / C19 全数据模块物品 id 引用 / C20 皮肤组（token 齐备·对比度·面板深色）） | 技能/联动/对决/装备/离线/存档（含删除当前槽不写回回归）/背包仓库/经济/成就图鉴/数值安全/赛季/扩充完整性/攻略/制作技能 Lv1 入口保护/美食知识经验/探索卡片经验/农耕自动收种 |
| `system_test2.mjs` | 77 | 竞技场随机/挂机关闭/公会扩展（含辅助型 xpPct 数值型生效）/农耕肥料/竞技场奖励/新功能（签到·强化·装饰·败场·红点·存档）/奥义食灵/二期赛季/装备/全域数值扫描 |
| `smoke_test.mjs` | 114 | 全部技能机制真机回归（legacy，非门禁） |
| `buff_test.mjs` | 22 | 物品 buff/regen/醉酒/增益剂/食灵/奥义/公会被动 数据与战斗内生效 |
| `season_check.mjs` | 20 | 40 赛季唯一性/10 任务/10 档/8 件套/点数经济/需求区间 |
| `audit_sync.mjs` | 19 | 跨系统引用完整性/克制三角/攻略数值/成就阈值同步 |
| `continuity_test.mjs` | 7 | 切页/切后台挂机不中断 |
| `content_sync_audit.mjs` | **28** | **内容同步审计（图鉴三查之前必跑）**：任务 kind 处理/引用、成就 id·奖励·check 可执行、故事结构·需求分支、称号唯一、统计字段来源、攻略总览覆盖与导航视图注册、左栏功能页跳转条、**check 误用静态扫描** |
| `item_triple_audit.mjs` | — | 图鉴三查（详细作用/可用于制作/获取来源 + 重名 + 交叉引用 + 炼金幽灵配方基线），另含**效果字段必被描述**与**物品图片齐备**两项，CI 门禁 |
| `minigame_ui_audit.mjs` | — | 27 款小游戏 × 11 项 UI 静态合规，CI 门禁 |
| `action_sweep.mjs` | — | **381 个 store 动作 ×（满配 / 新档）**逐个真调（每个动作独立 player），异常 0；跳过 `Object.prototype` 与 `_` 前缀 |
| e2e（**4 套 15 项**） | — | `e2e-test`（流程/响应式/手机 390px 全页溢出/全 view key 注册）+ `e2e-dark`（深浅色对比度体检，**含按皮肤循环**）+ `e2e-text`（标签裸露：66 页 + 6 弹窗 + 页内页签）+ `e2e-audio-skin`（皮肤选择/锁定态/写盘重载恢复/BGM 启停与曲目） |
| `scripts/sim/balance_sim.mjs` | — | 解锁曲线（对决 10→12 场 … 90→10963 场）与 BOSS 节奏 |
| `scripts/sim/endgame_sim.mjs` | — | 端局 90-101 BOSS 胜率（满配验证） |
| `gen_drift_audit.mjs` | — | **生成器漂移审计**：13 个生成器跑进临时目录与产物按「行内容」比对，逐条打印「重跑会改什么」（已登记漂移 6 / 跳过 1，FAIL 须为 0）；产物哈希前后校验证明只读，CI 门禁 |
| `verify_modules.cjs` | — | 全模块编译 + 渲染风险静态检查 |
| `scripts/gen/*.mjs` | — | 13 个内容生成器（确定性、唯一性）。⚠️ **重跑有门禁**：5 个产出「已固定」冻结数据的生成器（`gen_exploration_targets` / `gen_expansion` / `gen_expansion2` / `gen_quests` / `gen_season_gear`）默认拒绝写仓库，确需重跑要 `ALLOW_FROZEN_REGEN=1`；`gen_smith_sets` / `gen_farm_seeds` 另有自引用陷阱。产出是「活数据」，生成器输出 ≠ 正确版本，详见 AGENTS.md 数据铁律 |
